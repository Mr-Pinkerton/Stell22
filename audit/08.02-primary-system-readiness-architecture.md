# ARCH-2 — Primary-System Readiness Architecture

**Mode:** DOCUMENTATION / ARCHITECTURE ONLY. No application-code, schema, migration, bootstrap, activation, production mutation, or deploy.

```text
AUDIT_REPO_BASE_SHA=71a01b40008cf6da7cbc843b3b0dbaa31cf01853
APPLICATION_CODE_BASE_SHA=71a01b40008cf6da7cbc843b3b0dbaa31cf01853
BRANCH=audit/arch2-primary-system-readiness
DATE=2026-09-12
MODE=ARCHITECTURE CONTRACT
STATUS=ACCEPTED / REVIEWED
INDEPENDENT_REVIEW_1=REQUEST CHANGES (R1–R6 CLOSED/PASS)
INDEPENDENT_REVIEW_2=REQUEST CHANGES (R7–R8 CLOSED/PASS)
INDEPENDENT_REVIEW_3=PASS / ACCEPT
CANONICAL_ON_MAIN=YES (after reviewed merge of this document)
NAMESPACE=PSR (Primary-System Readiness)
```

**Canonicality:** GitHub `main` is the source of truth. After Independent Review #3 **PASS / ACCEPT** and the reviewed merge of this file, `audit/08.02-primary-system-readiness-architecture.md` on `main` is the **canonical ARCH-2 architecture contract**.

`ACCEPTED / REVIEWED` means the **architecture contract** is accepted. It does **not** mean PSR implementation is complete, primary-system readiness is achieved, the ledger is deployed, paper can already be removed, or `production_cost_flow` is active.

Charter: `audit/00-audit-principles.md`. File contract: `.cursor/rules/audit-truth.mdc`.

This document is **not** a reopening of AUDIT 1. It does **not** replace `audit/08.01-audit-1-system-architecture.md` and does **not** reopen `audit/03.02-production-cost-flow-architecture.md` owner decisions.

| Document | Question it answers |
| --- | --- |
| `audit/08.01` | What is the **current** whole-system architecture, and which defects exist today? Status: **COMPLETE / REVIEWED**. |
| `audit/03.02` | How should **monetary cost** move with production stock after `production_cost_flow` activation? Status: **COMPLETE**. |
| `audit/08.02` (this file) | What **minimal foundation** must exist before Stell22 can be the **only** warehouse/production source of truth, with paper no longer a parallel insurance system? |

Readiness gaps use the **PSR-*** namespace. They are **not** automatically `CONFIRMED BUG` / P1 / P2. AUDIT 1 defect classification remains a separate ledger.

Do **not** import Woodveri / W architecture, door entities, generic `CommandExecution` buses, or event-sourcing frameworks.

---

## 0. Independent review record

ARCH-2 status: **`ACCEPTED / REVIEWED`**.

This is the accepted **architecture contract**. It is **not** a full PSR implementation-complete signal. PSR-P0 = **COMPLETE / ACCEPTED** as an architecture/design contract only (`audit/08.03` + `audit/08.04`; ACCEPTED DESIGN / NOT IMPLEMENTED). PSR-P1 = **COMPLETE / IMPLEMENTED / DEPLOYED / SHADOW SCHEMA EMPTY** (`audit/08.06`). PSR-P2 dual-write is **NOT STARTED**. Independent Review #3 found **no new architecture blockers**.

R1…R8 are **CLOSED / PASS**. They are **not** reopened.

### Review #1

| | |
| --- | --- |
| Verdict | **REQUEST CHANGES** (findings applied; now **CLOSED / PASS**) |
| Date | 2026-09-12 |
| Resulting disposition | R1…R6 remain satisfied. Do not reopen. |

| ID | Finding | Disposition |
| --- | --- | --- |
| `ARCH2-R1` | Ledger epoch / shadow ordering unsafe | **CLOSED / PASS.** Pre-epoch shadow excluded from reconstruction (§6.3 A/C). |
| `ARCH2-R2` | Authoritative cutover preceded history-preserving correction | **CLOSED / PASS.** Correction remains before cutover. **Strengthened by R7:** also before the **final** epoch. |
| `ARCH2-R3` | Ledger-vs-projection ≠ physical-world recon | **CLOSED / PASS.** `PSR-MUST-008`. |
| `ARCH2-R4` | Gateway incomplete; DI-011 conditional | **CLOSED / PASS.** `PSR-MUST-007` + DI-011 conditional gate. |
| `ARCH2-R5` | Historical reference retention undefined | **CLOSED / PASS.** `PSR-DESIGN-002`. |
| `ARCH2-R6` | Branch artifact as if already main truth | **CLOSED / PASS.** Distinguishing path vs main truth was required until Review #3 merge. This file on `main` is now the canonical ARCH-2 contract. |

### Review #2

| | |
| --- | --- |
| Verdict | **REQUEST CHANGES** |
| Date | 2026-09-12 |
| Resulting disposition | R7/R8 applied; then **CLOSED / PASS** after Review #3. |

| ID | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| `ARCH2-R7` | **BLOCKER** | Final authoritative-candidate epoch currently precedes history-preserving correction/cancellation | **CLOSED / PASS.** Final epoch **after** destructive posted-op paths are removed/blocked. No authoritative post-epoch interval may contain hard-deleted source facts. |
| `ARCH2-R8` | **MAJOR** | Schema-shaping PSR questions not required before InventoryMovement schema | **CLOSED / PASS.** PSR-P0 must close Q1/Q2/Q5/Q7/Q8 before PSR-P1. |

### Review #3

| | |
| --- | --- |
| Verdict | **PASS / ACCEPT** |
| Date | 2026-09-12 |
| New blockers | **0** |
| Resulting disposition | Architecture contract accepted. R1…R8 **CLOSED / PASS**. NEXT for **this Review #3 task** = PSR-P0 (not started in that task). **Historical.** Later current next: see §24.4 (`audit/08.06`). |

---

## 1. Purpose / non-goals

### Purpose

Define the smallest architecture that lets Stell22 become the **primary operational source of truth** for warehouse and production quantity.

Today Stell22 already runs a real shop-floor chain and is deployed. Paper (and operator memory) still functions as a parallel insurance system because:

- current `*Stock` rows are mutated balances without an append-only physical movement history;
- posted `ProductionOperation` can still be physically deleted (except generic TORCOVKA delete, which is contained);
- consequential manual corrections do not always carry actor + reason + causal identity;
- there is no ledger-vs-projection reconciliation that can prove `OPENING_BALANCE + post-epoch movements = live stock`;
- ledger==projection still would not prove system==factory; physical-count coverage is incomplete (raw rails excluded from current Inventory; WIP keys not equivalent to DETAIL lines).

ARCH-2 is the contract for that foundation. It is a **forward-looking** architecture. Acceptance does not implement anything.

### Non-goals

This architecture contract does **not**:

- reopen AUDIT 1 or change its P0/P1/P2/P3 counts;
- start AUDIT 2 — FINANCE & MONEY INTEGRITY;
- activate `production_cost_flow`;
- apply cost-flow bootstrap;
- invent a second monetary journal;
- copy Woodveri;
- require PeriodClose before the first physical-ledger cutover;
- require arbitrary backdating for the first cutover;
- require a generic persistent `CommandExecution` table;
- rewrite Stell22 into microservices, Kafka, CQRS, or full event sourcing;
- implement any schema/code in this task.

---

## 2. Verified baseline

**ARCH-2 acceptance snapshot (historical, 2026-09-12).** Review #3 accepted the architecture contract against this application SHA. This section is **not** the current production checkpoint.

Dated production pointer (2026-09-17, **historical**): `0686d0036da52cc32aaff531c91f1dd2ded899ec`. See §24.1. Current production: see §24.7.

Verified at ARCH-2 acceptance:

```text
git fetch origin
git rev-parse origin/main                                  → 71a01b40008cf6da7cbc843b3b0dbaa31cf01853
```

At that date, `origin/main` application checkpoint was `71a01b4`. An ARCH-2 docs merge was **not** a production application deploy. Later application SHA `0686d00` (PR #3 / Production Deploy `35204366279`) superseded `71a01b4` as the running production application. Do not treat `71a01b4` as current.

| | |
| --- | --- |
| Repository | `Mr-Pinkerton/Stell22` |
| Canonical `origin/main` (application) **at ARCH-2 acceptance** | `71a01b40008cf6da7cbc843b3b0dbaa31cf01853` — `Merge pull request #1 from Mr-Pinkerton/integration/p2025-di020`. **Historical.** Current production = `0686d00`. |
| ARCH-2 review branch | `audit/arch2-primary-system-readiness` (architecture accepted; merged as documentation only) |
| Application code at that SHA | **yes** (not a docs-only commit) |
| Production Deploy | run `34635510843` SUCCESS on 2026-09-11 (`workflow_dispatch`; jobs Resolve SHA / CI verify / Deploy origin/main all SUCCESS) |
| Prior documented prod SHA | `3608b36bd324a5118f4ba5b1bbb21462cc0723d9` (INC-001 containment) — **superseded as running SHA** by the 71a01b4 deploy |
| `production_cost_flow` | **INACTIVE** (no app writer; Setting absent or `active: false`; `PROJECT.md` activation BLOCKED) |
| Cost-flow bootstrap | **NOT APPLIED** |
| Close Month | **NOT IMPLEMENTED** |
| AUDIT 1 | **COMPLETE / REVIEWED** — not reopened |
| `ARCH-P1-001` | **OPEN / CONFIRMED** — ACTIVE-path scope only (see §23) |
| INC-001 | **OPEN** — containment remains deployed; production data **not** corrected in this pass |
| AUDIT 2 | **NOT STARTED** |

Application commits after the stale `3608b36` checkpoint that are now in `origin/main` (and deployed with 71a01b4):

- INC-001 containment docs (`72ac62b`);
- local UI filters (cashflow / production / warehouse / sales / goals);
- TORCOVKA BlankStock length canonicalization and railsTaken correction hardening (`9c87cf4` … `ac245be`, merged as PR #1).

Those application changes **do not** invalidate the ARCH-2 decisions below. Length canonicalization hardens BlankStock identity for the future projection key; it is not a physical ledger. Generic TORCOVKA delete remains blocked (`src/lib/torcovka-delete-policy.ts`).

Documents actually read for this draft:

1. `START-HERE.md`
2. `PROJECT.md`
3. `audit/AUDIT-INDEX.md`
4. `audit/00-audit-principles.md`
5. `.cursor/rules/audit-truth.mdc`
6. `audit/08.01-audit-1-system-architecture.md`
7. `audit/03.02-production-cost-flow-architecture.md`

Implementation files checked against current `origin/main` (read/grep; no edits):

`prisma/schema.prisma`, `src/server/terminal.ts`, `src/server/production.ts`, `src/server/warehouse.ts`, `src/server/purchases.ts`, `src/server/payroll.ts`, `src/server/change-log.ts`, `src/server/internal/inventory-integrity.ts`, `src/server/internal/finance-operations.ts`, `src/server/internal/cost-flow-state.ts`, `src/server/internal/cost-flow-raw.ts`, `src/server/internal/cost-flow-downstream.ts`, `src/server/internal/cost-flow-pools.ts`, `src/lib/torcovka-delete-policy.ts`.

---

## 3. Current architecture relevant to primary-system readiness

Stell22 is a **modular monolith**: Next.js Server Actions → Prisma → one PostgreSQL, one app process.

Physical chain (unchanged, accepted):

```text
Batch / RailLot
  → TORCOVKA → BlankStock
  → PRISADKA → DetailStock
  → UPAKOVKA → ProductStock
                ↘ NomenclatureStock (SimplePurchase in; UPAKOVKA consume)
Inventory CONDUCTED → absolute *Stock SET
Marketplace Supply → ProductStock deduct (second FG writer; fail-closed if cost-flow active)
```

### 3.1 What currently counts as physical truth

| Object | Current role (08.01) | Readiness problem |
| --- | --- | --- |
| `RailLot.remainingQuantity` | identified primary fact | mutated in place; no movement register of consumes/receipts |
| `BlankStock` / `DetailStock` / `NomenclatureStock` / `ProductStock` | primary current balances | same: live qty without reconstructable movement history |
| `ProductionOperation` + lines | posted production fact | unpaid PRISADKA/UPAKOVKA/HOURS can be **physically deleted**; TORCOVKA generic delete is contained |
| `OperationDetailLine` / `OperationNomenclatureLine` | provenance snapshots | survive later BOM edits; lost if the parent op is deleted |
| `Inventory` CONDUCTED | physical count document + DI-009 boundary | absolute SET of projections; not a movement ledger of the shop-floor flow |
| `ChangeLog` | partial history | `userId` populated mainly from settings; terminal/production/payroll/warehouse writes often omit actor |
| `clientRequestId` | terminal submit idempotency | unique per op; not a generic command bus; admin corrections do not get a new request id |

There is **no** `InventoryMovement` (or equivalent) model in `prisma/schema.prisma`.

Runtime stock writers today (inactive gate — current production):

- `terminal.submitTorcovka` / `submitPrisadka` / `submitUpakovka`
- `production.updateProductionLineQuantity` / `correctTorcovkaRailsTaken` / `deleteProductionOperation` (TORCOVKA delete rejected before mutation)
- `warehouse.conductInventory` absolute SET
- `purchases.createSimplePurchase` nomenclature receive; batch/lot create; write-off remaining rails
- marketplace `applySupplyDeduction` on `ProductStock`

Active cost-flow writers exist behind `isCostFlowActive` and are **not** the current production path.

### 3.2 Current correction / delete semantics (runtime — do not change now)

| Action | Current runtime | Target (this draft) |
| --- | --- | --- |
| Generic TORCOVKA delete | **Rejected** before mutation (`assertTorcovkaGenericDeleteAllowed`). INC-001 containment. Keep. | Containment stays until an explicit cancellation model exists. |
| Unpaid PRISADKA / UPAKOVKA / HOURS delete | Physical `delete` of `ProductionOperation` after reverse | **Superseded target:** posted op is not physically destroyed |
| Unpaid qty edit | Mutates the same op/lines; reverse+reapply | Preserve original fact + explicit delta/effects |
| Paid op | Blocked (`isPaid`) | Keep payroll finality |
| Active UPAKOVKA qty edit | snapshot current BOM → reverse old refs → apply current BOM **without** union boundary | **ARCH-P1-001** remains OPEN on this path; gate stays inactive |

### 3.3 Time fields that already exist

| Field | Meaning today |
| --- | --- |
| `ProductionOperation.createdAt` | server recording time (`@default(now())`) |
| `ProductionOperation.workDate` | business day for payroll/stats |
| `Inventory.date` | inventory document date (DI-009 boundary uses `date > occurredAt`) |
| `Payment.paidAt` | pay instant |
| `ChangeLog.changedAt` | log time |

There is no first-class `recordedAt` vs `effectiveAt` on a physical movement row, because there is no movement row.

### 3.4 Cost interaction (must not be silently changed)

`audit/03.02` remains the monetary architecture:

- component WAC on pooled stock when the gate is active;
- exact operation/line monetary snapshots;
- `RailLot` remaining/consumed values;
- `costVersion` exact reverse or BLOCK;
- `CostEvent` for non-flow monetary events;
- `CostPeriod` / `ProductCost` as the month-close target;
- HOURS in a monthly pool, not in Blank/Detail/Product.

`production_cost_flow` is inactive. Live `getCostReport` still reconstructs. That is accepted sequencing, not a reason to add a second money journal.

---

## 4. What is already strong and must be preserved

These are current assets. ARCH-2 must not throw them away.

1. **Modular monolith** on one Postgres / one app process — accepted. No rewrite.
2. **One-TX production submit/correct** for stock + operation (exception rolls back).
3. **Terminal idempotency** via `ProductionOperation.clientRequestId` UNIQUE NOT NULL (DI-007/008 closed).
4. **DI-015 rate snapshots** on the operation; unpaid payroll reads snapshots, not live employee cards.
5. **Payment finality:** `isPaid` claim + `Payment` + unique `PaymentBatchItem.operationId`; no in-app void (REVIEW-023 remains known).
6. **Inactive UPAKOVKA qty-edit union** `old refs ∪ current BOM refs` in `prepareUpakovkaEdit` **before** reverse (`inventory-integrity.ts`). This is the current inactive runtime. Do not claim it is missing.
7. **DI-009 inventory boundary** on CONDUCTED documents for covered refs (inactive path).
8. **INC-001 containment:** generic TORCOVKA delete does not restore `RailLot` and is therefore blocked. Do not invert INV-047.
9. **Exact-or-block monetary reverse** (`costVersion`) in 03.02 — keep for money; do not replace with current-WAC reverse.
10. **Operation line provenance** (blank spec, source flags, nomenclature lines) — keep as the raw/WIP consumption snapshot.
11. **BlankStock identity** by `(materialId, lengthM, detailType, sort)` including post-71a01b4 length canonicalization.
12. **Cost-flow gate fail-closed** (`isCostFlowActive`; malformed JSON fails closed). Gate stays inactive.
13. **Existing *Stock tables** — they stay. They become projections, not deleted tables.

---

## 5. Primary-system readiness requirements

Classification rules for this file only:

| Prefix | Meaning |
| --- | --- |
| `PSR-MUST-*` | Required before Stell22 can be the sole warehouse/production operational SoT. **Not** a current P1 bug label. |
| `PSR-DESIGN-*` | Required capability; implementation shape is still a design choice. |
| `PSR-DEFER-*` | Explicitly not an initial physical-ledger cutover blocker. |
| `PSR-REJECT-*` | Consciously rejected for this horizon. |

PSR items are **readiness gaps**. They do not reopen AUDIT 1 counts and are not `CONFIRMED BUG` unless an existing AUDIT 1 / DI / INC card already says so.

---

## 6. Physical inventory ledger decision

**ACCEPTED.**

Before Stell22 is the only warehouse/production SoT, introduce an **append-only physical inventory movement register**.

Conceptual flow:

```text
business fact
  → physical movement(s)
  → current stock projection
```

A suitable name: `InventoryMovement`.

It represents **physical quantity history**, not generic business events and not an accounting GL.

Current tables **MUST NOT be deleted**:

- `BlankStock`
- `DetailStock`
- `NomenclatureStock`
- `ProductStock`

Target role of those tables: **current-balance projections / fast read models**.

Do **not** propose ledger-only runtime reads. Do **not** propose full event sourcing.

### 6.1 Movement kinds (illustrative, not a frozen enum dump)

| Kind | Typical cause |
| --- | --- |
| `PURCHASE_RECEIPT` | Batch/RailLot receive; SimplePurchase |
| `PRODUCTION_CONSUME` | TORCOVKA rails; PRISADKA blanks/WIP; UPAKOVKA details/nom |
| `PRODUCTION_OUTPUT` | TORCOVKA blanks; PRISADKA details; UPAKOVKA FG |
| `SHIPMENT` | Marketplace/supply FG deduct |
| `RETURN` | later, if a physical return path exists |
| `SCRAP` | explicit physical loss (not silent reverse) |
| `INVENTORY_ADJUSTMENT` | CONDUCTED inventory SET → signed delta vs accounted |
| `REVERSAL` | compensating movement that references original effects |
| `OPENING_BALANCE` | ledger epoch only; **do not fabricate pre-epoch history** |

Do **not** invent unit-level serial tracking.

### 6.2 Provenance grain (accepted)

- **Raw wood:** `RailLot`-level identity remains. `RailLot.remainingQuantity` / remaining value stay lot-identified, not a mixed WAC pool.
- **Downstream WIP/FG:** operation/movement-level provenance. Pooled `BlankStock` / `DetailStock` / `NomenclatureStock` / `ProductStock` stay fungible. No serial-per-part.

`InventoryMovement` may reference:

- stock kind + pool identity (blank spec / detail flags / nomenclatureId / productId / railLotId);
- causing document (`ProductionOperation.id`, `Inventory.id`, `SimplePurchase.id`, `Supply.id`, …);
- optional reversal pointer to the movement(s) being compensated.

Exact Prisma columns are PSR-P0 / PSR-P1 work. This contract accepts the **role**, not a frozen schema dump. Historical reference retention (`PSR-DESIGN-002`) must be defined before that schema is accepted.

`PSR-MUST-001` — Append-only physical movement register.

### 6.3 Ledger epoch protocol (ARCH2-R1, strengthened by ARCH2-R7)

**MANDATORY.** Dual-write **before** an epoch must not double-count (R1). The **final** epoch that can later become authoritative must not precede destructive-history elimination (R7).

#### A. SHADOW BEFORE EPOCH

`InventoryMovement` may be dual-written for diagnostics before the **final** authoritative-candidate epoch.

Those rows:

- are **pre-epoch shadow evidence only**;
- **MUST NOT** automatically participate in authoritative balance reconstruction;
- must be distinguishable from the post-epoch authoritative stream (`PSR-Q-007`).

A rehearsal / test epoch, if ever created during implementation, is **NON-AUTHORITATIVE and disposable**. It must not become the retained authoritative epoch.

#### B. FINAL EPOCH CUT

`PSR-MUST-003` refers to this **FINAL authoritative-candidate epoch**, not a shadow/test epoch.

**Precondition (ARCH2-R7 / PSR-MUST-005):** posted physical production facts already have history-preserving correction/cancellation, **or** every destructive posted-fact mutation path is disabled. Generic TORCOVKA-delete containment stays throughout.

Then:

1. prevent concurrent physical stock mutations using a bounded write barrier / maintenance gate / equivalent transaction-safe cutover mechanism (`PSR-Q-006` before this cut);
2. capture **one consistent snapshot** of every authoritative physical projection (`RailLot.remainingQuantity`, `BlankStock`, `DetailStock` including WIP keys, `NomenclatureStock`, `ProductStock`);
3. create `OPENING_BALANCE` movements representing **that snapshot only**;
4. persist an **immutable ledger epoch** identity / time / sequence;
5. ensure no mutation can fall ambiguously across the epoch boundary.

Do **not** fabricate movements for pre-epoch shop history. Opening = current qty at the cut, not reconstructed past.

Do **not** allow “block destructive paths only at authoritative cutover” if the **retained** epoch predates that block. No authoritative post-epoch interval may contain destructively erased business facts.

Exact barrier/epoch storage is an implementation design (`PSR-Q-006`). The semantics above are not.

#### C. POST-EPOCH

Authoritative reconstruction is:

```text
OPENING_BALANCE at the FINAL epoch
  + movements belonging to the post-epoch authoritative stream
  = current projection quantity
```

Pre-epoch shadow rows are **excluded** from this equation.

`PSR-MUST-003` — **FINAL** authoritative-candidate ledger epoch: write barrier + consistent snapshot + `OPENING_BALANCE` + immutable epoch identity. Established only after destructive posted-fact paths are gone/blocked. Pre-epoch shadow and any disposable rehearsal epoch are not this opening and are not the post-epoch stream. Do not fabricate historical movements.

---

## 7. Projection model

**ACCEPTED.**

Target invariant **after epoch**:

```text
OPENING_BALANCE at epoch
  + movements belonging to the post-epoch authoritative stream
  = current projection quantity
```

Pre-epoch shadow dual-writes are **not** in this sum.

Rules:

- Movement append and projection mutation **must be atomic in one DB transaction**.
- Runtime reads continue to use `*Stock` (and `RailLot.remainingQuantity` for lots). **No ledger-only reads.**
- A reconciliation job/query must reconstruct quantities from **epoch opening + post-epoch authoritative movements** and compare them to live projections (`PSR-MUST-004`). That proves ledger == projection. It does **not** prove system == factory (`PSR-MUST-008`).
- Drift is an integrity failure, not a UI inconvenience.
- Never silently edit a projection to match ledger or reality. Adjustments are explicit movements.

`PSR-MUST-002` — Current `*Stock` tables become explicit projections with atomic movement+projection writes.

`PSR-MUST-004` — Automated **ledger-vs-projection** reconciliation (internal consistency only).

`RailLot.remainingQuantity` is an identified-lot balance, not a fungible `*Stock` row. After cutover it obeys the same reconstructability rule (epoch opening remaining + post-epoch lot movements = live remaining). It is in scope for the gateway and both reconciliation kinds.

---

## 8. Provenance model

**ACCEPTED grain; no serials.**

| Layer | Provenance |
| --- | --- |
| Raw | `RailLot` identity (code, length, type, sort, remaining) |
| TORCOVKA output | Blank spec `(materialId, lengthM, detailType, sort)` + causing operation/movement |
| PRISADKA | Detail + prisadka flags; source blank vs WIP recorded on lines today |
| UPAKOVKA | Operation detail/nom lines (historical consume); live BOM only for **new** packs |
| FG | `ProductStock` pool per product; shipment via `Supply` |

Do not add per-piece serials, package-in-WIP layers, or FIFO layers as a primary-system prerequisite.

Existing line snapshots (`OperationDetailLine`, `OperationNomenclatureLine`) remain the **operation-level** consume/output record. After cutover they must stay consistent with the movements they caused. They are not a substitute for `InventoryMovement`.

Referenced master/source identities on movements must remain historically interpretable (`PSR-DESIGN-002`).

---

## 9. Correction / cancellation / reversal semantics

**TARGET SUPERSEDED for posted production hard delete. Runtime unchanged in this documentation pass.**

Do **not** change current delete/correct code in this task.

**Ordering invariant (`ARCH2-R2` + `ARCH2-R7`):**

```text
history-preserving posted facts
  BEFORE final authoritative-candidate epoch
  BEFORE authoritative cutover
```

Destructive posted-operation mutation must be gone or blocked **before the FINAL epoch**, not merely at cutover. Do not retain an epoch that predates that block.

Generic TORCOVKA-delete containment remains intact throughout.

### 9.1 Posted-fact immutability

Target invariant:

A posted physical fact must remain **historically reconstructable** after correction.

It is acceptable for a current normalized representation to exist, but **original posted values/effects must survive in immutable history**.

A correction **must not** rely only on `ChangeLog` JSON after overwriting or deleting the source fact.

| Mode | Target semantics |
| --- | --- |
| Correction | Original posted fact remains reconstructable in immutable history. Current representation may show net result. Effects are explicit deltas, not silent overwrite of the only copy. |
| Cancellation | A **new causal action** with compensating physical movements and a retained reference to the original fact. The original remains identifiable as cancelled. |
| Reversal | Explicitly references original effects (movements and/or stored component snapshots). |

Posted `ProductionOperation` is **not physically destroyed** in the target model.

### 9.2 What stays from current runtime until the target exists

- Generic TORCOVKA-delete containment (`TORCOVKA_GENERIC_DELETE_BLOCKED`). Do not remove it.
- `isPaid` blocks edit/delete.
- Inactive inventory-boundary + gte guards.
- Active `costVersion` exact-or-block (when the gate is on).
- `correctTorcovkaRailsTaken` as the path for over-entered rails that were never taken — distinct from cancelling a physical cut.

Current unpaid PRISADKA / UPAKOVKA / HOURS **hard delete** remains a **FINAL-epoch blocker** until PSR-P5.

### 9.3 INC-001 relationship

INC-001 showed why hard delete of a posted TORCOVKA is not a safe “undo”: rails are not restored, the op row disappears, and reconstruction depends on `ChangeLog`. Containment is deployed. A proper `cancelErroneousTorcovka` is **not** implemented.

ARCH-2 does **not** close INC-001 and does **not** implement Scenario A/B. It records that the **target** cancellation model is the replacement for both generic delete and the missing cancel path.

`PSR-MUST-005` — Before the **final** ledger epoch, posted physical production facts must already have history-preserving correction/cancellation semantics, or all destructive posted-fact mutation paths must be disabled. No authoritative post-epoch interval may contain destructively erased business facts. Cancellation is a new causal action; original posted facts remain reconstructable.

---

## 10. Temporal semantics

**ACCEPTED WITH RESTRICTION.**

The target movement/history model distinguishes:

| Field | Meaning |
| --- | --- |
| `recordedAt` | immutable server recording time |
| `effectiveAt` | business/physical occurrence time |

`ProductionOperation.workDate` remains a payroll/statistics business day. It is **not** automatically permission to backdate stock.

**Restriction for first physical-ledger cutover:**

- initial cutover **MAY** keep `effectiveAt` server-controlled and equal to current time (`recordedAt ≈ effectiveAt`);
- **arbitrary user backdating MUST remain disabled** until historical validation/replay and period rules exist;
- full backdating is **not** a prerequisite for the first physical-ledger cutover.

`PSR-DEFER-001` — Arbitrary backdating.

---

## 11. Causality / audit semantics

**REQUIREMENT ACCEPTED; generic `CommandExecution` is NOT YET ACCEPTED.**

Do **not** copy Woodveri. Stell22 already has domain identities:

- `ProductionOperation.clientRequestId`
- `ProductionOperation.id`
- `Inventory.id`
- `Payment.id`
- `SimplePurchase.id`
- `Supply` identity (`marketplace` + `externalId` + sku)
- `CashFlow.id` / `Deal.id` (finance contour; not this ledger)
- `ChangeLog.id` (partial)

Every **consequential** mutation must be causally attributable:

- actor (admin `User.id` or terminal `Employee.id`);
- operation/document identity;
- recorded time;
- reason when a correction/manual mutation requires one;
- correlation/causation identity where a retry or compensating action must point at the original;
- release/deploy trace where practical;
- **atomic audit with the mutation** (same DB transaction).

Current gap (readiness, not a new AUDIT 1 P1): `writeChangeLog` supports `userId`, but most production/warehouse/payroll/terminal writers omit it. Settings writes include `admin.id`. That is insufficient for primary-system corrections.

**`PSR-DESIGN-001` = CLOSED / ACCEPTED (2026-09-17, `audit/08.04`):**

No universal `CommandExecution` / generic command bus for Stell22. Narrow domain causal contexts.

Production-correction slice remains **ACCEPTED** as `ProductionOperationMutation` (`audit/08.03`). Normal physical postings use the retained domain document (`ProductionOperation`, `Inventory`, `SimplePurchase`, `Batch`/`RailLot`, `Supply`) and persist causal identity on `InventoryMovement`. Manual actions without a retained source persist `causationId` / actor / reason on the movement. Payroll remains `Payment` / `PaymentBatchItem`. Finance import remains `CashFlow` / `importKey`.

Canonical remainder contract: `audit/08.04-psr-p0-core-contract.md` (**ACCEPTED DESIGN / NOT IMPLEMENTED**).

Do **not** create a universal command bus.

`PSR-MUST-006` — Consequential manual corrections carry actor + reason + causal identity + atomic audit. **Specified for production corrections in 08.03; NOT implemented in runtime.**

### 11.1 Movement idempotency invariant

Every movement-producing business effect must be **idempotently identifiable**.

A retry/replay of the **same** business effect must **not** append a second physical movement or mutate the projection twice.

This applies to terminal submit **and** to correction, cancellation, inventory adjustment, receipt, shipment/restoration, write-off, and any future movement-producing path.

`ProductionOperation.clientRequestId` remains a useful domain identity for terminal submit. It is **not** a universal solution.

Exact uniqueness / source-effect identity = `PSR-Q-002` **CLOSED / ACCEPTED** in `audit/08.04` (`UNIQUE(causationKind, causationId, effectKey)` conceptually; `effectKey` must be retry-stable from business semantics, not newly generated line ids). Do **not** introduce a universal command bus solely for this. Exact Prisma names are not frozen here.

### 11.2 Historical reference retention (`PSR-DESIGN-002`, ARCH2-R5)

Before `InventoryMovement` schema is accepted, define how movement history stays interpretable when referenced domain entities would otherwise be deleted.

The A-vs-B fork is **CLOSED / ACCEPTED** as a **hybrid** in `audit/08.04` (retain authoritative source documents **and** immutable identifying snapshot for mutable masters). The bullets below remain the original problem statement from ARCH-2.

For identities referenced by authoritative movements / causality, the original fork was **one of**:

- **A.** archive + FK/restrict physical deletion after first historical use; or
- **B.** immutable identifying snapshot stored on the movement where reference deletion must remain possible.

Do **not** silently allow an append-only movement to point at an identity that can disappear with no historical representation.

Applies conceptually to relevant: `ProductionOperation`, `Product`, `Detail`, `Material`, `NomenclatureItem`, `RailLot` / source documents.

Do **not** freeze a Prisma schema in this architecture contract.

`PSR-DESIGN-002` — Historical reference retention. **CLOSED / ACCEPTED** in `audit/08.04` as a **hybrid** (retain authoritative source documents + immutable identifying snapshot for mutable masters). Exact JSON-vs-columns belongs to PSR-P1 schema review.

### 11.3 Pre-schema design gate (`ARCH2-R8`)

PSR-P1 (`InventoryMovement` schema / migration) **cannot start** until PSR-P0 has reviewed and accepted every question that materially shapes that schema. Those questions are **CLOSED / ACCEPTED** in `audit/08.04`. PSR-P1 may begin **after reviewed merge of `08.04`**, as InventoryMovement schema in SHADOW only.

| Must close in PSR-P0 | Why it shapes schema |
| --- | --- |
| `PSR-Q-001` | Causal persistence: generic `CommandExecution` vs narrower domain context. **CLOSED / ACCEPTED** (`audit/08.04`). No universal command bus. Production-correction slice remains `ProductionOperationMutation` (`audit/08.03`). |
| `PSR-Q-002` | Movement identity / uniqueness / idempotency contract. **CLOSED / ACCEPTED** (`audit/08.04`). |
| `PSR-Q-005` | Actor representation. **CLOSED / ACCEPTED** (`audit/08.04`): `USER` / `EMPLOYEE` / `SYSTEM`. |
| `PSR-Q-007` | How shadow movements are labelled and excluded from authoritative history. **CLOSED / ACCEPTED** (`audit/08.04`): explicit `SHADOW` / `AUTHORITATIVE`. |
| `PSR-Q-008` | Historical reference retention. **CLOSED / ACCEPTED** (`audit/08.04`): hybrid A+B by entity role. |

Those decisions **may** produce: no generic CommandExecution table; narrow fields/context; DB uniqueness; movement source/effect identity; epoch/shadow marker; FKs and/or immutable identity snapshots.

ARCH-2 does **not** freeze Prisma column dumps. It froze the **sequencing**: unresolved schema-shaping questions blocked PSR-P1. That PRE-SCHEMA gate is now closed in `audit/08.04`.

---

## 12. Cost / valuation interaction

**REJECT a separate valuation journal for now.**

Do **not** introduce `InventoryValuationEntry` / generic monetary movement journal at this stage.

Reason: `audit/03.02` already establishes monetary truth through:

- component WAC pools;
- exact operation/line monetary snapshots;
- `RailLot` values;
- `costVersion`;
- `CostEvent` for non-flow monetary events;
- `CostPeriod` / `ProductCost` target close model.

A second monetary journal would create another money source of truth.

`InventoryMovement` is initially **quantity/provenance** focused. Monetary fields on movements are **not** accepted in this draft.

If a future requirement proves a separate monetary journal is necessary, that is a **new architecture decision**. It is not this file.

Do **not** silently change accepted 03.02 cost accounting semantics.

`PSR-REJECT-001` — Separate valuation journal / `InventoryValuationEntry` at this stage.

`PSR-DEFER-002` — Cost-flow integration of the physical ledger (after quantity cutover). Includes **ARCH-P1-001** remediation on the ACTIVE path.

### 12.1 Explicit supersession vs 03.02 “no movement ledger”

`audit/03.02` §6.1 / §23 deliberately rejected a **cost** movement ledger (“component columns + `costVersion` instead of a ledger”).

ARCH-2 **does not reopen** that monetary decision. It **does** accept a **physical quantity** register.

| 03.02 statement | ARCH-2 reading |
| --- | --- |
| Do not add a (cost) movement ledger | **Still accepted** for money |
| Do not add `EmployeeDayCost` | Unchanged |
| `CostEvent` for non-flow money | Unchanged |
| Physical `InventoryMovement` | **New**, quantity/provenance only |

This is a scoped supersession of “no movement ledger” for **physical quantity**, not a competing cost-period or WAC model.

---

## 13. Payroll finality

**PRESERVE.**

- Unpaid production remains correctable (under the new history-preserving model, not hard delete).
- Paid operations remain blocked (`isPaid` + `Payment` + unique `PaymentBatchItem.operationId`).
- No Payment void in this architecture package (REVIEW-023 stays known; not a physical-ledger MUST).
- DI-015 snapshots remain the payroll rate SoT.

`PSR-DEFER-003` — Payment void / compensating payment (AUDIT 2 / payroll contour, not physical-ledger cutover).

---

## 14. Period close

**DEFERRED FROM INITIAL INVENTORY CUTOVER.**

`PeriodClose` / `closeMonth` is **not** a prerequisite for making the physical ledger authoritative **if backdating remains disabled**.

It **is** required before:

- arbitrary backdated postings;
- authoritative closed historical costing;
- reopening/recalculating historical cost periods.

Preserve existing `CostPeriod` direction from `audit/03.02` (Packages 6–7). Do **not** create a competing quantity-period model without necessity.

If a later quantity period-close is needed, it must be designed against `CostPeriod`, not as a second calendar.

`PSR-DEFER-004` — Full PeriodClose for quantity-only ledger and for cost close.

---

## 15. Concurrency / transaction model and gateway

**ACCEPTED direction; keep current lock habits.**

- Isolation remains PostgreSQL READ COMMITTED unless a later package proves a need to change it.
- Keep `SELECT FOR UPDATE`, id-sorted locks, `gte` quantity guards, `clientRequestId` uniqueness.
- Movement insert + projection UPDATE happen in the **same** interactive transaction.
- Reconciliation may run out of band but must not “repair” by silent projection rewrite without an explicit `INVENTORY_ADJUSTMENT` / compensating movement.

Current dual inactive/active writer trees (`ARCH-SIMPLIFY-001`) remain temporary cost-flow migration debt. The physical gateway must not fork into a third tree.

### 15.1 Gateway — all writers (`PSR-MUST-007`, ARCH2-R4)

**Before authoritative cutover, ALL physical stock mutation paths — existing and future — must post through the approved movement/projection boundary or be disabled.**

Do **not** read this as “all new writers only”.

A direct projection writer remaining at cutover is a **blocker**.

Known paths that must be enumerated and covered or disabled:

- TORCOVKA submit
- PRISADKA submit
- UPAKOVKA submit
- production corrections
- production cancellation / reversal
- RailLot creation / receipt
- raw write-off
- SimplePurchase / nomenclature receipt
- Inventory conduct / adjustment
- marketplace ProductStock deduction **and** restoration
- any future return / scrap path

`PSR-MUST-007` — Before authoritative cutover, every existing and future physical stock mutation path posts through the approved movement/projection gateway **or is disabled**.

### 15.2 Marketplace / DI-011 — conditional cutover gate (ARCH2-R4)

**Do not reopen DI-011.** Current status remains **`DEFERRED BY OWNER`**.

**Conditional readiness gate (does not change that status):**

If marketplace stock mutation remains **ENABLED** and participates in authoritative `ProductStock` at primary-system cutover, **DI-011 must be fixed or the marketplace contour must receive an equivalent re-audit/remediation before cutover**.

Reason: a physical ledger can faithfully record a semantically wrong SHIPPED→PENDING transition. Ledger infrastructure does not fix incorrect business-state semantics.

If marketplace stock mutation is **disabled or excluded** from the authoritative contour at cutover, DI-011 may remain deferred.

### 15.3 Physical-world reconciliation (`PSR-MUST-008`, ARCH2-R3)

`PSR-MUST-004` only proves `ledger == projection`. That does **not** prove `system == physical factory`.

`PSR-MUST-008` — Before paper can stop being the parallel insurance system, every physical quantity domain that becomes authoritative must have a defined physical-count / reconciliation path.

At minimum explicitly cover:

- `RailLot` / raw rails
- `BlankStock`
- `DetailStock` including relevant WIP states
- `NomenclatureStock`
- `ProductStock`

Do **not** assume the current Inventory document already covers these correctly. Current inventory **explicitly excludes raw rails**, and its DETAIL abstraction is **not** automatically equivalent to every WIP projection key.

Required architecture (UI not required now):

```text
physical count
  → explicit signed adjustment movement
  → projection update
  → retained count document / causality
```

Never silently edit a projection to match reality. Inventory adjustment remains an explicit business fact.

---

## 16. Migration strategy

**No big bang. Do not implement in this task.**

Mandatory ordering:

```text
history-preserving posted facts
  BEFORE final authoritative-candidate epoch
  BEFORE authoritative cutover
```

**NO FINAL epoch before:**

- destructive posted-operation mutation is gone or blocked;
- every writer is covered or disabled (or will dual-write through the gateway in shadow until then);
- physical reconciliation capability exists.

**NO authoritative cutover before:**

- the **final** epoch is safe (no shadow double-count; epoch after destructive-path elimination);
- ledger↔projection reconciliation is green on the post-epoch stream;
- soak with zero unexplained drift.

Preferred sequence:

```text
SHADOW schema
  → dual-write / diagnostic validation
  → physical reconciliation capability
  → history-preserving correction/cancellation
  → disable every destructive posted-operation mutation
  → FINAL epoch/barrier + OPENING_BALANCE
  → reconciliation/soak
  → authoritative cutover
```

Any earlier test/rehearsal epoch is **NON-AUTHORITATIVE and disposable**.

Do **not** dual-write into an equation that later adds `OPENING_BALANCE` on top of those same shadow rows.

`PSR-MUST-003` — §6.3 **FINAL** epoch only. Shadow before that epoch is diagnostics only.

Shadow/dual-write is compatible with current inactive runtime. Cost-flow stays inactive.

---

## 17. Pre-primary-system MUST

These are the minimal prerequisites before Stell22 can drop paper as a parallel warehouse/production insurance system. **Not current P1 bugs.**

| ID | Requirement |
| --- | --- |
| `PSR-MUST-001` | Append-only physical movement register (`InventoryMovement` or equivalent). |
| `PSR-MUST-002` | Current `*Stock` tables (and lot remaining) become explicit projections with atomic movement+projection writes. |
| `PSR-MUST-003` | **FINAL** authoritative-candidate ledger epoch: write barrier + consistent snapshot + `OPENING_BALANCE` + immutable epoch identity. Created only after destructive posted-fact paths are gone/blocked. Pre-epoch shadow and disposable rehearsal epochs are excluded from reconstruction. Do not fabricate historical movements. |
| `PSR-MUST-004` | Automated ledger-vs-projection reconciliation (`OPENING_BALANCE` at the **final** epoch + post-epoch stream = live projection). Internal consistency only. |
| `PSR-MUST-005` | Before the **final** ledger epoch, posted physical production facts must already have history-preserving correction/cancellation semantics, or all destructive posted-fact mutation paths must be disabled. No authoritative post-epoch interval may contain destructively erased business facts. |
| `PSR-MUST-006` | Consequential manual corrections carry actor + reason + causal identity + atomic audit. |
| `PSR-MUST-007` | Before authoritative cutover, **ALL** existing and future physical stock mutation paths post through the approved gateway **or are disabled**. A leftover direct projection writer is a blocker. |
| `PSR-MUST-008` | Physical-count / reconciliation path for every authoritative quantity domain (RailLot, BlankStock, DetailStock including WIP, NomenclatureStock, ProductStock). Count → signed adjustment movement → projection → retained document. |

`PSR-DESIGN-001` — How to persist causal identity. **CLOSED / ACCEPTED** (`audit/08.04`). No universal `CommandExecution`. Production-correction slice = `ProductionOperationMutation` (`audit/08.03`). Remainder uses retained domain documents / movement-level causation.

`PSR-DESIGN-002` — Historical reference retention. **CLOSED / ACCEPTED** (`audit/08.04`): hybrid retain-source + immutable identifying snapshot.

**Conditional cutover gate (not a MUST that reopens DI-011):** if marketplace mutation stays enabled on authoritative `ProductStock`, DI-011 must be fixed or equivalently re-audited before cutover. DI-011 status remains `DEFERRED BY OWNER`.

---

## 18. Can wait until after go-live

Recorded as `PSR-DEFER-*` unless later evidence proves they block quantity SoT while backdating stays off:

| ID | Item |
| --- | --- |
| `PSR-DEFER-001` | Arbitrary user backdating |
| `PSR-DEFER-002` | Cost-flow / valuation integration of the physical ledger; ARCH-P1-001 ACTIVE-path fix |
| `PSR-DEFER-003` | Payment void / compensating payment |
| `PSR-DEFER-004` | Full PeriodClose (quantity calendar or 03.02 `closeMonth`) for the first quantity-ledger cutover |
| `PSR-DEFER-005` | Production orders |
| `PSR-DEFER-006` | Scheduling |
| `PSR-DEFER-007` | Reservations |
| `PSR-DEFER-008` | Multiple warehouses UI |
| `PSR-DEFER-009` | Serial tracking / per-part identity |
| `PSR-DEFER-010` | Multi-org |
| `PSR-DEFER-011` | Subcontracting |
| `PSR-DEFER-012` | Generic workflow engine |
| `PSR-DEFER-013` | Marketplace SHIPPED→PENDING restore: DI-011 remains **`DEFERRED BY OWNER`** (not reopened). **Conditional cutover gate:** if marketplace stock mutation is enabled on authoritative ProductStock, DI-011 must be fixed or equivalently re-audited before cutover; if marketplace mutation is disabled/excluded, DI-011 may stay deferred. |

---

## 19. Explicit rejected overengineering

| ID | Rejected |
| --- | --- |
| `PSR-REJECT-001` | Separate valuation journal / `InventoryValuationEntry` at this stage |
| `PSR-REJECT-002` | Microservices |
| `PSR-REJECT-003` | Kafka / generic message bus as the stock SoT |
| `PSR-REJECT-004` | Generic event-sourcing framework |
| `PSR-REJECT-005` | Ledger-only runtime reads |
| `PSR-REJECT-006` | Full CQRS rewrite |
| `PSR-REJECT-007` | Serial number per part as a cutover prerequisite |
| `PSR-REJECT-008` | Generic workflow engine |
| `PSR-REJECT-009` | Full deterministic replay of the entire ERP |
| `PSR-REJECT-010` | Copying Woodveri `CommandExecution` / door MES as mandatory Stell22 infrastructure |
| `PSR-REJECT-011` | Deleting `BlankStock` / `DetailStock` / `NomenclatureStock` / `ProductStock` |
| `PSR-REJECT-012` | Fabricating historical movements back to shop open |
| `PSR-REJECT-013` | Making PeriodClose a prerequisite for the first quantity-ledger cutover while backdating is disabled |
| `PSR-REJECT-014` | Treating PSR-MUST items as current AUDIT 1 `CONFIRMED BUG` / P1 |

Current modular monolith remains accepted.

---

## 20. Accepted implementation package order

Independent reviews #1–#3 are complete. **No PSR implementation in the ARCH-2 acceptance task (historical).** PSR-P0 is the first package after architecture merge. PSR-P0-CORR recorded the production-correction slice (`audit/08.03`). PSR-P0-CORE (`audit/08.04`) closes remaining PRE-SCHEMA questions. PSR-P0 = **COMPLETE / ACCEPTED** as an architecture/design contract only. PSR-P0 is design/contract closure, **not** InventoryMovement schema implementation.

No implementation package may begin if one of its schema/invariant prerequisites is unresolved.

| Package | Intent | Notes |
| --- | --- | --- |
| **ARCH-2R1** | Independent Review #1 | REQUEST CHANGES; R1…R6 **CLOSED / PASS** |
| **ARCH-2R2** | Independent Review #2 | REQUEST CHANGES; R7…R8 **CLOSED / PASS** |
| **ARCH-2R3** | Independent Review #3 | **PASS / ACCEPT**. New blockers = 0 |
| **PSR-P0** | Pre-schema contracts / design closure | **COMPLETE / ACCEPTED** as architecture/design contract only (`audit/08.03` + `audit/08.04`). Q1, Q2, Q5, Q7, Q8 **CLOSED / ACCEPTED**. Not runtime. Not InventoryMovement schema. No universal bus. |
| **PSR-P1** | `InventoryMovement` schema **SHADOW** | **COMPLETE / IMPLEMENTED / DEPLOYED / SHADOW SCHEMA EMPTY** (`audit/08.06`). Schema contract `audit/08.05` remains **ACCEPTED DESIGN** (historical **NOT IMPLEMENTED** at contract creation preserved). Empty unused table; production row count = **0**; no runtime writers. Does **not** mean dual-write, authoritative ledger, paper removal, `production_cost_flow` activation, or Correction Center. Pre-final-epoch rows remain diagnostics only |
| **PSR-P2** | Dual-write **ALL** physical mutation contours in SHADOW | Close `PSR-Q-004` before this if still relevant |
| **PSR-P3** | Ledger↔projection **diagnostic** reconciliation in shadow | Internal consistency of dual-write; not yet authoritative |
| **PSR-P4** | Physical-count reconciliation capability | Close `PSR-Q-003` before this. `PSR-MUST-008` |
| **PSR-P5** | History-preserving correction/cancellation + remove/block destructive posted mutations | **Before FINAL epoch.** TORCOVKA containment stays until explicit cancel replaces it |
| **PSR-P6** | **FINAL** epoch/barrier + `OPENING_BALANCE` | Close `PSR-Q-006` before this. Not a rehearsal epoch |
| **PSR-P7** | Post-epoch reconciliation + stability soak | Zero unexplained drift |
| **PSR-P8** | Authoritative physical-ledger cutover | Only after final epoch, writers, physical recon, destructive-path disable, green recon |
| **PSR-P9** | Later cost-flow integration / `ARCH-P1-001` | Gate stays inactive until then |
| **PSR-P10** | Later PeriodClose / backdating | After replay/period rules |

Invariant: **history-preserving posted facts → FINAL epoch → authoritative cutover.**

Marketplace routing belongs in PSR-P2 and, if still enabled, the DI-011 conditional gate before PSR-P8.

---

## 21. Open design questions

These are **not** AUDIT 1 owner questions (those were closed). They are accepted **open** ARCH-2 design questions with a **mandatory timing gate**. They are **not** resolved in this acceptance task.

No implementation package may begin if one of its schema/invariant prerequisites is unresolved.

### 21.1 Timing matrix (`ARCH2-R8`)

| Gate | Questions | Rule |
| --- | --- | --- |
| **PRE-SCHEMA (PSR-P0, before PSR-P1)** | Q1, Q2, Q5, Q7, Q8 | Schema-shaping. PSR-P1 blocked until closed and accepted. |
| **BEFORE DUAL-WRITE (PSR-P2)** | Q4 | Shadow-mode activation/control, if still relevant. |
| **BEFORE PHYSICAL RECON (PSR-P4)** | Q3 | Inventory adjustment shape / rails/WIP physical-count representation. |
| **BEFORE FINAL EPOCH (PSR-P6)** | Q6 | Exact barrier/maintenance mechanism. |

If a later review shows a question must close earlier, it moves earlier. It never moves later than the package that depends on it.

### 21.2 Question list

| ID | Question | Constraint | Timing |
| --- | --- | --- | --- |
| `PSR-Q-001` | Generic `CommandExecution` table vs narrower mutation/audit context? | **CLOSED / ACCEPTED** (`audit/08.04`). No universal command bus for Stell22. Production-correction slice remains `ProductionOperationMutation` (`audit/08.03`). | PRE-SCHEMA |
| `PSR-Q-002` | Movement identity / uniqueness / idempotency contract (per op line vs aggregate pool per op; source-effect identity)? | **CLOSED / ACCEPTED** (`audit/08.04`). `UNIQUE(causationKind, causationId, effectKey)` conceptually. `effectKey` MUST be retry-stable from business semantics (not newly generated line ids / reverse-reapply recreation). `clientRequestId` remains terminal-submit only. | PRE-SCHEMA |
| `PSR-Q-003` | Inventory adjustment movement shape; rails/WIP physical-count representation? | DI-009 boundary stays. Current Inventory is not automatically sufficient. | BEFORE PHYSICAL RECON |
| `PSR-Q-004` | Shadow-mode flag: Setting vs deploy-time constant vs always-on dual-write after schema? | Must not activate `production_cost_flow`. | BEFORE DUAL-WRITE |
| `PSR-Q-005` | Actor representation: `Employee.id` vs admin `User.id` + reason; single nullable pair? | **CLOSED / ACCEPTED** (`audit/08.04`): `USER` / `EMPLOYEE` / server-controlled `SYSTEM`. Marketplace is SOURCE not actor. Do not invent a second user system. | PRE-SCHEMA |
| `PSR-Q-006` | Exact epoch write-barrier / maintenance-gate mechanism? | Semantics in §6.3 are mandatory; storage/lock implementation is not frozen. | BEFORE FINAL EPOCH |
| `PSR-Q-007` | How are pre-epoch / shadow movements stored, labelled, and excluded from authoritative reconstruction? | **CLOSED / ACCEPTED** (`audit/08.04`): explicit immutable `SHADOW` / `AUTHORITATIVE` assigned by server/gateway; never in-place promotion. Must not infer authority from timestamps/SHA/Setting/migration. | PRE-SCHEMA |
| `PSR-Q-008` | Historical reference retention: archive+restrict (A) vs immutable snapshot on the movement (B), per entity class? | **CLOSED / ACCEPTED** (`audit/08.04`): hybrid by entity role. | PRE-SCHEMA |

INC-001 physical facts (remaining rails in `ПАК-40-1280-01-7`, whether 3843 × 0.36 m SORT1 blanks existed) remain an **incident** owner decision, not an ARCH-2 design question.

---

## 22. Independent-review checklist

Reviewers must challenge this draft **before** any implementation package is accepted.

Confirm all of the following remain true after review comments:

1. `audit/08.01` is **not** overwritten; AUDIT 1 stays `COMPLETE / REVIEWED`.
2. AUDIT 1 P0/P1/P2/P3 counts are unchanged; PSR items are not relabeled as those findings.
3. `ARCH-P1-001` is **OPEN / CONFIRMED**, scope = `production_cost_flow` **ACTIVE** UPAKOVKA quantity-edit path. Inactive `prepareUpakovkaEdit` union is **not** described as missing.
4. `production_cost_flow` is **INACTIVE**; this draft does not activate it.
5. No separate valuation journal is accepted.
6. Generic `CommandExecution` is **not** marked mandatory.
7. `*Stock` tables are not proposed for deletion.
8. Ledger-only reads, event sourcing, microservices, Kafka, CQRS rewrite, serials, workflow engine are rejected.
9. PeriodClose is **not** a prerequisite for first quantity-ledger cutover while backdating is disabled.
10. Opening epoch does **not** fabricate historical movements.
11. 03.02 cost semantics (WAC components, `costVersion`, `CostEvent`, `CostPeriod`) are not replaced.
12. Woodveri architecture is not imported.
13. INC-001 containment is kept; INC-001 is not silently closed.
14. No `src/**`, `prisma/**`, migrations, scripts runtime, or production mutation is implied as already done.
15. NEXT after architecture acceptance was **PSR-P0** (pre-schema contracts). In the ARCH-2 acceptance task PSR-P0 was **not** started. **Current (2026-09-17 closeout):** PSR-P0 = **COMPLETE / ACCEPTED** as architecture/design contract only (`audit/08.03` + `audit/08.04`). PSR-P1 = **COMPLETE / IMPLEMENTED / DEPLOYED / SHADOW SCHEMA EMPTY** (`audit/08.06`). PSR-P2 **NOT STARTED**. AUDIT 2 not started.
16. **FINAL** epoch is **after** history-preserving correction and after destructive posted-op paths are removed/blocked. Authoritative cutover is after that epoch.
17. Pre-epoch shadow movements are excluded from authoritative reconstruction.
18. After reviewed merge this file on `main` is the canonical ARCH-2 architecture contract. `ACCEPTED / REVIEWED` ≠ implementation complete.
19. Schema-shaping questions Q1/Q2/Q5/Q7/Q8 close in PSR-P0 **before** InventoryMovement schema.
20. Movement idempotency is a mandatory invariant (retry of the same effect must not double-post).

---

## 23. ARCH-P1-001 — keep open, refine scope

**Do not say it is fixed. Do not say current inactive runtime has the same missing union check.**

### Exact wording (canonical for PROJECT.md / INDEX)

`ARCH-P1-001` — **OPEN / CONFIRMED**. Scope = `production_cost_flow` **ACTIVE** UPAKOVKA quantity-edit path. Activation blocker.

Current inactive runtime already protects `old refs ∪ current BOM refs` via `prepareUpakovkaEdit` (`src/server/internal/inventory-integrity.ts`) before reverse/re-apply.

The unresolved finding is specifically the **ACTIVE** branch in `src/server/production.ts` `updateProductionLineQuantity`:

```text
snapshotUpakovkaApply (current BOM)
  → reverseActiveUpakovkaOperation (boundary on old refs only)
  → delete lines
  → applyUpakovkaPrepared / applyActiveUpakovkaPrepared (current BOM)
```

without a single pre-mutation inventory boundary over **old ∪ current** refs.

`production_cost_flow` stays **INACTIVE**. This is not reachable on current production. It remains a **cost-flow activation blocker**, not a current inactive-path P1.

Remediation stays in later package **PSR-P9** / cost-flow activation work. It is **not** a physical-ledger P0 for inactive quantity cutover.

---

## 24. Relation to 08.01 and 03.02 (contradictions handled)

| Topic | 08.01 / 03.02 | This draft |
| --- | --- | --- |
| `*Stock` as PRIMARY FACT | 08.01 source-of-truth map | **Target reclassification to projection** after cutover. Current runtime unchanged. Not a new CONFIRMED BUG. |
| Posted op hard delete | Current runtime deletes unpaid PRISADKA/UPAKOVKA/HOURS | **Target superseded**; must be gone/blocked **before the FINAL epoch** (R7). Not merely at cutover. |
| Movement ledger | 03.02 rejected a **cost** ledger | Physical quantity ledger **accepted**; monetary journal **rejected** |
| Period close | 03.02 Package 7; 08.01 known missing | Deferred from first quantity cutover; still required before backdating / historical costing |
| Next audit | 08.01 next = AUDIT 2 | AUDIT 2 still **not started**. Current NEXT = **PSR-P2 POST-R06 DUAL-WRITE READINESS / INVENTORY WRITER CONTRACT DECISION** (see §24.11 / `audit/08.10`). R-06 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED / POST-DEPLOY VERIFIED**. R-05 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED**. R-04 = **DEPLOYED**. PSR-P1 = **COMPLETE / IMPLEMENTED / DEPLOYED / SHADOW SCHEMA EMPTY**. PSR-P0 = **COMPLETE / ACCEPTED** as architecture/design contract only |
| Dual writer trees | `ARCH-SIMPLIFY-001` temporary debt | Keep; do not add a third permanent tree |
| Canonical 08.02 | — | Canonical ARCH-2 architecture on `main` after Review #3 merge |

No 08.01 defect count is changed.

---

## 24.1 Addendum — PSR-P0-CORR (2026-09-17)

ARCH-2 acceptance BASE (`71a01b4`) is **historical** and is **not rewritten**. Production application (verified this addendum): `0686d0036da52cc32aaff531c91f1dd2ded899ec`. Application code base for the correction contract: same SHA. A docs merge of `08.03` will advance GitHub `main`; production remains `0686d00` until a separate deploy.

Production-correction slice **DECIDED / ACCEPTED** in `audit/08.03-psr-p0-correction-history-contract.md` (**ACCEPTED DESIGN / NOT IMPLEMENTED**):

- no universal `CommandExecution` for this contour;
- working name `ProductionOperationMutation`;
- snapshots, single-transaction apply, advisory preview, v1 scope;
- once history-preserving correction is enabled for a type, destructive delete of that posted type **MUST** be blocked;
- Correction Center v1 **MUST** block destructive delete of posted HOURS (containment, not cancellation);
- Correction Center v1 TORCOVKA line-qty apply **MUST** reject a frozen batch (hardening; current line-qty path does not check freeze);
- cancellation **DEFERRED / NOT IMPLEMENTED**.

`PSR-Q-001` and `PSR-DESIGN-001` were left **OPEN / PARTIALLY RESOLVED** globally in this 08.03 cycle (warehouse / inventory / payroll / other writers unresolved). **Superseded for current status by §24.2.**

This addendum does **not** implement schema/code, does **not** mark PSR-P0 complete, does **not** start InventoryMovement (PSR-P1 was **BLOCKED** until Q1, Q2, Q5, Q7, Q8 were fully closed), and does **not** close INC-001.

---

## 24.2 Addendum — PSR-P0-CORE (2026-09-17)

Production application remains `0686d0036da52cc32aaff531c91f1dd2ded899ec`. GitHub `main` at this addendum's contract creation: `bd82aa0e0f4785cee316d282ef55eb593aadca3b` (`08.03` docs merge). A later docs merge of `08.04` will advance GitHub `main` again; production remains `0686d00` until a separate deploy. Do not describe the docs-merge SHA as deployed application code.

Remaining PRE-SCHEMA questions **CLOSED / ACCEPTED** in `audit/08.04-psr-p0-core-contract.md` (**ACCEPTED DESIGN / NOT IMPLEMENTED**):

- `PSR-Q-001` / `PSR-DESIGN-001` — no universal `CommandExecution`; narrow domain causal contexts;
- `PSR-Q-002` — causation + retry-stable `effectKey` uniqueness;
- `PSR-Q-005` — `USER` / `EMPLOYEE` / server-controlled `SYSTEM`;
- `PSR-Q-007` — explicit immutable `SHADOW` / `AUTHORITATIVE` (never in-place promotion);
- `PSR-Q-008` / `PSR-DESIGN-002` — hybrid historical reference retention;
- opening-balance grain from physical projections (not Inventory UI).

PSR-P0 = **COMPLETE / ACCEPTED** as an architecture/design contract only. Not runtime.

PSR-P1 = **NEXT after reviewed merge of `08.04`** = InventoryMovement schema in **SHADOW**. Not dual-write, not authoritative, not deploy, not paper removal, not `production_cost_flow` activation, not Correction Center.

`PSR-Q-003` / `Q4` / `Q6` remain at existing later timing. INC-001 remains OPEN. Cancellation remains DEFERRED.

This addendum does **not** implement schema/code. **Superseded for current PSR-P1 status by §24.3.**

---

## 24.3 Addendum — PSR-P1 InventoryMovement schema contract (2026-09-17)

Production application remains `0686d0036da52cc32aaff531c91f1dd2ded899ec`. GitHub `main` at this addendum's contract creation: `b11c37df94be8aee0fb4d0e9a466182924e29fc7` (`08.04` docs merge). A later docs merge of `08.05` will advance GitHub `main` again; production remains `0686d00` until a separate deploy. Do not describe the docs-merge SHA as deployed application code.

Exact `InventoryMovement` schema **frozen** in `audit/08.05-psr-p1-inventory-movement-schema-contract.md` (**ACCEPTED DESIGN / NOT IMPLEMENTED**):

- enums `InventoryMovementKind` / `InventoryStockDomain` / `InventoryMovementAuthority` / `InventoryMovementActorKind` / `InventoryMovementCausationKind`;
- no FK to masters/sources; no self-FK on `reversalOfMovementId`;
- `UNIQUE(causationKind, causationId, effectKey)` — do **not** add nullable `epochId` to this key;
- lookup raw partial indexes plus five **AUTHORITATIVE-only** opening UNIQUE indexes;
- CHECK contract (not executed in this cycle), including: AUTHORITATIVE requires non-null nonblank `epochId`; SHADOW **may** carry a non-null nonblank rehearsal `epochId`; SHADOW is never promoted in place;
- reversal: compensating movement, partial allowed, multiple compensations of one original allowed, chains not prohibited; no `UNIQUE(reversalOfMovementId)` / no composite self-FK;
- `targetSnapshot` v=1 JSON object; keys present, values may be null where the source is null; Decimal JSON as canonical strings; **no monetary valuation fields**;
- application append-only; no DB trigger in PSR-P1.

Independent Review #1 of `08.05`: **PASS WITH NON-BLOCKING FINDINGS** (P0=0, P1=0). ChatGPT adopted schema-shaping corrections in `08.05`. Writer/gateway items remain PSR-P2 / later.

Prisma/migration **NOT STARTED**. Default authority = SHADOW. Implementation is **not** authorized by the Independent Review cycle.

Does **not** mean dual-write, opening-balance rows, AUTHORITATIVE rows, projection change, delete-behavior change, gateway, Correction Center, `production_cost_flow` activation, or deploy.

`PSR-Q-003` / `Q4` / `Q6` remain at existing later timing. INC-001 remains OPEN. Cancellation remains DEFERRED.

This addendum does **not** implement schema/code. **Superseded for current PSR-P1 implementation/deploy status by §24.4.**

---

## 24.4 Addendum — PSR-P1 SHADOW schema implemented and deployed empty (2026-09-17)

Production application is now `92532e818df41160b434caac2c6d94933e93d810`. Previous production application `0686d0036da52cc32aaff531c91f1dd2ded899ec` is **historical**. Do not rewrite §24.3 contract-creation production SHA.

Canonical closeout: `audit/08.06-psr-p1-deployment-closeout.md`.

PSR-P1 = **COMPLETE / IMPLEMENTED / DEPLOYED / SHADOW SCHEMA EMPTY**:

- implementation PR #7; reviewed head `087ed7c5b09937cacf10fe3c110465a08c14de78`;
- merge commit `92532e818df41160b434caac2c6d94933e93d810`;
- post-merge CI `35227949324` SUCCESS;
- Production Deploy `35228794988` SUCCESS;
- migration `20260917150000_psr_p1_inventory_movement_shadow` applied (`finished_at` set; not rolled back; 1 row);
- `InventoryMovement` EXISTS; production row count = **0**;
- 5 enums / 12 CHECKs / FK 0 / indexes PASS;
- health HTTP 200;
- no runtime writers;
- PSR-P2 **NOT STARTED**.

This does **not** mean dual-write, SHADOW posting, AUTHORITATIVE rows, `OPENING_BALANCE` data, reconciliation, paper removal, `production_cost_flow` activation, or Correction Center.

**NEXT (historical PSR-P1 closeout sentence; later closed — §24.5):** PSR-P2 PRE-DUAL-WRITE CONTRACT / PRECONDITIONS. Close `PSR-Q-004`, R-10, R-11 before PSR-P2. Close R-07 before movement writers. Close R-04 / R-05 / R-06 before their contour dual-write. `PSR-Q-003` remains before P4. `PSR-Q-006` remains before P6.

`production_cost_flow` remains the existing canonical delivery state / **INACTIVE** (no fresh live query in this closeout). INC-001 remains OPEN. Cancellation remains DEFERRED.

---

## 24.5 Addendum — PSR-P2 general preconditions Q4 / R-07 / R-10 / R-11 (2026-09-17)

This addendum does **not** rewrite §24.4 or earlier ARCH-2 text.

Canonical record: `audit/08.07-psr-p2-pre-dual-write-general-preconditions.md`.

| ID | Status | Contract |
| --- | --- | --- |
| `PSR-Q-004` | **CLOSED** | Dedicated Setting `inventory_movement_shadow_write` `{version:1, active:boolean}`. Missing → inactive. Malformed → fail closed. No UI. No cache. Independent from `production_cost_flow`. Writer = SHARED TX lock. Supported ON/OFF = same EXCLUSIVE TX lock as reset, via transaction-only setter + maintenance CLI. Not wired into physical writers in this cycle. |
| R-07 | **CLOSED** | `recordedAt` = `TIMESTAMPTZ(3)` DB default `CURRENT_TIMESTAMP`. New migration; P1 migration not rewritten. Empty-table guard before `ALTER TYPE`. |
| R-10 | **CLOSED** | Controlled SHADOW purge/reset. Causal UNIQUE remains `(causationKind, causationId, effectKey)`. `epochId` not in that UNIQUE. Maintenance script only. |
| R-11 | **CLOSED** | CI/static guard against application `update`/`delete`/`upsert` and raw UPDATE/DELETE. Allowed: migrations, tests, R-10 script. |

PSR-P2 dual-write **NOT STARTED**. R-04 / R-05 / R-06 remain OPEN before their contours. `PSR-Q-003` remains before P4. `PSR-Q-006` remains before P6. Production application SHA **unchanged** (`92532e8`). Production not queried. Deploy **NO**. `production_cost_flow` canonical delivery state **INACTIVE**; live runtime **NOT QUERIED**. INC-001 remains OPEN. AUDIT 2 remains NOT STARTED.

---

## 24.6 Addendum — PSR-P2 R-04 Supply cycle identity (2026-09-17)

This addendum is **historical** (PR #11 implementation/review, 2026-09-17). It does **not** rewrite §24.5. Current runtime status is in §24.7.

Canonical record: `audit/08.08-psr-p2-r04-supply-cycle-identity.md`.

R-04 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED**. Long-lived `Supply` unique `(marketplace, externalId, sku)` plus `stockAccountingGeneration` / `stockAccountingOpen`. One-shot shortfall unchanged. Ozon cancel closes an open cycle even when `deductedQty = 0`. WB remains consume-only; DI-011 still deferred. No InventoryMovement writer. Dual-write **NOT STARTED**.

Delivery vehicle: PR #11. Verify GitHub/main for current merge state. **NEXT = R-04 DELIVERY — PR #11 MERGE / PRODUCTION PREFLIGHT / DEPLOYMENT DECISION**. This is **not** SUPPLY SHADOW dual-write. SHADOW gate remains inactive. InventoryMovement writer remains 0.

R-05 / R-06 remain OPEN before their contours.

---

## 24.7 Addendum — PSR-P2 R-04 production deploy (2026-09-18)

This addendum does **not** rewrite §24.6. §24.6 remains the historical PR-#11 implementation/review status (**NOT DEPLOYED** at that time).

Canonical record: `audit/08.08-psr-p2-r04-supply-cycle-identity.md` §10.

R-04 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED**. Production application `c14a58641d57dfabfce223b2c213db9c86167ca4`. Production Deploy `35314440036` SUCCESS. Migration `20260917200000_psr_p2_r04_supply_stock_accounting_cycle` **APPLIED**. `stockAccountingGeneration` / `stockAccountingOpen` LIVE. CHECKs LIVE. Unique `(marketplace, externalId, sku)` unchanged. InventoryMovement 0/0/0. Runtime writers 0. `inventory_movement_shadow_write` ABSENT. `production_cost_flow` INACTIVE. Dual-write **NOT STARTED**.

**NEXT = PSR-P2 R-05 CORRECTION IDENTITY — RECONNAISSANCE / CONTRACT DECISION**. Not SUPPLY SHADOW dual-write. Not an InventoryMovement writer. Not R-06. Not Correction Center. Not AUDIT 2. R-05 / R-06 remain OPEN.

---

## 24.8 Addendum — PSR-P2 R-05 correction identity (2026-09-18)

This addendum is **historical** (PR #13 implementation/review, 2026-09-18). It does **not** rewrite §24.7. Current runtime status is in §24.9.

Canonical record: `audit/08.09-psr-p2-r05-correction-identity.md`.

R-05 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED**. Narrow retained entity `ProductionOperationCorrection` wraps `correctTorcovkaRailsTaken` (`requestId` UNIQUE, expected-old CAS, idempotent replay). Scalar IDs; no FK/CASCADE. Empty table; no ChangeLog backfill. No InventoryMovement writer. Full `ProductionOperationMutation` / Correction Center **not** implemented. Dual-write **NOT STARTED**. Production application SHA **unchanged** (`c14a586`). Deploy **NO**.

**NEXT = PSR-P2 R-05 PRODUCTION PREFLIGHT / DEPLOYMENT DECISION**. Not SUPPLY SHADOW dual-write. Not an InventoryMovement writer. Not R-06. Not Correction Center. Not AUDIT 2. R-06 remains OPEN.

---

## 24.9 Addendum — PSR-P2 R-05 production deploy (2026-09-18)

This addendum does **not** rewrite §24.8. §24.8 remains the historical PR-#13 implementation/review status (**NOT DEPLOYED** at that time).

Canonical record: `audit/08.09-psr-p2-r05-correction-identity.md` §9.

R-05 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED**. Production application `27538a5d5777d33fb87d060963a6d07a189cf044`. Production Deploy `35329071777` SUCCESS. Migration `20260918120000_psr_p2_r05_production_operation_correction` **APPLIED**. `ProductionOperationCorrection` **LIVE**. Initial row count **0**. Historical backfill **NO**. InventoryMovement 0/0/0. Runtime writers 0. `inventory_movement_shadow_write` ABSENT. `production_cost_flow` INACTIVE. Dual-write **NOT STARTED**. R-04 remains **DEPLOYED**.

**NEXT = PSR-P2 R-06 INVENTORY IDENTITY — RECONNAISSANCE / CONTRACT DECISION**. Not R-06 implementation. Not SHADOW activation. Not dual-write. Not an InventoryMovement writer. Not Correction Center. Not AUDIT 2. R-06 remains OPEN. R-06 concerns the already identified `conductInventory` / BlankStock shared-pool identity `(materialId, lengthM, detailType, sort)` and per-detail-line accounting risks (`audit/08.05` §13).

---

## 24.10 Addendum — PSR-P2 R-06 inventory identity implementation (2026-09-18)

This addendum does **not** rewrite §24.9. §24.9 remains the historical R-05 deploy closeout (NEXT then = R-06 reconnaissance).

Canonical record: `audit/08.10-psr-p2-r06-inventory-identity.md`.

R-06 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED**. First-class BLANK InventoryLine. No-prisadka DETAIL excluded from new drafts. DETAIL ready aggregate + deterministic bucket effects. UNIQUE `(inventoryId, refType, refId)`. Legacy DRAFT fail-closed `LEGACY_INVENTORY_DRAFT_RECREATE`. InventoryMovement writers **0**. SHADOW inactive. Dual-write **NOT STARTED**. `production_cost_flow` INACTIVE. Production application SHA **unchanged** (`27538a5`). Deploy **NO**. Not full MUST-008.

**NEXT = R-06 HEAD REVIEW**. Not InventoryMovement dual-write. Not SHADOW activation. Not Correction Center. Not AUDIT 2.

Later current-state (2026-09-18): R-06 was subsequently **DEPLOYED**. See §24.11 / `audit/08.10` §12. Dual-write remains **NOT STARTED**.

---

## 24.11 Addendum — PSR-P2 R-06 production deploy (2026-09-18)

This addendum does **not** rewrite §24.10. §24.10 remains the historical R-06 implementation/review status (**NOT DEPLOYED** at that time; NEXT then = R-06 HEAD REVIEW).

Canonical record: `audit/08.10-psr-p2-r06-inventory-identity.md` §12.

R-06 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED / POST-DEPLOY VERIFIED**. Production application `eb02b17f488ef9ec13a11dcf158fc169b78e3e37`. Production Deploy `35337215903` SUCCESS. Migration `20260918130000_psr_p2_r06_inventory_line_identity` **APPLIED**. Index `InventoryLine_inventoryId_refType_refId_key` **LIVE**. Cutover Inventory 1 CONDUCTED / 0 DRAFT / 0 lines. Historical backfill **NO**. InventoryMovement 0/0/0. Runtime writers 0. `inventory_movement_shadow_write` ABSENT. `production_cost_flow` INACTIVE. Dual-write **NOT STARTED**. R-05 remains **DEPLOYED**.

**ROLLBACK WINDOW = PHASE A.** Rollback to `27538a5` is currently **SAFE**. Phase B = SAFE BUT DRAFT UNUSABLE FOR CONDUCT. Phase C: after the first CONDUCTED R-06 BLANK inventory, rollback to `27538a5` is **SEMANTICALLY UNSAFE**. AFTER FIRST CONDUCTED R-06 BLANK INVENTORY, DO NOT BLINDLY ROLLBACK TO `27538a5`.

**NEXT = PSR-P2 POST-R06 DUAL-WRITE READINESS / INVENTORY WRITER CONTRACT DECISION**. Not InventoryMovement writer implementation. Not SHADOW activation. Not dual-write activation. Not Correction Center. Not AUDIT 2.

---

## 25. Completion status

| | |
| --- | --- |
| Artifact | `audit/08.02-primary-system-readiness-architecture.md` |
| Location | Canonical ARCH-2 architecture on `main` after reviewed merge |
| Status | **ACCEPTED / REVIEWED** (architecture contract; **not** implementation complete) |
| Independent Review #1 | **REQUEST CHANGES** → R1…R6 **CLOSED / PASS** |
| Independent Review #2 | **REQUEST CHANGES** → R7…R8 **CLOSED / PASS** |
| Independent Review #3 | **PASS / ACCEPT** (new blockers = 0) |
| R1…R8 | **CLOSED / PASS** |
| Application code | unchanged in the ARCH-2 acceptance cycle. Current production application: see §24.11 (`eb02b17`) |
| Schema / migration | unchanged in the ARCH-2 acceptance cycle. PSR-P1 SHADOW schema later implemented/deployed empty: see §24.4 / `audit/08.06`. R-04 Supply fields later **DEPLOYED**: see §24.7 / `audit/08.08`. R-05 correction identity later **DEPLOYED**: see §24.9 / `audit/08.09`. R-06 inventory identity later **DEPLOYED**: see §24.11 / `audit/08.10` |
| `production_cost_flow` | unchanged / INACTIVE |
| `ARCH-P1-001` | OPEN / CONFIRMED (ACTIVE path; not fixed) |
| AUDIT 1 | COMPLETE / REVIEWED |
| AUDIT 2 | NOT STARTED |
| INC-001 | OPEN |
| Production deploy | **NO** in the ARCH-2 acceptance cycle. Later PSR-P1 production deploy: `35228794988` SUCCESS — see §24.4. Later R-04 production deploy: `35314440036` SUCCESS — see §24.7. Later R-05 production deploy: `35329071777` SUCCESS — see §24.9. Later R-06 production deploy: `35337215903` SUCCESS — see §24.11 |
| Next | **PSR-P2 POST-R06 DUAL-WRITE READINESS / INVENTORY WRITER CONTRACT DECISION.** R-06 = **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED / POST-DEPLOY VERIFIED** (`audit/08.10`). R-05 = **DEPLOYED** (`audit/08.09`). R-04 = **DEPLOYED** (`audit/08.08`). Not InventoryMovement dual-write. Not Correction Center. |

---
## PSR-P2 writer-contract addendum — 2026-09-18

The accepted PSR-P2 writer contract is `audit/08.11-psr-p2-shadow-writer-contract.md` (**ACCEPTED / REVIEWED**).

It preserves the accepted ARCH-2 roadmap while freezing the implementation boundary required before SHADOW dual-write: the dedicated `inventory_movement_shadow_write` gate is acquired/read first under the shared advisory TX lock; projection mutation and SHADOW movement insertion are atomic; `InventoryPhysicalEffect[]` is the sole Inventory movement derivation source; dual-active Inventory fails closed until the ACTIVE projection is unified; and P2 is not complete until every enabled physical writer either dual-writes or is disabled.

This addendum does not start P2 dual-write, P3, P9, P10, or AUDIT 2. It does not activate either gate. NEXT after contract acceptance was **P2-GUARD/CI IMPLEMENTATION PACKAGE** (now completed).

---
## PSR-P2 P2-GUARD/CI addendum — 2026-09-18

`audit/08.12-psr-p2-guard-ci.md` records P2-GUARD/CI as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED** (PR #18; accepted head `91c7e3e`; merge `643c796`; post-merge CI `35373616566` SUCCESS). Runtime InventoryMovement physical writers remain **0**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. Current NEXT at that addendum was **PSR-P2 INVENTORY WRITER PREREQUISITE PACKAGE**. This addendum does not implement that package, start the first physical writer, or deploy.

---
## PSR-P2 Inventory writer prerequisite addendum — 2026-09-19

`audit/08.13-psr-p2-inventory-writer-prerequisite.md` records the Inventory writer prerequisite as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / INVENTORY WRITER NOT STARTED** (PR #20; accepted head `1f8b1d6`; merge `4dfaae2`; accepted PR CI `35379385996` SUCCESS; post-merge CI `35422069460` SUCCESS). HEAD review **PASS**. Independent Opus **PASS**. P0=0 / P1=0 / P2=5 (non-blocking; dispositioned in `08.13` §10). Runtime InventoryMovement physical writers remain **0**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA.

Current NEXT at that addendum was **PSR-P2 INVENTORY SHADOW WRITER IMPLEMENTATION PACKAGE**. This addendum does not start that package, activate SHADOW, or deploy.

---
## PSR-P2 Inventory SHADOW writer addendum — 2026-09-19

`audit/08.14-psr-p2-inventory-shadow-writer.md` records the Inventory SHADOW writer as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (PR #22; accepted head `c58131c`; merge `2fd138e`; accepted PR CI `35427250317` SUCCESS; post-merge CI `35427681739` SUCCESS). HEAD review **PASS**. Independent Opus **PASS — P0=0 / P1=0 / P2=4**. W1 CLOSED. W2 accepted non-blocking. W3 accepted pre-existing/non-blocking. W4 CLOSED. Source/main connected physical InventoryMovement contour = **Inventory conduct only**. Production writer currently live = **NO**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **PSR-P2 NORMAL TERMINAL PRODUCTION SHADOW WRITERS PACKAGE**. This addendum does not start that package, activate SHADOW, or deploy.

---
## PSR-P2 terminal production SHADOW writers addendum — 2026-09-19

`audit/08.15-psr-p2-terminal-production-shadow-writers.md` records the terminal production SHADOW writers as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (PR #24; accepted head `1367e30`; merge `853db69`; exact-head CI `35439899641` SUCCESS; post-merge CI `35440444019` SUCCESS). HEAD review **PASS**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Initial findings **P0=0 / P1=2 / P2=1**. P1-01 CLOSED (one captured `costFlowActive` per normal UPAKOVKA TX for lock + apply). P1-02 CLOSED. P2-01 CLOSED. Broader `production_cost_flow` activation/cutover is still **not** coordinated with in-flight physical operations. Source/main connected physical InventoryMovement contours = **Inventory conduct + normal TORCOVKA / PRISADKA / UPAKOVKA**. Production writer currently live = **NO**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **PSR-P2 R-05 TORCOVKA CORRECTION SHADOW WRITER PACKAGE**. This addendum does not start that package, start Supply, activate SHADOW, or deploy.

---
## PSR-P2 R-05 TORCOVKA correction SHADOW writer addendum — 2026-09-19

`audit/08.16-psr-p2-r05-correction-shadow-writer.md` records the R-05 TORCOVKA correction SHADOW writer as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (PR #26; accepted head `c9d3c0b`; merge `debafda`; exact-head CI `35443727340` SUCCESS; post-merge CI `35444256886` SUCCESS). HEAD review **PASS**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Findings **P0=0 / P1=0 / P2=0**. R-05 identity prerequisite remains separately **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED / DEPLOYED** (`audit/08.09`). Source/main connected physical InventoryMovement contours = **Inventory conduct + normal TORCOVKA / PRISADKA / UPAKOVKA + R-05 `correctTorcovkaRailsTaken`**. Production writer currently live = **NO**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **PSR-P2 SUPPLY PREREQUISITE FIX PACKAGE** (`audit/08.11` §16: read-only production edge query, null-product fail-closed, global Supply/ProductStock lock order). This addendum does not start that package, start the later Supply writer, activate SHADOW, or deploy.

---
## PSR-P2 Supply writer prerequisite addendum — 2026-09-19

`audit/08.17-psr-p2-supply-prerequisite.md` records the Supply writer prerequisite as **COMPLETE / NOT DEPLOYED / PRODUCTION PREFLIGHT COMPLETE / TRUSTED**. Runtime hardening PR #28 accepted head `8e92dd6`; merge `5fe5f40`; exact-head CI `35449013808` SUCCESS; post-merge CI `35449670249` SUCCESS. Initial HEAD review of `96558c6` = REQUEST CHANGES (P0=0 / P1=2 / P2=0). P1-01 CLOSED. P1-02 CLOSED. Final implementation HEAD review **PASS**. Parser-fix PR #29 accepted head `7795e88`; merge `eb79b26`; exact-head CI `35451478137` SUCCESS; post-merge CI `35452147190` SUCCESS. `SUPPLY-PREFLIGHT-PARSER-001` **CLOSED**. Canonical-doc P2-01 **CLOSED**. HEAD final re-review **PASS** (P0=0 / P1=0 / P2=0). Independent Opus **SKIPPED BY OWNER COST POLICY**. Historical failed read-only preflight `35449877592` (no trusted counts). Trusted rerun `35452435411` SUCCESS on `eb79b26`: TOTAL=0 / DEDUCTED_POSITIVE=0 / SHORTFALL_ONLY=0 / OPEN_DEDUCTED_POSITIVE=0 / `SUPPLY_DATA_BLOCKER=NO`. `SUPPLY-PREFLIGHT-NULL-BINDING` = **NO**. Source/main connected physical InventoryMovement contours remain **Inventory conduct + normal TORCOVKA / PRISADKA / UPAKOVKA + R-05 `correctTorcovkaRailsTaken`**. Supply **NOT connected**. Production writer currently live = **NO**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **PSR-P2 SUPPLY SHADOW WRITER IMPLEMENTATION PACKAGE**. Supply writer is **READY TO START / NOT STARTED**. This addendum does not start that package, activate SHADOW, or deploy.

---
## PSR-P2 Supply SHADOW writer addendum — 2026-09-20

`audit/08.18-psr-p2-supply-shadow-writer.md` records the Supply SHADOW writer as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (PR #31; initial implementation head `b178720`; initial HEAD review **REQUEST CHANGES** P0=0 / P1=1 / P2=1; correction/accepted head `2e90060`; merge `164ffca`; exact-head CI `35493552025` SUCCESS; post-merge CI `35493953060` SUCCESS). Final HEAD review **PASS**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Findings **P0=0 / P1=0 / P2=0**. P1-01 **CLOSED**. P2-01 **CLOSED**. Supply prerequisite remains separately **COMPLETE** (`audit/08.17`). Trusted production preflight remains `35452435411` (TOTAL=0 / D=0 / S=0 / O=0 / `SUPPLY_DATA_BLOCKER=NO`). Source/`main` connected physical InventoryMovement contours = **Inventory conduct + normal TORCOVKA / PRISADKA / UPAKOVKA + R-05 `correctTorcovkaRailsTaken` + Supply deduction / Ozon restore**. Production writer currently live = **NO**. Schema/migration **NONE**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **PSR-P2 RAW RECEIPT / WRITE-OFF IDENTITY PREREQUISITE PACKAGE**. This addendum does not start that package, implement `createBatch` / `writeOffBatchRemainder` / `createSimplePurchase` writers, activate SHADOW, or deploy.

---
## PSR-P2 raw receipt / write-off identity addendum — 2026-09-20

`audit/08.19-psr-p2-raw-identity-prerequisite.md` records retained identities for `createBatch`, `writeOffBatchRemainder`, and `createSimplePurchase` as **IMPLEMENTATION ON FEATURE BRANCH / NOT MERGED / NOT DEPLOYED** (branch `feat/psr-p2-raw-identity-prerequisite`; base `ea794246`). Schema/migration **YES**. Historical backfill **NO**. InventoryMovement writers **NO**. Source connected contours remain **6**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT at that addendum was **HEAD REVIEW OF RAW RECEIPT / WRITE-OFF IDENTITY PR**. This addendum does not merge that PR, implement the three InventoryMovement writers, activate SHADOW, or deploy.

---
## PSR-P2 raw receipt / write-off identity closeout addendum — 2026-09-20

`audit/08.19-psr-p2-raw-identity-prerequisite.md` records the raw receipt / write-off identity prerequisite as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED** (PR #33; base `ea794246`; initial implementation HEAD `1af25770`; initial HEAD review **REQUEST CHANGES** P0=0 / P1=1 / P2=0; P1-01 CLOSED; correction/accepted HEAD `919a427`; merge `76131ca`; exact-head CI `35499639266` SUCCESS; post-merge CI `35500149127` SUCCESS). Final HEAD review **PASS**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Findings **P0=0 / P1=0 / P2=0**. Schema/migration **YES** on `main` (`20260920120000_psr_p2_raw_identity_prerequisite`). Historical backfill **NO**. InventoryMovement writers **NO**. Source connected contours remain **6**. SHADOW is not activated. Dual-write is **NOT STARTED**. Production application checkpoint remains `eb02b17`. Production does **not** contain the new identity tables. The merge SHA is **not** a production application SHA. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT is **PSR-P2 RAW IDENTITY PREREQUISITE PRODUCTION DEPLOYMENT PACKAGE**. This addendum does not deploy that package, implement the three InventoryMovement writers, activate SHADOW, or activate `production_cost_flow`.

---
## PSR-P2 raw identity production deploy addendum — 2026-09-20

This addendum does **not** rewrite the historical implementation / closeout addenda above.

`audit/08.19-psr-p2-raw-identity-prerequisite.md` now records the raw identity prerequisite as **IMPLEMENTED / MERGED / DEPLOYED / POST-DEPLOY VERIFIED**. Production Deploy `35502770382` SUCCESS (`workflow_dispatch`). Previous production application `eb02b17`. Deployed application `0cc8382`. Migration `20260920120000_psr_p2_raw_identity_prerequisite` **APPLIED**. Identity tables **LIVE / EMPTY AT CUTOVER**. Historical backfill **NO**. InventoryMovement pre/post 0/0/0. SHADOW ABSENT / INACTIVE. `production_cost_flow` ABSENT / INACTIVE. Rollback **NO**. P3009 recovery **NO**.

The already-accepted source writers that rode this 64-commit application rollout are now **DEPLOYED DORMANT / SHADOW INACTIVE**:

- Inventory conduct (`audit/08.14`)
- normal TORCOVKA / PRISADKA / UPAKOVKA (`audit/08.15`)
- R-05 `correctTorcovkaRailsTaken` (`audit/08.16`)
- Supply deduct / Ozon restore (`audit/08.18`)

Production dual-write **NOT STARTED**. Ledger **NOT AUTHORITATIVE**. PSR-P2 globally **NOT COMPLETE**. PSR-P3 **NOT STARTED**. A later docs-only merge is **not** a new production application SHA.

Current NEXT is **PSR-P2 RAW RECEIPT / WRITE-OFF SHADOW WRITERS IMPLEMENTATION PACKAGE**. This addendum does not implement those writers, activate SHADOW, or activate `production_cost_flow`.

---
## PSR-P2 raw receipt / write-off SHADOW writers addendum — 2026-09-20

This addendum does **not** rewrite the historical identity / deploy addenda above.

`audit/08.20-psr-p2-raw-shadow-writers.md` records SHADOW writers for `createBatch`, `writeOffBatchRemainder`, and `createSimplePurchase` as **IMPLEMENTATION ON FEATURE BRANCH / NOT MERGED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (branch `feat/psr-p2-raw-shadow-writers`; base `8374a0f`; production application `0cc8382`; Production Deploy `35502770382` unchanged). Schema/migration **NO**. Feature source connected contours **9**. Production deployed dormant contours **6**. SHADOW is not activated. Dual-write is **NOT STARTED**. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**. Independent Opus **SKIPPED BY OWNER COST POLICY**.

Current NEXT at that addendum was **HEAD REVIEW**. This addendum does not merge, deploy, activate SHADOW, or activate `production_cost_flow`.

---
## PSR-P2 raw receipt / write-off SHADOW writers merge closeout addendum — 2026-09-20

This addendum does **not** rewrite the historical identity / feature-branch addenda above.

`audit/08.20-psr-p2-raw-shadow-writers.md` now records the raw SHADOW writers as **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED** (PR #36; accepted HEAD `424f119`; merge `0bf2c77`; exact-head CI `35507221861` SUCCESS; post-merge CI `35507868236` SUCCESS). HEAD review **PASS**. Findings **P0=0 / P1=0 / P2=0**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Schema/migration **NO**. Source/`main` connected contours **9**. Production deployed dormant contours **6**. Production application remains `0cc8382`. The merge SHA is **not** a production application SHA. SHADOW is not activated. Dual-write is **NOT STARTED**. P2 globally **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

Current NEXT is **PSR-P2 RAW SHADOW WRITERS PRODUCTION DEPLOYMENT PACKAGE**. This addendum does not deploy, activate SHADOW, or activate `production_cost_flow`.
