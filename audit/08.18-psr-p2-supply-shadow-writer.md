# PSR-P2 — Supply SHADOW writer

Updated: 2026-09-20

Status: **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED**.

Source/`main` now contains the sixth physical InventoryMovement contour: Supply ProductStock deduction and Ozon cancellation positive ProductStock restore. Production application remains `eb02b17`. Production writers currently live = **NO**. SHADOW **NOT ACTIVATED**. Production dual-write **NOT STARTED**. Ledger **NOT AUTHORITATIVE**. PSR-P2 globally **NOT COMPLETE**. PSR-P3 **NOT STARTED**.

Supply prerequisite remains separately **COMPLETE** (`audit/08.17`). Trusted production preflight `35452435411` recorded TOTAL=0 / DEDUCTED_POSITIVE=0 / SHORTFALL_ONLY=0 / OPEN_DEDUCTED_POSITIVE=0 / `SUPPLY_DATA_BLOCKER=NO`. Do not rerun it. Do not conflate that prerequisite with this writer merge.

| | |
| --- | --- |
| Project / repository | Stell22 / `Mr-Pinkerton/Stell22` |
| Base `origin/main` | `b030086746292464414e1508c798cea2c8ff7e5a` |
| Initial implementation HEAD | `b1787205d51b9ce60ea6859708f9c3d80a6bda43` |
| Initial HEAD review | **REQUEST CHANGES** — P0=0 / P1=1 / P2=1 |
| Correction commit | `2e90060d145bac9fc9eb6b2fcaef4253fbfe877b` |
| Exact accepted HEAD | `2e90060d145bac9fc9eb6b2fcaef4253fbfe877b` (PR #31) |
| Merge commit | `164ffca49d5200af0b442de05c8966fc3b5c70ed` |
| Exact-head CI | `35493552025` SUCCESS |
| Post-merge CI | `35493953060` SUCCESS |
| Final HEAD review | **PASS** — P0=0 / P1=0 / P2=0; P1-01 CLOSED; P2-01 CLOSED |
| Independent Opus | **SKIPPED BY OWNER COST POLICY** |
| Production application checkpoint | `eb02b17f488ef9ec13a11dcf158fc169b78e3e37` (Production Deploy `35337215903`) **unchanged** |
| Schema / migration | **NONE** |
| SHADOW activation | **NO** |
| `production_cost_flow` | **INACTIVE** (unchanged; not activated) |
| Production access / mutation / deploy | **NO / NO / NO** |
| Runtime P2 global completeness | **NOT COMPLETE** |
| P3 | **NOT STARTED** |
| Ledger authoritative | **NO** |

The merge SHA is **not** a production application SHA. This package is **not deployed**. SHADOW remains **NOT ACTIVATED**. Dual-write remains **NOT STARTED**.

Canonicality: implementation PR #31 is already merged to `main`. This closeout update is on the docs PR and is **not** canonical on `main` until that PR receives HEAD acceptance and is merged. After reviewed merge of the closeout PR, this file becomes the canonical closeout state on `main`. Nothing in this docs PR is deployed.

```text
Source/main connected physical InventoryMovement contours:
1. Inventory conduct
2. normal TORCOVKA
3. normal PRISADKA
4. normal UPAKOVKA
5. R-05 correctTorcovkaRailsTaken
6. Supply deduction / Ozon restore

Production application:
still eb02b17
Supply SHADOW writer NOT DEPLOYED
other source writers NOT DEPLOYED

SHADOW:
NOT ACTIVATED

Production dual-write:
NOT STARTED

production_cost_flow:
INACTIVE
```

## 1. Scope

Connected:

- Supply ProductStock deduction (`toRemove > 0`)
- Ozon cancellation positive ProductStock restore (`restoreQty > 0`)

Not connected:

- Supply upsert alone
- PENDING Supply
- full-shortfall quantity
- shortfall itself
- zero-restore cancellation
- WB cancellation
- marketplace stock snapshot (`MpStock`)
- marketplace Sale rows
- `createBatch` / `writeOffBatchRemainder` / `createSimplePurchase`
- TORCOVKA line quantity edit
- PRISADKA/UPAKOVKA edit/delete
- generic TORCOVKA delete
- destructive master paths
- Correction Center / full `ProductionOperationMutation`

No schema change. No migration. No Supply command table. No new request IDs. No automatic/System sync. No raw receipt/write-off. No P3.

## 2. Actor contract

Current physical Supply path is authenticated manual marketplace sync.

```text
syncMarketplaces()
  → requireAdmin()
  → userMovementActorFromAdmin(admin)
  → syncMarketplacesAsUserInternal(actor)
```

Actor is captured **outside** the physical DB transaction. Required movement actor:

```text
actorKind = USER
userId = admin.id
actorDisplaySnapshot = admin.name
employeeId = NULL
systemActorKey = NULL
```

Marketplace/Ozon/WB is not the actor. User identity is not re-resolved inside the physical transaction.

Current enabled Supply SHADOW actor contour is only:

```text
authenticated application sync
→ requireAdmin()
→ USER actor
```

CLI physical sync is **DISABLED**. `scripts/run-mp-sync.ts` keeps import-only smoke. Normal execution fails closed with `MP_SYNC_CLI_PHYSICAL_EXECUTION_DISABLED` and directs the operator to `syncMarketplaces()`. It does not select an ADMIN, fabricate a USER actor, or call `syncMarketplacesAsUserInternal`. A future SYSTEM/CLI marketplace sync requires a separate explicit `systemActorKey` contract.

## 3. Transaction gate-first order

Accepted SHADOW-capable marketplace persistence order:

```text
BEGIN TX
→ isInventoryMovementShadowWriteActiveForWriter(tx)
→ Sale upsert
→ Supply global lock plan
→ validations
→ physical ProductStock effects
→ corresponding InventoryMovement
→ Supply accounting/status
→ ChangeLog / remaining marketplace writes
→ COMMIT
```

Outer gate snapshot `shadowWriteActive` is captured first and propagated into Supply accounting. Gateway may reacquire the same SHARED advisory xact lock later (reentrant).

Malformed `inventory_movement_shadow_write` fails closed before Sale, Supply, ProductStock, watermarks, MpStock, ChangeLog, and InventoryMovement. Remote API fetches before the DB transaction are not DB mutations.

## 4. Consume identity

Emitted only when an actual ProductStock decrement commits (`toRemove > 0`).

```text
stockDomain = PRODUCT
kind = CONSUMPTION
role = consume
quantityDelta = -toRemove
authority = SHADOW
epochId = NULL
causationKind = SUPPLY
causationId = Supply.id
reason = NULL
effectKey = imfx1:consume:<productTargetHashV1>:g=<generation>:w=<processedQtyAfter>
processedQtyAfter = newDeducted + newShortfall
stockAccountingGeneration = lifecycle.generation
```

Shortfall is never part of `quantityDelta`. Full shortfall (`toRemove = 0`, `shortfall > 0`) opens/updates the accounting cycle and emits **zero** InventoryMovement rows. A later quantity increase in the same generation uses the higher processed watermark and a distinct effectKey.

Retry / no-backfill: if `targetQty <= deductedQty + shortfallQty` there is no physical mutation and no movement. SHADOW OFF physical deduction followed by SHADOW ON retry of the same target must not backfill.

Consume causationSnapshot v1:

```text
{
  v: 1,
  d: "SUPPLY",
  type: "CONSUME",
  supplyId, marketplace, externalId, sku, productId,
  stockAccountingGeneration, processedQtyAfter,
  deductedQtyAfter, shortfallQtyAfter, quantityDelta
}
```

No money fields. No mutable Product name. No provider blob. No ChangeLog identity.

## 5. Restore identity

For an open cycle with `deductedQtyBeforeClose > 0` and retained `productId`:

```text
role = restore
kind = ADJUSTMENT
quantityDelta = +deductedQtyBeforeClose
causationKind = SUPPLY
causationId = Supply.id
effectKey = imfx1:restore:<productTargetHashV1>:g=<generation>:w=<deductedQtyBeforeClose>
```

Not `REVERSAL`. `reversalOfMovementId` remains NULL. Pre-SHADOW consume then ACTIVE restore is legal and does not require a historical consume movement.

Zero-restore (`deductedQty = 0`, `shortfallQty > 0`) still closes the cycle and emits **zero** movements.

Null-product positive restore remains `SUPPLY_CANCEL_PRODUCT_BINDING_REQUIRED` before ProductStock mutation, cycle close, movement, and ChangeLog.

Same-sync cancellation wins: no new consume for that snapshot; only the actual positive restore if prior `deductedQty > 0`.

Restore causationSnapshot v1:

```text
{
  v: 1,
  d: "SUPPLY",
  type: "RESTORE",
  supplyId, marketplace: "OZON", externalId, sku, productId,
  stockAccountingGeneration, deductedQtyBeforeClose,
  shortfallQtyBeforeClose, quantityDelta
}
```

## 6. effectiveAt

Supply `effectiveAt` is the server occurrence timestamp of the accounting physical mutation. Never marketplace `createdAt` / `acceptedAt` / provider time. `InventoryMovement.recordedAt` remains DB-generated. Separate Supply physical effects may have separate server occurrence timestamps. Persistence uses the common UTC-naive gateway contract.

## 7. Gateway invariant

For an outer SHADOW ACTIVE actual physical effect:

```text
result.gateActive === true
result.inserted === 1
```

Otherwise throw `SUPPLY_SHADOW_GATE_INVARIANT_VIOLATION` and roll back. Full shortfall / zero restore / retry/no-op do not call the gateway.

## 8. Cost-flow behavior

One TX-local `costFlowActive` remains pinned for global ProductStock planning + apply helpers.

- INACTIVE: quantity-only deduct/restore unchanged; SHADOW represents actual committed quantity changes.
- ACTIVE: missing ProductStock may be canonical initialized zero pool; full shortfall may still account; positive quantity-only deduct/restore fail `COST_FLOW_QTY_ONLY_WRITER`. No physical quantity effect → no movement.

No Inventory-style blanket dual-active error. `ARCH-P1-001` is untouched. Cost flow is not activated. Monetary Supply semantics are not implemented.

## 9. Proofs

Real PostgreSQL proofs cover:

- SHADOW OFF deduction and positive restore (movements = 0)
- SHADOW ACTIVE consume / partial shortfall / full shortfall / same-generation increase
- consume retry and OFF→ON no-backfill
- SHADOW ACTIVE restore / zero restore
- pre-SHADOW consume → ACTIVE restore
- cancellation OFF→ON no-backfill
- same-sync cancellation-wins
- null-product guard
- malformed gate fail-closed before transactional writes
- gate-flip: SHARED `(8322,1)` then later Supply lock; EXCLUSIVE waiter blocked until persist commits
- atomic rollback of deduction and restore via integrity-only INSERT trigger
- non-UTC `America/New_York` effectiveAt
- retained USER actor
- ACTIVE/INACTIVE cost-flow

Source pin: `isInventoryMovementShadowWriteActiveForWriter(tx)` occurs before Sale upsert, Supply accounting, Supply locks, ProductStock physical work, MpStock mutation, and ChangeLog.

## 10. Runtime confinement

Approved runtime `appendShadowInventoryMovements` owners:

- Inventory conduct
- production writer wrapper
- R-05 writer wrapper
- Supply writer wrapper (`src/server/internal/supply-shadow-write.ts`)
- gateway itself

No direct raw InventoryMovement INSERT outside the gateway.

## 11. Schema / activation

- Schema changes: **NONE**
- Migrations: **NONE**
- SHADOW activation: **NO**
- `production_cost_flow` activation: **NO**
- Production dual-write: **NOT STARTED**
- Production access / mutation / deploy: **NO / NO / NO**
- P2: **NOT COMPLETE**
- P3: **NOT STARTED**
- Ledger authoritative: **NO**

## 12. HEAD review of `b1787205d51b9ce60ea6859708f9c3d80a6bda43`

Verdict: **REQUEST CHANGES**. Findings: **P0=0 / P1=1 / P2=1**.

| Finding | Issue | Disposition |
| --- | --- | --- |
| P1-01 | Unauthenticated CLI falsely attributes Supply movements to first ADMIN | **CLOSED** |
| P2-01 | Optional SHADOW context allowed silent physical bypass | **CLOSED** |

P1-01 fix: physical `run-mp-sync.ts` execution is fail-closed. Import-only CI smoke remains.

P2-01 fix: `runSupplySyncAccounting` requires `actor` + `shadowWriteActive`. `applySupplyDeduction` / `applyOzonSupplyCancellation` require `shadow: { active, actor }`. `undefined` is not OFF (`SUPPLY_SHADOW_CONTEXT_REQUIRED`). Runtime helper ownership stays behind `persistMarketplaceSyncInTransaction`.

Final re-review of accepted HEAD `2e90060d145bac9fc9eb6b2fcaef4253fbfe877b`: **PASS — P0=0 / P1=0 / P2=0**. P1-01 **CLOSED**. P2-01 **CLOSED**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Exact-head CI `35493552025` SUCCESS. Post-merge CI `35493953060` SUCCESS.

## 13. Validation

- Unit: 99 files / 924 tests PASS (`npm test`; exact-head CI `35493552025`)
- PostgreSQL integrity: 29 files / 379 tests PASS (`npm run test:integrity:ci`; exact-head CI `35493552025`; post-merge CI `35493953060`)
- Includes new Supply writer integrity file plus R-04 / Supply prerequisite / Inventory / terminal / R-05 writer regressions
- `npx tsc --noEmit` PASS
- `npx eslint . --max-warnings=0` PASS
- `npm run build` PASS
- Server Action source policy PASS
- CLI import security PASS
- InventoryMovement append-only PASS
- SHADOW gate control PASS
- INSERT confinement PASS
- Supply production preflight workflow PASS
- Server Action manifest PASS
- `git diff --check` PASS
- Schema / migration: **NONE**
- Production access / mutation / deploy: **NO / NO / NO**

## 14. Still-blocked P2 contours

Connecting Supply does **not** make P2 near-complete. Still unresolved include:

- `createBatch` — retained retry/recovery identity required
- `writeOffBatchRemainder` — retained lifecycle/command identity required
- `createSimplePurchase` — retained request identity required
- TORCOVKA quantity edit
- PRISADKA/UPAKOVKA edit/delete
- positive-stock destructive master paths
- broader retained `ProductionOperation` mutation/cancellation identities

Generic TORCOVKA delete remains disabled.

P2 **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

## 15. NEXT

**PSR-P2 RAW RECEIPT / WRITE-OFF IDENTITY PREREQUISITE PACKAGE**

The next package must address retained identities before writers for `createBatch`, `writeOffBatchRemainder`, and `createSimplePurchase`. Do not implement those writers from this closeout. Do not add InventoryMovement to those contours yet. Do not deploy. Do not activate SHADOW. Do not activate `production_cost_flow`. Independent Opus remains **SKIPPED BY OWNER COST POLICY**.
