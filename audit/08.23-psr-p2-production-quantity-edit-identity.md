# PSR-P2 — Package 2 production quantity-edit identity prerequisite

Updated: 2026-09-21

Status:

```text
IMPLEMENTED /
MERGED TO MAIN /
CI VERIFIED /
DEPLOYED /
POST-DEPLOY VERIFIED
```

This package adds retained identity / replay / stable target / expected-old CAS / actor retention / effect snapshot for enabled physical contours A/B/C only.

It does **not** add an InventoryMovement writer. A/B/C remain without a ledger writer until Package 3. It does **not** activate SHADOW. It does **not** start dual-write. PSR-P2 remains **NOT COMPLETE**. PSR-P3 remains **NOT STARTED**. Package 3 is **NOT STARTED**. `ARCH-P1-001` remains **OPEN / CONFIRMED**.

Architecture is inherited from frozen `audit/08.21`. That file is unchanged.

| | |
| --- | --- |
| Project / repository | Stell22 / `Mr-Pinkerton/Stell22` |
| Starting `origin/main` | `fb22ab7ce2e55cf9ee4aee8026298f488107698c` |
| Implementation PR | [#42](https://github.com/Mr-Pinkerton/Stell22/pull/42) **MERGED** |
| Historical implementation-code commit | `789d63b471e98b665d14d098c92c60b2b38efbfa` (CI `35531258226` SUCCESS) |
| Historical prior docs/evidence commit | `e619e52490e647dfda542cff927fe583871ada84` (CI `35531560055` SUCCESS) |
| Accepted exact PR HEAD | `e143e22b6f805c9d4d053d761b5629fa5a325d9b` |
| Accepted exact-head CI | `35534868456` SUCCESS on `e143e22b6f805c9d4d053d761b5629fa5a325d9b` |
| Merge commit | `b033cfad88f01d9231f7b892fcce290244f48130` |
| Merge parents | `fb22ab7ce2e55cf9ee4aee8026298f488107698c` + `e143e22b6f805c9d4d053d761b5629fa5a325d9b` |
| Post-merge CI | `35535532806` SUCCESS on `b033cfad88f01d9231f7b892fcce290244f48130` |
| Previous production application | `4508ee1c5d242e679f18e331c35efc679e37c10c` |
| Production Deploy | `35536089109` SUCCESS |
| Production application | `b033cfad88f01d9231f7b892fcce290244f48130` |
| Migration | `20260920180000_psr_p2_production_quantity_edit_identity` **APPLIED** |
| Production table | `ProductionOperationQuantityEdit` **LIVE / EMPTY AT VERIFICATION** |
| Production structural verification | table exists = YES; row count = 0; foreign keys = 0; requestId exact-column UNIQUE = YES |
| Production read-only verifier PR | [#43](https://github.com/Mr-Pinkerton/Stell22/pull/43) **MERGED** |
| Accepted verifier PR HEAD | `e06009831abdd2462849fd71e40ce2efd09c9379` |
| Verifier workflow merge | `6621e875fdb10b720886708c361df13a08c7a471` |
| Verifier post-merge CI | `35538298018` SUCCESS on `6621e875fdb10b720886708c361df13a08c7a471` |
| Read-only production verifier run | `35592291536` **PASS** (job `106309154438`; completed / success) |
| Verifier source SHA | `6621e875fdb10b720886708c361df13a08c7a471` (**not** a production application SHA) |
| Production state at verification | `inventory_movement_shadow_write` **ABSENT**; `production_cost_flow` **ABSENT**; InventoryMovement count **0** |
| Architecture | `audit/08.21` frozen. Canonical? **YES** |
| Scope | A TORCOVKA / B PRISADKA / C UPAKOVKA quantity edit only |
| InventoryMovement writer for A/B/C | **NOT IMPLEMENTED** |
| SHADOW activation | **NO** |
| Dual-write | **NOT STARTED** |
| Ledger authoritative | **NO** |
| Package 3 | **NOT STARTED** |
| Runtime P2 global completeness | **NOT COMPLETE** |
| P3 | **NOT STARTED** |
| Independent Opus | **SKIPPED BY OWNER COST POLICY** |
| NEXT after this package | **PSR-P2 PRODUCTION QUANTITY-EDIT SHADOW WRITERS (PACKAGE 3)**. Not started. Not SHADOW activation. |

```text
A/B/C retained quantity-edit identity = IMPLEMENTED / DEPLOYED / POST-DEPLOY VERIFIED
InventoryMovement writer for A/B/C = NOT IMPLEMENTED
SHADOW = NOT ACTIVATED
dual-write = NOT STARTED
ledger authoritative = NO
Package 3 = NOT STARTED
P2 = NOT COMPLETE
P3 = NOT STARTED
ARCH-P1-001 = OPEN
```

---

## 1. Domain scope

Only A/B/C quantity edits.

Not absorbed: R-05 `correctTorcovkaRailsTaken`; production delete/cancellation; HOURS; Correction Center; generic `ProductionOperationMutation`; CommandExecution; InventoryMovement writer; SHADOW activation; Package 3.

HOURS remains editable through `updateProductionLineQuantity` and never creates `ProductionOperationQuantityEdit`.

## 2. Schema

```prisma
model ProductionOperationQuantityEdit {
  id                       String        @id @default(cuid())
  requestId                String        @unique
  operationId              String
  adminUserId              String
  actorDisplaySnapshot     String
  operationType            OperationType
  targetLineId             String?
  expectedOldQuantity      Int
  newQuantity              Int
  expectedStateFingerprint String
  requestSnapshot          Json
  effectSnapshot           Json
  recordedAt               DateTime      @default(now()) @db.Timestamptz(3)

  @@index([operationId, recordedAt])
}
```

No Prisma relation to `ProductionOperation` or `User`. No FK. No CASCADE.

`targetLineId`:

- TORCOVKA / PRISADKA: required original `OperationDetailLine.id`
- UPAKOVKA: null; target is the ProductionOperation / `productQty`

`operationType` on rows created by this package is only TORCOVKA / PRISADKA / UPAKOVKA.

Migration: `prisma/migrations/20260920180000_psr_p2_production_quantity_edit_identity/migration.sql`.

No historical backfill. Table empty at cutover. No InventoryMovement schema change. Existing correction tables unchanged.

## 3. Public API

Dedicated physical action:

`editProductionOperationQuantity(input: ProductionQuantityEditInput)`

```ts
type ProductionQuantityEditInput = {
  operationId: string;
  requestId: string;
  targetLineId?: string | null;
  expectedOldQuantity: number;
  newQuantity: number;
  expectedStateFingerprint: string;
};
```

`operationType` is not accepted from the client. The locked server row is authoritative.

`lineIndex` is not accepted as the A/B/C identity.

HOURS stays on `updateProductionLineQuantity` and does not require a physical-edit requestId.

`updateProductionLineQuantity` is HOURS-only. Direct invocation for TORCOVKA / PRISADKA / UPAKOVKA fails closed with `PHYSICAL_QUANTITY_EDIT_REQUIRES_RETAINED_COMMAND`. No physical mutation, retained row, or ChangeLog. There is no public compatibility adapter that translates mutable `lineIndex` into a retained physical command.

Integrity helpers may resolve a current id-ordered index only to discover the present stable line id, then call `editProductionOperationQuantity` with requestId + targetLineId/null + expectedOldQuantity + fingerprint. That helper is not a Server Action.

## 4. Stable UI target

`ProductionDetailLine` exposes `id`, current quantity, and opaque `editStateFingerprint` for TORCOVKA / PRISADKA.

UPAKOVKA exposes current `productQty` and operation-level `editStateFingerprint` on `ProductionEntryRow`.

Array indexes remain render-only.

UI helper: `src/lib/production-quantity-edit-request.ts`. Same requestId is retained for retry of the exact command and rotates after `STALE_QUANTITY_EDIT` / `REQUEST_ID_REUSE`. While a quantity-edit transition is pending, all Save buttons are disabled (`quantityEditUiLocked`). No mandatory reason UI.

## 5. Fingerprint v1

```text
version = qedit-state-v1
digest = SHA-256
encoding = lowercase hex
```

Canonical material is an explicit field-order builder. Arrays are sorted by stable row id. The fingerprint is the OLD COMMITTED operation state. It does not bind the future UPAKOVKA BOM used for the new apply.

TORCOVKA binds: operationId, operationType, lineId, quantity, blankMaterialId, blankLengthM (4-decimal), blankType, blankSort.

PRISADKA binds: operationId, operationType, lineId, quantity, detailId, blank*, prisadka flags, sourceIsBlank, source*Done.

UPAKOVKA binds: operationId, operationType, productId, productQty, committed `OperationDetailLine[]` + `OperationNomenclatureLine[]`.

## 6. Expected-old CAS

After lock/load:

- TORCOVKA / PRISADKA: exact `targetLineId` belonging to the operation; `line.quantity === expectedOldQuantity`; recomputed fingerprint must match.
- UPAKOVKA: `targetLineId == null`; `op.productQty === expectedOldQuantity`; recomputed provenance fingerprint must match.

Mismatch: `STALE_QUANTITY_EDIT`. No physical mutation. No retained command. No ChangeLog. No silent retarget.

## 7. Request / replay

Namespace `8326`: `pg_advisory_xact_lock(8326, hashtext(requestId))` inside the TX.

Same `requestId` + exact command-defining payload (operationId, adminUserId, targetLineId/null, expectedOldQuantity, newQuantity, expectedStateFingerprint): replay success. No second stock mutation, retained row, or ChangeLog.

Same `requestId` + different payload or different authenticated admin: `REQUEST_ID_REUSE`.

Replay is resolved before interpreting the post-edit line collection. This is required for PRISADKA because the original line may have been deleted by the first command.

## 8. Actor / ChangeLog

`requireAdmin()` and `userMovementActorFromAdmin` run outside the TX.

Retained: `adminUserId`, immutable `actorDisplaySnapshot`.

Successful non-replay ChangeLog: entity `ProductionOperation`, `userId = actor.userId`, old/new quantity, `quantityEditId`, `requestId`.

Replay and no-op do not create ChangeLog.

## 9. TX order

`Prisma.TransactionIsolationLevel.ReadCommitted`.

```text
requireAdmin outside TX
→ build retained USER actor
→ pure input validation
→ BEGIN READ COMMITTED
→ read SHADOW write gate first
→ if SHADOW ACTIVE: QUANTITY_EDIT_SHADOW_WRITER_NOT_READY
→ acquire qedit request advisory lock
→ lookup retained command by requestId
→ exact replay return OR REQUEST_ID_REUSE
→ lock ProductionOperation
→ optional retained-row recheck
→ load authoritative operation/provenance
→ unpaid guard
→ type/target validation
→ expected-old quantity CAS
→ expected-state fingerprint CAS
→ cost-flow/type safety guards
→ select exact replacement-set (INACTIVE PRISADKA = target line; ACTIVE PRISADKA = destination-sharing group)
→ PREPARE exact mutation:
   UPAKOVKA current-BOM plan
   INACTIVE PRISADKA dry-run allocation
   ACTIVE PRISADKA `prepareActivePrisadkaQuantityEdit`:
     Detail parent serialization lock
     → discover/ensure complete ACTIVE write-set
       (old dest/source, every existing DetailStock, all legal dest flag combos, Blank source)
     → ensure+lock active Blank/Detail pools
     → freeze canonical physical target set
→ discover FULL old ∪ prepared physical target set
→ acquire canonical physical locks for that FULL set
→ snapshot BEFORE quantities while locks are held
→ execute exactly the prepared mutation (ACTIVE PRISADKA still `correctActivePrisadkaLine`)
→ ACTIVE PRISADKA: fail closed if execution used a physical target outside the frozen set
→ snapshot AFTER quantities for the SAME full target set
→ derive command-only physical adjustments (missing before/after key is fail-closed)
→ persist ProductionOperationQuantityEdit
→ ChangeLog with admin userId + editId/requestId
→ COMMIT
```

No physical mutation before replay/CAS validation.

## 10. Temporary SHADOW-active guard

Package 2 has no InventoryMovement writer.

If `inventory_movement_shadow_write` is ACTIVE, A/B/C quantity edits fail closed before mutation with `QUANTITY_EDIT_SHADOW_WRITER_NOT_READY`.

This block exists only until Package 3. SHADOW is not activated here. No InventoryMovement is emitted.

## 11. Cost-flow

TORCOVKA / PRISADKA keep current supported ACTIVE/INACTIVE physical algorithms.

UPAKOVKA + `production_cost_flow = ACTIVE`: fail closed before physical mutation with `QUANTITY_EDIT_UPAKOVKA_COST_FLOW_NOT_READY`. `ARCH-P1-001` remains OPEN / CONFIRMED. Package 2 does not close it.

UPAKOVKA + INACTIVE remains supported.

## 12. No-op

If authoritative current quantity equals `newQuantity`: success/current row; no stock mutation; no retained command; no ChangeLog.

## 13. Request snapshot v1

```json
{
  "v": 1,
  "operationId": "...",
  "target": { "kind": "LINE", "lineId": "..." },
  "expectedOldQuantity": 10,
  "newQuantity": 12,
  "expectedStateFingerprint": "qedit-state-v1:..."
}
```

UPAKOVKA target is `{ "kind": "OPERATION" }`.

No actor fields. No money/value fields.

## 14. Effect snapshot v1

```json
{
  "v": 1,
  "before": { "... canonical committed old provenance ..." },
  "after": { "... canonical committed new provenance ..." },
  "physicalAdjustments": [
    { "... canonical physical target ...", "quantityDelta": 1 }
  ]
}
```

`physicalAdjustments` are NET COMMITTED PHYSICAL CHANGE per exact target (BLANK / DETAIL / NOMENCLATURE / PRODUCT). Zero deltas omitted. Sorted by targetType then canonical target-key. No monetary fields. No InventoryMovement `REVERSAL`. No `effectKey` grammar — Package 3 owns that.

PRISADKA provenance is the exact replacement-set of this command, not “all rows except originalLineId”. INACTIVE before = original target line; after = rows created as its replacement. ACTIVE before = destination-sharing group rewritten by `correctActivePrisadkaLine`; after = rows created to replace that group. Untouched retained lines are excluded. Snapshots are sorted by line id.

PRISADKA destination identity uses `prisadkaDestFlags` (same helper as reverse/apply). Second-stage TORCEVAYA from a plosk-ready source and second-stage PLOSKOST from a torcev-ready source both dest `true/true`.

ACTIVE PRISADKA frozen write-set is established only after the parent Detail lock. A destination/source that the correction will create or touch cannot first become visible after the BEFORE snapshot. Unused conservative members produce zero delta and are omitted from `physicalAdjustments`.

## 15. Tests

Dedicated PostgreSQL integrity: `src/server/integrity/psr-p2-production-quantity-edit-identity.integrity.test.ts` — **23/23 passed**.

Unit / confinement / admin-boundary: `production-quantity-edit.test.ts`, `production-quantity-edit-migration.test.ts`, `production-quantity-edit-request.test.ts`, confinement files, `admin-actions.security.test.ts`.

Local validation on this branch (pre-GitHub CI):

| Check | Result |
| --- | --- |
| Dedicated Package 2 integrity | 23/23 passed |
| Full `npm test` | 109 files / 986 tests passed |
| Full integrity suite | 33 files / 466 tests passed |
| `npx tsc --noEmit` | passed |
| `npm run lint` | passed |
| `npm run build` | passed |
| `git diff --check` | passed |
| `security:actions` | passed (114 public actions; 104 admin / 6 terminal / 4 auth) |
| `security:manifest` | passed |
| `security:cli-imports` | passed |
| `security:inventory-movement-append-only` | passed |
| `security:inventory-movement-shadow-write-control` | passed |
| `security:inventory-movement-insert-confinement` | passed (no A/B/C InventoryMovement INSERT path) |
| `security:supply-preflight-workflow` | passed |
| Migration | empty table, unique `requestId`, no FK / no backfill |

Regression included: Package 1 safe destructive guards, R-05, InventoryMovement append-only / SHADOW gate / INSERT confinement, DI-009, cost-flow package2/package3, cost-freeze, inventory integrity, supply preflight.

Historical implementation-code CI: run `35531258226` SUCCESS on `789d63b471e98b665d14d098c92c60b2b38efbfa`. Historical prior reviewed-head CI: run `35531560055` SUCCESS on `e619e52490e647dfda542cff927fe583871ada84`. Accepted exact-head CI: run `35534868456` SUCCESS on `e143e22b6f805c9d4d053d761b5629fa5a325d9b`. Post-merge CI: run `35535532806` SUCCESS on `b033cfad88f01d9231f7b892fcce290244f48130`.

## 16. Production verification

Read-only production verifier workflow: `.github/workflows/package2-production-readonly-verify.yml` (PR #43).

Verifier run `35592291536` completed / **success**. Job `106309154438`. Workflow source SHA `6621e875fdb10b720886708c361df13a08c7a471`. Production application SHA checked by the workflow: `b033cfad88f01d9231f7b892fcce290244f48130`. Verifier was run **exactly once**.

Exact verified markers:

```text
PRODUCTION_APP_SHA=b033cfad88f01d9231f7b892fcce290244f48130
P2_MIGRATION=APPLIED
P2_TABLE=EXISTS
P2_ROW_COUNT=0
P2_FK_COUNT=0
P2_REQUEST_ID_UNIQUE=YES
SHADOW_SETTING=ABSENT
COST_FLOW_SETTING=ABSENT
INVENTORY_MOVEMENT_COUNT=0
PACKAGE2_PRODUCTION_DB_VERIFY_OK
```

```text
migration 20260920180000_psr_p2_production_quantity_edit_identity = APPLIED
ProductionOperationQuantityEdit = LIVE / EMPTY AT VERIFICATION
foreign keys = 0
requestId exact-column UNIQUE = YES
InventoryMovement = 0
inventory_movement_shadow_write = ABSENT
production_cost_flow = ABSENT
```

`6621e875fdb10b720886708c361df13a08c7a471` is the verifier-workflow merge on `main`. It is **not** the production application SHA.

## 17. End state

A/B/C retained identity = **IMPLEMENTED / DEPLOYED / POST-DEPLOY VERIFIED**.

Remaining writer gap is unchanged: A/B/C still have no InventoryMovement writer.

```text
InventoryMovement writer for A/B/C = NOT IMPLEMENTED
SHADOW = NOT ACTIVATED
dual-write = NOT STARTED
ledger authoritative = NO
Package 3 = NOT STARTED
PSR-P2 = NOT COMPLETE
PSR-P3 = NOT STARTED
ARCH-P1-001 = OPEN
```
