# PSR-P2 — Inventory SHADOW writer prerequisite

Updated: 2026-09-18

Status: **IMPLEMENTATION ON FEATURE BRANCH / NOT MERGED / NOT DEPLOYED / INVENTORY WRITER NOT STARTED**.

Dual-write **NOT STARTED**. SHADOW **NOT ACTIVATED**. Physical InventoryMovement runtime writers **0**.

Base `origin/main`: `6a1eeaf6ad7f39803f0ddf193e2ac232531636a9`.
Branch: `feat/psr-p2-inventory-writer-prereq`.
Production application checkpoint remains `eb02b17f488ef9ec13a11dcf158fc169b78e3e37` (Production Deploy `35337215903`). This branch SHA is **not** a production application SHA.

Canonicality: this file lives on the feature branch until HEAD review and a reviewed merge. It is **not** accepted, **not** merged, **not** deployed.

This package prepares `conductInventory()` for the first SHADOW writer. It does **not** implement the writer.

## 1. Scope

Implemented:

- common SHADOW gate first in the Inventory transaction
- temporary fail-closed while the Inventory writer is still absent
- dual-active fail-closed
- one global deterministic BlankStock lock set / one acquisition pass
- retained authenticated USER actor propagation seam
- PostgreSQL-backed prerequisite / concurrency / retry tests
- this documentation

Explicitly not in this package:

- `appendShadowInventoryMovements(...)` call from `conductInventory`
- any InventoryMovement INSERT from physical runtime
- SHADOW activation
- first dual-write
- Supply / terminal production / R-05 / receipt / write-off writers
- schema changes / migrations
- Correction Center / full `ProductionOperationMutation`
- production deploy, access, or mutation
- AUDIT 2

## 2. Transaction order

Authenticated admin is captured **outside** the transaction.

```text
const admin = await requireAdmin()
actor = userMovementActorFromAdmin(admin)
        ↓
BEGIN TX
        ↓
SHARED InventoryMovement advisory TX lock
  via isInventoryMovementShadowWriteActiveForWriter(tx)
        ↓
read/validate inventory_movement_shadow_write (inside that helper)
        ↓
evaluate Inventory SHADOW safety
        ↓
read production_cost_flow
        ↓
fail closed if unsafe
        ↓
ONLY THEN Inventory row FOR UPDATE
        ↓
legacy DRAFT fail-closed / load lines
        ↓
stock locks (global BlankStock set, then Nom/Product or ACTIVE pools)
        ↓
STALE_SNAPSHOT validation / physical plan from deriveInventoryPhysicalEffects
        ↓
existing physical projection behavior
        ↓
status / ChangeLog with retained USER actor
        ↓
COMMIT
```

Ordinary writer-facing SHADOW gate access uses only `isInventoryMovementShadowWriteActiveForWriter(tx)`. Inventory does not read `inventory_movement_shadow_write` directly.

## 3. Temporary SHADOW-active fail-closed

This PR still has **no** Inventory SHADOW writer. Therefore `conductInventory()` must not silently continue physical mutation when the SHADOW gate is ACTIVE.

| Gate state | Behavior |
| --- | --- |
| SHADOW missing/inactive | Existing Inventory projection conduct continues |
| SHADOW ACTIVE + `production_cost_flow` INACTIVE | Fail closed **before** Inventory/business/stock mutation with `INVENTORY_SHADOW_WRITER_NOT_CONNECTED` |
| SHADOW ACTIVE + `production_cost_flow` ACTIVE | Fail closed **before** business/stock mutation with `INVENTORY_DUAL_ACTIVE_UNSUPPORTED` |

The package does **not** switch the SHADOW gate off, mutate the Setting, or activate either gate.

The next Inventory writer PR may remove the SHADOW-active / no-cost-flow temporary block only when `appendShadowInventoryMovements(...)` is connected atomically in the same transaction.

## 4. Global BlankStock lock plan

### 4.1 Old inactive shape (removed)

```text
lockBlankSpecs(no-prisadka DETAIL specs)
then
lockBlankStockByIds(BLANK InventoryLine ids)
```

Two independently ordered BlankStock acquisition batches. `assertNotLegacyInventoryDraft` does **not** make the second batch architecturally unreachable.

### 4.2 New plan

`planInventoryBlankStockLockSpecs` / `collectInventoryBlankLockSpecs`:

1. Direct `BLANK` InventoryLine `BlankStock.id` → physical tuple `(materialId, lengthM, detailType, sort)`.
2. DETAIL-related shared BlankStock tuples that the current Inventory lock plan can require (no-prisadka DETAIL aliases).
3. Deduplicate by canonical physical tuple.
4. Canonicalize `lengthM` with `canonicalLengthFixed4` / `canonicalLengthDecimal`.
5. Globally sort with `uniqueSortedBlankSpecs`.
6. Acquire BlankStock locks **once** for that set.

Input line order is irrelevant. Duplicate aliases collapse. `1.8` and `1.8000` are one target. Direct BLANK + no-prisadka DETAIL alias of the same tuple is one lock target. Missing BLANK ids are omitted (no invented tuple). Prisadka DETAIL lines do not add BlankStock targets.

`lockBlankSpecs` may still `createMany(... quantity: 0, skipDuplicates: true)` for a missing lock/projection row. That zero placeholder is **not** a physical movement and is **not** classified as an InventoryMovement writer.

### 4.3 ACTIVE cost-flow path

`ensureAndLockActiveBlankPools` locks the **same physical BlankStock rows** (FOR UPDATE by spec, with qty=0 ensure including cost fields). `lockActiveInventoryWriteSet` previously then called `lockBlankStockByIds` again — a second BlankStock batch on the same dependency.

That second batch is removed. ACTIVE Inventory now:

```text
lockDetails
collectInventoryBlankLockSpecs → ensureAndLockActiveBlankPools (one BlankStock pass)
ensureAndLockActiveDetailPools   // DetailStock cost pools, not BlankStock
ensureAndLockActiveNomPools
ensureAndLockActiveProductPools
```

Cost-flow valuation/accounting semantics, CostPool business behavior, and `production_cost_flow` activation are unchanged.

### 4.4 Cross-resource order (inactive)

```text
SHADOW advisory lock
→ Inventory row
→ Detail catalog
→ global BlankStock physical set
→ NomenclatureStock
→ ProductStock
```

ACTIVE keeps the already-established cost-flow pool order after the unified BlankStock pass. No new AB-BA relationship is introduced: every Inventory caller that locks BlankStock now uses the same global canonical spec order.

## 5. Retained USER actor

`conductInventory` captures `const admin = await requireAdmin()` before the transaction and builds `userMovementActorFromAdmin(admin)`.

The transaction core `conductInventoryInTransaction(tx, { docId, actor })` requires `actorKind = USER` with authenticated `User.id` and `actorDisplaySnapshot`. It does not re-resolve the user from the DB inside the TX. It does not hardcode `"Admin"`. It does not treat a marketplace/provider as actor.

The actor is written onto the Inventory CONDUCTED ChangeLog (`userId` + `actorKind` / `actorDisplaySnapshot` in `newValues`) and onto InventoryLine conduct ChangeLog `userId`. No InventoryMovement is created.

## 6. Canonical physical effect source

Unchanged: `InventoryPhysicalEffect[]` from `deriveInventoryPhysicalEffects(...)` remains the sole future movement derivation source. This package does not insert movements and does not rewrite effect semantics.

## 7. Tests

Unit:

- lock-plan order independence, alias collapse, `1.8`/`1.8000`, BLANK+DETAIL alias
- inactive/ACTIVE source pins: one BlankStock acquisition, no `lockBlankStockByIds`
- SHADOW safety decision table
- actor helper retains User.id
- conduct core uses only `isInventoryMovementShadowWriteActiveForWriter`
- warehouse action captures `requireAdmin()` before TX

PostgreSQL integrity (`src/server/integrity/psr-p2-inventory-writer-prereq.integrity.test.ts`):

- SHADOW inactive conduct: CONDUCTED, projection, deviation, `InventoryMovement` count **0**
- SHADOW ACTIVE / writer absent: `INVENTORY_SHADOW_WRITER_NOT_CONNECTED`; DRAFT; lines/projection/ChangeLog unchanged; movements **0**
- dual-active: `INVENTORY_DUAL_ACTIVE_UNSUPPORTED`; same no-mutation proof
- second conduct: `ALREADY_CONDUCTED`; projection not applied twice; movements **0**
- concurrent overlapping BlankStock lock plans from reversed line order: same canonical plan, no deadlock/lock-timeout, quantities unchanged, movements **0**
- concurrent second conduct of the same DRAFT: one success, one `ALREADY_CONDUCTED`, projection once
- retained USER actor: ChangeLog `userId` is the authenticated User.id, not `"Admin"`

### 7.1 Writer-specific tests still remaining (`audit/08.11` §15.1)

Not implemented here and must not be faked:

- one movement per actual canonical `InventoryPhysicalEffect`
- physical projection + InventoryMovement atomic rollback
- actual SHADOW movement retry / duplicate behavior
- exact movement rows produced by Inventory conduct

## 8. Static / runtime writer count

`appendShadowInventoryMovements(...)` has no physical Inventory call site. Allowed sites remain infrastructure / tests / checker only.

Physical runtime InventoryMovement writer count remains **0**.

P2-GUARD/CI static policy is not weakened.

## 9. Schema / migration / activation

- Schema changes: **NONE**
- Migrations: **NONE**
- SHADOW activation: **NO**
- `production_cost_flow`: unchanged / INACTIVE in this package
- Dual-write: **NOT STARTED**
- Production access / mutation / deploy: **NO / NO / NO**

## 10. What remains for the first Inventory writer

Connect `appendShadowInventoryMovements(...)` inside the same Inventory transaction **after** the existing physical plan, using:

- retained USER actor already on the conduct context
- canonical `InventoryPhysicalEffect[]`
- SHADOW-active path that currently fails `INVENTORY_SHADOW_WRITER_NOT_CONNECTED`

Remove that temporary SHADOW-active / no-writer block only in the same PR that actually inserts movements. Keep dual-active fail-closed until a later accepted dual-write design. Do not activate SHADOW or deploy from this file.

## 11. NEXT

**HEAD REVIEW OF INVENTORY WRITER PREREQUISITE PR**.

Not merge. Not deploy. Not SHADOW activation. Not the Inventory writer itself.
