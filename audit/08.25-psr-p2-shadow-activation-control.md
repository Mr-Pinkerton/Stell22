# PSR-P2 — SHADOW activation control and start verification

Updated: 2026-09-21

Package working name: `PSR-P2 SHADOW ACTIVATION CONTROL & START VERIFICATION`

Status:

```text
IMPLEMENTED /
CI VERIFIED /
AWAITING HEAD REVIEW /
NOT MERGED /
NOT RUN IN PRODUCTION /
SHADOW INACTIVE
```

Previous candidate `357576f182cf71331cf1547b20a09fbe451bbb12` is **REWORKED / SUPERSEDED**. It is not accepted evidence. Its CI record in §6 stays as history. Corrective commit `5f480224d28f4df484d20a81e286ff014a2ed147` is recorded in §6.2. This status is not production-activation readiness: `PRODUCTION_ENVIRONMENT_REF_POLICY = UNSAFE` (§7).

This package adds the production control plane required before the first activation of `inventory_movement_shadow_write`. It does **not** activate SHADOW. It does **not** run activate or deactivate against production. It does **not** start dual-write. It does **not** start PSR-P3. It does **not** make the ledger authoritative.

Package 3 remains `audit/08.24` (**IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / DEPLOYED / POST-DEPLOY VERIFIED / SHADOW INACTIVE**). This file does not rewrite that status. The Package 3 read-only verifier remains the dormant-state verifier (`PACKAGE3_PRODUCTION_POST_DEPLOY_VERIFY_OK`).

| | |
| --- | --- |
| Project / repository | Stell22 / `Mr-Pinkerton/Stell22` |
| Starting `origin/main` | `693a9f12fe52b7b6babecdf4f8b6d05e1082f7b6` |
| Canonical tree at start | `f1cf5cd2cef5cac46590bb85fcf05670968e96c5` |
| Production application | `2d2cbcbdc01ee83cb9103ad8fdaa43ebf8488dcf` |
| Production Deploy | `35616829882` SUCCESS |
| Verified production checkpoint | `35619733266` PASS |
| Branch | `psr-p2/shadow-activation-control` |
| Schema / migration | **NO / NO** |
| Production accessed | **NO** |
| Production mutated | **NO** |
| SHADOW activated | **NO** |
| `production_cost_flow` changed | **NO** |
| PSR-P3 started | **NO** |
| Dual-write | **NOT STARTED** |
| Ledger authoritative | **NO** |
| Connected / deployed dormant contours | **12 / 12** |
| Remaining enabled uncovered contours | **0** |
| `ARCH-P1-001` | **OPEN / CONFIRMED** |
| `INC-001` | **OPEN — CONTAINMENT DEPLOYED; PHYSICAL FACT REQUIRED FOR DATA CORRECTION** |

Writers are not redesigned. The twelve physical SHADOW writers remain the accepted surface. `ARCH-P1-001` stays the ACTIVE `production_cost_flow` + UPAKOVKA quantity-edit path. The inactive-cost-flow UPAKOVKA SHADOW writer stays supported.

## 1. Two states that must not be collapsed

| State | Meaning |
| --- | --- |
| A. `SHADOW GATE ACTIVATED` | `inventory_movement_shadow_write` is `{version:1, active:true}` |
| B. `PSR-P2 DUAL-WRITE STARTED` | Later real physical production activity wrote at least one valid SHADOW `InventoryMovement` while the gate was ACTIVE, shown by read-only evidence |

This package can only prepare A. Running the control workflow is not B. The control policy and the post-activation verifier always report `PSR_P2_DUAL_WRITE_STARTED=NO`. A SHADOW row count above zero is reported separately as `SHADOW_ROW_EVIDENCE=PRESENT` and is still not a dual-write-start declaration. This package does not create a synthetic production mutation to manufacture that evidence.

## 2. Activation workflow

File: `.github/workflows/shadow-activation-control.yml`

- Manual `workflow_dispatch` only. No push, pull request, or schedule trigger.
- Actions are exactly `activate` and `deactivate`.
- Job `control` uses GitHub `environment: production` only when `github.ref == 'refs/heads/main'` and the pin job succeeded. The pin job does not use that environment.
- Dispatch from any other branch or tag fails in the pin job (`REFUSE code=DISPATCH_REF_NOT_MAIN`) and does not start the production job.
- Control-plane checkout is explicit `ref: main`. It does not use the implicit `workflow_dispatch` checkout.
- `scripts/shadow-activation-assert-canonical-main.sh` fetches `origin/main`, prints `CONTROL_PLANE_CHECKOUT_SHA` and `ORIGIN_MAIN_SHA`, and refuses unless they are equal. That proof runs in the pin job, before the production environment job, and again before SSH.
- A workflow-file check of `github.ref` is not a security boundary against a different workflow file on another branch. See §7.
- The supplied `expected_application_sha` must equal the pinned production application SHA `2d2cbcbdc01ee83cb9103ad8fdaa43ebf8488dcf`.
- That application pin is not GitHub `main` and is not `origin/main`. The control-plane SHA and the production application SHA are different values and both are printed.
- Before any mutation, a read-only snapshot is taken on the server from the live `git rev-parse HEAD`.
- The runner evaluates that snapshot with `scripts/shadow-activation-policy.ts`. Refusal exits before apply.
- Apply pipes `scripts/shadow-activation-apply-deployed-cli.sh` to the server. That script re-checks the live SHA, the clean setter tree, and the image/host hash of `scripts/set-inventory-movement-shadow-write.ts`, re-reads the SHADOW setting in `BEGIN TRANSACTION READ ONLY`, then runs the deployed CLI:

`./node_modules/.bin/tsx scripts/set-inventory-movement-shadow-write.ts --state=on|off --confirm=INVENTORY_MOVEMENT_SHADOW_WRITE_CONTROL`

- The production image does not contain `src/`. The one-off container mounts the pinned checkout `src/` read-only so the already deployed CLI can load `setInventoryMovementShadowWriteGate`. The entrypoint is `tsx`, so the image entrypoint does not run migrations.
- The deployed setter remains the only Setting mutation. It upserts `{version:1, active}` under the existing EXCLUSIVE advisory transaction lock `(8322,1)`. Writers keep SHARED `(8322,1)`.
- There is no raw SQL Setting mutation, no reset, no backfill, and no `production_cost_flow` write.

The read-only pre-snapshot and the deployed setter are not one database transaction. This is not an atomic compare-and-set of every activation prerequisite. The apply path re-reads the SHADOW prior state immediately before the deployed CLI. The CLI then changes the gate inside its own EXCLUSIVE `(8322,1)` transaction. The setter itself is unchanged and does not branch on the previous Setting value. Overlapping control runs are serialized by the workflow concurrency group `inventory-movement-shadow-gate-control`.

### ACTIVATE

Transition: `ABSENT | INACTIVE → {version:1, active:true}`.

Already `ACTIVE` or `MALFORMED` fails closed. `CLI_STATE=on`.

### DEACTIVATE

Transition: `ACTIVE → {version:1, active:false}`.

`ABSENT`, `INACTIVE`, and `MALFORMED` fail closed. `CLI_STATE=off`.

Deactivation does not delete SHADOW rows, invoke reset, alter AUTHORITATIVE rows, alter epoch data, or alter `production_cost_flow`. Post-check requires the Setting to be `INACTIVE`, the application SHA unchanged, AUTHORITATIVE count unchanged, epoch counts unchanged, and cost-flow state unchanged. SHADOW count may rise (`after >= before`) when a writer that already holds SHARED `(8322,1)` commits before the EXCLUSIVE deactivation lock. A lower SHADOW count fails. Any total-count increase must equal the SHADOW increase (`total = SHADOW + AUTHORITATIVE`).

Post-activate uses the same count rule. After the gate is ACTIVE, a production transaction may commit SHADOW rows before the post-control snapshot. That increase passes when the SHA is unchanged, the gate is ACTIVE, cost-flow stays `ABSENT` or `INACTIVE`, AUTHORITATIVE count stays 0, and both epoch counts stay 0. SHADOW row loss fails.

## 3. Pre-activation checks

ACTIVATE refuses unless all of the following hold:

- live production application SHA equals the supplied SHA and the pin `2d2cbcbdc01ee83cb9103ad8fdaa43ebf8488dcf`;
- setter sources are clean and the deployed CLI file hash matches the checkout;
- `inventory_movement_shadow_write` is `ABSENT` or `INACTIVE`;
- `production_cost_flow` is `ABSENT` or `INACTIVE`;
- `AUTHORITATIVE` InventoryMovement count = 0;
- AUTHORITATIVE rows with non-null `epochId` = 0;
- any-row non-null `epochId` count = 0;
- SHADOW count + AUTHORITATIVE count = total InventoryMovement count;
- these migrations are applied and not rolled back:
  - `20260917150000_psr_p1_inventory_movement_shadow`
  - `20260917180000_psr_p2_inventory_movement_recorded_at_timestamptz`
  - `20260917200000_psr_p2_r04_supply_stock_accounting_cycle`
  - `20260918120000_psr_p2_r05_production_operation_correction`
  - `20260918130000_psr_p2_r06_inventory_line_identity`
  - `20260920120000_psr_p2_raw_identity_prerequisite`
  - `20260920180000_psr_p2_production_quantity_edit_identity`
- `InventoryMovement.recordedAt` is `timestamptz`;
- `InventoryMovement_authority_epoch` exists;
- Supply.`stockAccountingGeneration` exists;
- `ProductionOperationCorrection` exists;
- `InventoryLine_inventoryId_refType_refId_key` exists;
- `BatchCreationCommand`, `BatchRemainderWriteOff`, and `SimplePurchaseCreationCommand` exist;
- `ProductionOperationQuantityEdit` exists, has zero foreign keys, and `requestId` is unique.

`InventoryMovement` total count = 0 is **not** an activation invariant. A later OFF→ON with retained SHADOW rows (`epochId` null) is allowed. Any non-null `epochId` fails closed for this pre-epoch start.

## 4. Post-activation verifier

File: `.github/workflows/shadow-post-activation-verify.yml`

Read-only. Manual dispatch. `environment: production` on the verify job only. It uses the same snapshot script and `verify-active`. It does not call the apply script or the gate CLI.

It passes only when:

- production application SHA matches the pin;
- SHADOW Setting = `ACTIVE`;
- SHADOW count is a separate integer `>= 0` (zero is valid);
- AUTHORITATIVE count is separate and = 0;
- non-null `epochId` count is separate and = 0;
- `production_cost_flow` is `ABSENT` or `INACTIVE`;
- writer schema prerequisites in the snapshot are still present.

Success token: `SHADOW_POST_ACTIVATION_VERIFY_OK`.

It also prints `SHADOW_GATE_ACTIVATED=YES` and `PSR_P2_DUAL_WRITE_STARTED=NO`. This workflow was not run in production by this package.

## 5. What this package does not do

- production SHADOW activation or deactivation;
- production Setting mutation;
- schema migration;
- SHADOW reset or row deletion;
- `production_cost_flow` activation or mutation;
- epoch creation, OPENING_BALANCE, backfill, or an authoritative ledger;
- PSR-P3;
- writer semantic changes;
- Correction Center, INC-001 data correction, AUDIT 2, ARCH-P1-001 closure.

Deploy does not call the gate CLI. CI runs `security:shadow-activation-control`, which fails if deploy or the application entrypoint references activation.

## 6. Superseded candidate CI evidence

This section is historical. Candidate `357576f182cf71331cf1547b20a09fbe451bbb12` is **REWORKED / SUPERSEDED / NOT ACCEPTED**. Do not treat it as accepted evidence.

## 6.1. Historical CI of the superseded candidate

| | |
| --- | --- |
| Implementation commit | `ad4ad12cebc7b26e07c588b0fb5d1655799f2a17` |
| Implementation tree | `05971b506585469b8de497f6a9dc877988f1c17f` |
| PR | [#48](https://github.com/Mr-Pinkerton/Stell22/pull/48) |
| CI evidence class | **PR MERGE-REF CI / EXACT CANDIDATE-TREE VERIFIED** for the implementation commit. Not `exact-head CI`. |
| PR CI run | `35629950903` SUCCESS |
| Event | `pull_request` |
| Merge-ref SHA | `29516a73030cf302bcf3a602f8d4932f4de9926c` |
| Parent 1 | `693a9f12fe52b7b6babecdf4f8b6d05e1082f7b6` |
| Parent 2 | `ad4ad12cebc7b26e07c588b0fb5d1655799f2a17` |
| Tested tree | `05971b506585469b8de497f6a9dc877988f1c17f` |
| Candidate tree at that run | `05971b506585469b8de497f6a9dc877988f1c17f` |
| Exact candidate-tree equality | **YES** for that run |

The superseded candidate HEAD was `357576f182cf71331cf1547b20a09fbe451bbb12`, tree `dcd0cdfb016293b1ae08f59b13f8bc6705a5f0e2`. Its PR merge-ref CI was `35630639761` SUCCESS. Merge-ref `14a32a0b7b4ec2abd9dac45ad75961086a9ef848`. Parents `693a9f12fe52b7b6babecdf4f8b6d05e1082f7b6` + `357576f182cf71331cf1547b20a09fbe451bbb12`. Tested tree `dcd0cdfb016293b1ae08f59b13f8bc6705a5f0e2`, equal to that candidate tree. Evidence class for that run: **PR MERGE-REF CI / EXACT CANDIDATE-TREE VERIFIED**, not `exact-head CI`. HEAD verdict on that candidate: **REWORK REQUIRED / NOT ACCEPTED / NOT MERGE AUTHORIZED**.

## 6.2. Rework implementation CI

This run verified the corrective commit. It is **PR MERGE-REF CI / EXACT CANDIDATE-TREE VERIFIED** for that commit, not `exact-head CI`. §6 is unchanged.

| | |
| --- | --- |
| Corrective commit | `5f480224d28f4df484d20a81e286ff014a2ed147` |
| Corrective tree | `92a9e073d44e7b3dd16db50aa7671b0c7041c983` |
| PR | [#48](https://github.com/Mr-Pinkerton/Stell22/pull/48) |
| PR CI run | `35632266307` SUCCESS |
| Event | `pull_request` |
| Merge-ref SHA | `63acbe5c9d8b2052d944bff52a96283cc466d38c` |
| Parent 1 | `693a9f12fe52b7b6babecdf4f8b6d05e1082f7b6` |
| Parent 2 | `5f480224d28f4df484d20a81e286ff014a2ed147` |
| Tested tree | `92a9e073d44e7b3dd16db50aa7671b0c7041c983` |
| Candidate tree at that run | `92a9e073d44e7b3dd16db50aa7671b0c7041c983` |
| Exact candidate-tree equality | **YES** for that run |

## 7. Canonical-main dispatch and production environment ref policy

`PRODUCTION_ENVIRONMENT_REF_POLICY = UNSAFE`

Read-only evidence, 2026-09-21, not modified by this rework:

- `GET /repos/Mr-Pinkerton/Stell22/environments/production`
- `deployment_branch_policy: null` (GitHub documents null as allowing all branches to deploy)
- `protection_rules: []`
- `GET /repos/Mr-Pinkerton/Stell22/environments/production/deployment-branch-policies` returned HTTP 404

The code refuses a non-`main` dispatch before the production job starts. That workflow check does not stop a different workflow file on another branch from requesting the `production` environment. Until the environment restricts deployment branches, this package is not production-activation ready. That external gap does not by itself block HEAD review of the code candidate.

Rework implementation CI is §6.2. The environment policy remains an activation blocker. Do not merge. Do not deploy. Do not dispatch either workflow.
