# PSR-P2 — R-05 TORCOVKA railsTaken correction identity

Updated: 2026-09-18

Status: **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED**. Dual-write **NOT STARTED**. No InventoryMovement writer. Full `ProductionOperationMutation` / Correction Center **not** implemented.

This file records the accepted R-05 architecture, the implementation reviewed in PR #13, and the production deploy at application SHA `27538a5d5777d33fb87d060963a6d07a189cf044` (Production Deploy `35329071777`). Historical PR-#13 review/implementation status was **NOT DEPLOYED**. Current runtime state is **DEPLOYED**. Always verify GitHub `main` HEAD. A later docs-only merge may advance `main`; that merge SHA is **not** a production application SHA. Historical ChangeLog railsTaken rows are **not** backfilled.

---

## 0. Scope

| ID | Status |
| --- | --- |
| R-05 | **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED** |
| R-04 | remains **DEPLOYED** (`audit/08.08`) |
| R-06 | OPEN before INVENTORY dual-write |
| `ProductionOperationMutation` | remains the later Correction Center target (`audit/08.03`). **Not** this package. |
| DI-011 | remains **DEFERRED BY OWNER** |
| INC-001 | remains OPEN (containment unchanged) |

Later current-state (2026-09-18): R-06 implementation is recorded in `audit/08.10-psr-p2-r06-inventory-identity.md` (**ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED**). The R-06 **OPEN** row above is historical at R-05 deploy closeout. Dual-write remains **NOT STARTED**.

PSR-P2 dual-write remains **NOT STARTED**. AUDIT 2 remains **NOT STARTED**.

---

## 1. Why decrease-only lock is not idempotency

`correctTorcovkaRailsTaken` already locked `ProductionOperation` `FOR UPDATE` and rejected `newRailsTaken >= current`. That prevents some double RailLot increments **after success**, but:

- a retry after commit-unknown **errored** instead of returning the same correction;
- each success minted a new `ChangeLog.id` (cuid, unseen by the caller);
- concurrent different targets with the same expected-old were not CAS-protected as distinct commands.

R-05 adds a retained command identity and expected-old compare-and-set.

---

## 2. Accepted model — `ProductionOperationCorrection`

Narrow retained entity for **this** physical path only.

It is **not** generic Correction Center, **not** cancellation, **not** an InventoryMovement writer.

Logical command:

```
ADMIN requests: operationId, expectedOldRailsTaken, newRailsTaken, reason, requestId
```

Meaning: correct this TORCOVKA **from expected old X to target Y**. Not a blind “set current to Y”.

`requestId` is caller-supplied, globally UNIQUE, reused unchanged for retries of the **same** logical command. It is **not** `ProductionOperation.clientRequestId`. Format/validation reuses `requireClientRequestId` (trim, 1–128).

No Prisma relations / SQL `REFERENCES`. Scalar retained IDs (`operationId`, `adminUserId`, `railLotId`, `batchId`) so correction history cannot cascade-delete with another domain row.

---

## 3. Idempotent replay and stale CAS

Inside one DB transaction:

1. Acquire R-05 requestId advisory xact lock  
   `pg_advisory_xact_lock(8325, hashtext(requestId))`.  
   Namespace `R05_CORRECTION_REQUEST_LOCK_NAMESPACE = 8325` is **not** R-10 `(8322, 1)`. The lock is transaction-scoped concurrency only; hash collisions may extra-serialize unrelated requests and must not change correctness.
2. Lookup by `requestId`.
3. If found: payload must match `operationId`, `adminUserId`, `expectedOldRailsTaken`, `newRailsTaken`, canonical trimmed `reason`. Match → **SUCCESS** `replayed=true`, no stock/op/ChangeLog writes (even if the op is now paid/frozen). Mismatch → `REQUEST_ID_REUSE`. No physical row locks required for a committed correction.
4. If missing: lock `ProductionOperation` `FOR UPDATE`; re-check `requestId` against non-conforming writers.
5. Verify TORCOVKA and lot/batch/rails fields; reject paid.
6. Stale if `op.railsTaken !== expectedOldRailsTaken` → `STALE_CORRECTION` (do not reinterpret against the new current value).
7. Lock RailLot / Batch in the existing canonical order; reject frozen; length guard; inventory boundary.
8. Insert correction row (stable `id` = future `causationId`) **before** the physical RailLot/op write (and before the active cost-flow helper if that gate is on).
9. Apply existing physical effect; ChangeLog; optional batch reopen; commit.

Global `requestId` is serialized **before** correction creation, so cross-operation reuse of the same key is deterministic (`REQUEST_ID_REUSE`) and cannot leak a P2002 / aborted-transaction error among conforming writers. Existing op `FOR UPDATE` still provides operation CAS serialization for distinct requestIds.

Concurrent identical `requestId`+payload: one physical effect, one row, one primary ChangeLog; waiters replay.

Concurrent different commands both `expectedOld=10`: lock winner commits; loser is **STALE**, not a silent 8→6.

Sequential 10→8 then 8→6 requires a **new** `requestId` and `expectedOld=8`.

Do **not** recover UNIQUE violations with a SELECT in the same PostgreSQL transaction after P2002; that transaction is already aborted.

---

## 4. ChangeLog

Secondary audit only. `ChangeLog.id` is **not** correction identity.

RailsTaken payload now includes `correctionId`, `requestId`, plus existing old/new/delta/reason. `userId` is the admin from `requireAdmin()`.

---

## 4.1 Exact schema / constraints

Prisma model `ProductionOperationCorrection` (no relations):

| Field | Type / notes |
| --- | --- |
| `id` | `String` `@id` `@default(cuid())` |
| `requestId` | `String` `@unique` |
| `operationId` | `String` (scalar; no FK) |
| `adminUserId` | `String` (scalar; no FK) |
| `railLotId` | `String` (scalar; no FK) |
| `batchId` | `String` (scalar; no FK) |
| `expectedOldRailsTaken` | `Int` |
| `newRailsTaken` | `Int` |
| `deltaReturned` | `Int` |
| `reason` | `String` |
| `recordedAt` | `DateTime` `@default(now())` `@db.Timestamptz(3)` |

**FK choice:** no Prisma relations and no SQL `REFERENCES`. Scalar retained IDs so correction history cannot be cascade-deleted with another domain row.

Migration: `prisma/migrations/20260918120000_psr_p2_r05_production_operation_correction/migration.sql`

Constraints / indexes:

- PK `id`
- UNIQUE `requestId`
- INDEX `(operationId, recordedAt)`
- CHECK `char_length(btrim(requestId)) BETWEEN 1 AND 128`
- CHECK `expectedOldRailsTaken > 0`
- CHECK `newRailsTaken > 0`
- CHECK `expectedOldRailsTaken > newRailsTaken`
- CHECK `deltaReturned > 0`
- CHECK `deltaReturned = expectedOldRailsTaken - newRailsTaken`
- CHECK `btrim(reason) <> ''`
- `recordedAt TIMESTAMPTZ(3) NOT NULL DEFAULT CURRENT_TIMESTAMP`

Empty table. No INSERT. No InventoryMovement changes.

---

## 5. Future InventoryMovement (not in this package)

| Field | Value |
| --- | --- |
| `causationKind` | `PRODUCTION_OPERATION_MUTATION` |
| `causationId` | `ProductionOperationCorrection.id` |
| grain | one RAIL_LOT return per correction (`deltaReturned`) |
| `railLotId` / `operationId` / actor / reason | from the correction row |
| `effectiveAt` candidate | `correction.recordedAt` |
| `recordedAt` | IM DB default when a later writer inserts |

Do **not** freeze `effectKey`. No writer here. `inventory_movement_shadow_write` remains ABSENT.

Compatible with later full `ProductionOperationMutation`: this table can remain the retained sub-identity for this path, or be referenced by POM later. This package does **not** implement POM.

---

## 6. Historical cutover

Table starts **empty**. Do not reconstruct prior railsTaken ChangeLogs. Do not fabricate InventoryMovement.

---

## 7. Implementation files

- `prisma/schema.prisma` (`ProductionOperationCorrection`, no FKs)
- `prisma/migrations/20260918120000_psr_p2_r05_production_operation_correction/migration.sql`
- `src/server/internal/production-operation-correction.ts` (`R05_CORRECTION_REQUEST_LOCK_NAMESPACE = 8325`)
- `src/server/production.ts` (`correctTorcovkaRailsTaken`)
- `src/lib/production-correction-request.ts` (UI requestId retain/mint)
- `src/components/production/production-view.tsx`

UI requestId lifecycle: mint once when a new logical correction submission begins; retain across retries of that same pending command; clear after definitive success; rotate after `STALE_CORRECTION` / `REQUEST_ID_REUSE`. Not minted on every React render.

---

## 8. Completion (PR #13 implementation / review — historical)

This section is the status at PR #13 implementation/review and merge-to-main time. It does **not** describe the later production deploy. Current runtime status is §9.

| | |
| --- | --- |
| Status at PR #13 review (**historical**) | **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED** |
| Production application then | `c14a58641d57dfabfce223b2c213db9c86167ca4` (**unchanged** in the implementation cycle) |
| Dual-write | **NOT STARTED** |
| InventoryMovement writers | **0** |
| `inventory_movement_shadow_write` | ABSENT / inactive |
| NEXT then | **R-05 PRODUCTION PREFLIGHT / DEPLOYMENT DECISION**. Not R-06. Not dual-write. Not Correction Center. Always verify current GitHub `main` HEAD. |

---

## 9. Production deploy closeout (2026-09-18)

This section does **not** rewrite §8 historical review state. Schema/constraint contract remains §4.1.

| | |
| --- | --- |
| Current status | **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED TO main / DEPLOYED** |
| Production application | `27538a5d5777d33fb87d060963a6d07a189cf044` |
| Evidence | Production Deploy run `35329071777` SUCCESS (`workflow_dispatch`). Resolve `105548942840`. CI/verify `105548987647`. Deploy `105549637693`. Created `2026-09-18T09:21:04Z`. Completed `2026-09-18T09:27:58Z`. |
| Prior production | `c14a58641d57dfabfce223b2c213db9c86167ca4` (**historical** R-04 application checkpoint) |
| Migration | `20260918120000_psr_p2_r05_production_operation_correction` **APPLIED** |
| `ProductionOperationCorrection` | **LIVE**. Initial row count **0**. Historical backfill **NO**. No fabricated correction identities. |
| InventoryMovement | 0 / SHADOW 0 / AUTHORITATIVE 0 |
| Runtime writers | **0**. R-05 deploy did **not** start an InventoryMovement writer. |
| `inventory_movement_shadow_write` | ABSENT / inactive |
| `production_cost_flow` | **INACTIVE** |
| Dual-write | **NOT STARTED** |
| P3009 recovery | **NO** |
| Rollback | **NO** |
| R-04 | remains **DEPLOYED** (`audit/08.08`) |
| R-06 | **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / NOT DEPLOYED** (`audit/08.10`) |
| INC-001 | **OPEN** |
| AUDIT 2 | **NOT STARTED** |
| NEXT | **R-06 HEAD REVIEW**. Not InventoryMovement dual-write. Not SHADOW activation. Not Correction Center. Not AUDIT 2. |
