# PSR-P2 — SHADOW writer contract

Updated: 2026-09-18

Status: **WRITER CONTRACT ONLY / DUAL-WRITE NOT STARTED / SHADOW NOT ACTIVATED**.

Starting `origin/main`: `2eb4f6a6d769900481a5c3fd33a76240789d953d` (PR #16 merge).
Production application checkpoint remains `eb02b17f488ef9ec13a11dcf158fc169b78e3e37` (Production Deploy `35337215903`).

This document freezes the PSR-P2 contract for future SHADOW InventoryMovement writers. It does **not** implement a runtime writer, activate `inventory_movement_shadow_write`, activate `production_cost_flow`, mutate production data, start PSR-P3, or start AUDIT 2.

## 1. Baseline and accepted review adjudication

Preserve:

- PSR-P1 = COMPLETE / IMPLEMENTED / DEPLOYED; InventoryMovement schema LIVE.
- Last verified InventoryMovement rows = 0; runtime InventoryMovement writers = 0.
- Q4 / R-07 / R-10 / R-11 = DEPLOYED DORMANT.
- R-04 Supply identity = DEPLOYED.
- R-05 TORCOVKA correction identity = DEPLOYED.
- R-06 Inventory physical identity = DEPLOYED / POST-DEPLOY VERIFIED.
- `production_cost_flow` = INACTIVE.
- `inventory_movement_shadow_write` = inactive / absent at last verification.
- PSR-P2 dual-write = NOT STARTED.
- AUDIT 2 = NOT STARTED.

Accepted P1-1 decision: `InventoryPhysicalEffect[]` is the canonical Inventory movement derivation source. While the ACTIVE cost-flow projection implementation still independently derives its physical mutation, Inventory conduct must fail closed **before any physical mutation** when both `inventory_movement_shadow_write` and `production_cost_flow` are active. P2 must not introduce a common effects-driven movement writer while allowing an independently re-derived ACTIVE physical mutation to run.

Before the first SHADOW writer PR may merge:

1. static policy must forbid direct application reads of `inventory_movement_shadow_write` outside the approved gate module;
2. append-only policy must also reject obvious raw `TRUNCATE` of `InventoryMovement`, except explicitly approved non-runtime maintenance/migration paths;
3. PostgreSQL-backed gate/writer/idempotency tests must run in mandatory GitHub CI; `npm test` alone is insufficient.

## 2. Physical-writer matrix

Readiness here means readiness **after the common gate/writer guard package described in this contract is present**. A contour marked BLOCKED must not receive a SHADOW writer until its prerequisite is closed.

| Contour | Projection(s) | TX / current lock order | Current retry/idempotency | Causation / actor | Exact physical effects | P2 readiness / blocker |
| --- | --- | --- | --- | --- | --- | --- |
| `createBatch` | creates `RailLot.remainingQuantity = quantity` | one Prisma TX; nested Batch/RailLot create | DB-unique Batch name prevents a same-payload retry from creating a second same-name batch, but replay does not return prior causal id | `BATCH` / Batch.id; USER; current action discards `requireAdmin()` result, so userId must be propagated | RECEIPT +qty per created RAIL_LOT | **BLOCKED** until retained retry-stable create command identity or an explicit proof/contract that duplicate-name failure is the accepted COMMIT-UNKNOWN outcome and writer identity can be recovered. Do not rely on InventoryMovement UNIQUE. |
| `writeOffBatchRemainder` | `RailLot.remainingQuantity -> 0` | one TX; RailLots then Batch locks | second call sees zero and fails; physical projection cannot apply the same write-off twice | `BATCH` / Batch.id; USER; current action discards `requireAdmin()` result, so userId must be propagated | ADJUSTMENT -current remaining per affected RAIL_LOT | **READY** after common gate + deterministic effect derivation. One write-off lifecycle only; effect identity includes RailLot target + WRITE_OFF. |
| `createSimplePurchase` | `NomenclatureStock +qty` | one TX; no retained request lock/idempotency command | none; retry after COMMIT-UNKNOWN can create a second SimplePurchase and add stock again | `SIMPLE_PURCHASE` / SimplePurchase.id; USER; current action discards `requireAdmin()` result, so userId must be propagated | RECEIPT +qty NOMENCLATURE | **BLOCKED**: add retained request/command identity and replay semantics before writer. |
| `submitTorcovka` | `RailLot -railsTaken`; `BlankStock +outputs` | one TX; terminal path; current business locks/conditional updates; blank outputs spec-sorted | `ProductionOperation.clientRequestId` UNIQUE; P2002 is replay success; stock + op atomic | `PRODUCTION_OPERATION` / ProductionOperation.id; EMPLOYEE | CONSUMPTION RailLot; PRODUCTION_OUTPUT per Blank target | **READY** after common gate is taken first and exact effect-set extraction is implemented. |
| `submitPrisadka` | `BlankStock/DetailStock -inputs`; `DetailStock +outputs` | one TX; Detail locks; current inactive source selection; active path has its own write-set locking | `clientRequestId` UNIQUE with replay success | `PRODUCTION_OPERATION` / ProductionOperation.id; EMPLOYEE | CONSUMPTION per retained source target; PRODUCTION_OUTPUT per destination Detail target | **READY** after common gate + deterministic effect aggregation/order. |
| `submitUpakovka` | `BlankStock/DetailStock/NomenclatureStock -inputs`; `ProductStock +output` | one TX; prepared BOM snapshot; Detail locks, then stock mutations | `clientRequestId` UNIQUE with replay success | `PRODUCTION_OPERATION` / ProductionOperation.id; EMPLOYEE | CONSUMPTION source pools and nomenclature; PRODUCTION_OUTPUT product | **READY** after common gate + deterministic global target lock/effect order. |
| `updateProductionLineQuantity` TORCOVKA | `BlankStock +/-delta` | one TX; ProductionOperation lock then inventory-boundary/stock locks | no retained mutation/request identity; same ProductionOperation may be edited repeatedly | future `PRODUCTION_OPERATION_MUTATION`; USER | signed correction to Blank target | **BLOCKED**: add retained mutation identity/replay contract. |
| `updateProductionLineQuantity` PRISADKA | reverse old Detail/Blank effects then apply new | one TX; ProductionOperation + reverse/apply locks | no retained mutation identity | future `PRODUCTION_OPERATION_MUTATION`; USER | compensating source/destination effects | **BLOCKED**: retained mutation identity and exact effect set required. |
| `updateProductionLineQuantity` UPAKOVKA | reverse Product/input pools then reapply | one TX; ProductionOperation + prepared union/reverse/apply locks | no retained mutation identity | future `PRODUCTION_OPERATION_MUTATION`; USER | compensating old effects + new effects | **BLOCKED**: retained mutation identity; do not use original ProductionOperation id as a new mutation identity. |
| `deleteProductionOperation` PRISADKA / UPAKOVKA | reverse stock then hard-delete retained operation/lines | one TX | no retry-stable deletion identity; causal source is destroyed | future history-preserving mutation/cancellation; USER | compensating physical effects | **BLOCKED**. During P2 these paths must either gain retained history-preserving causal identity or be disabled from physical mutation. Full destructive-history cleanup remains P5. |
| generic TORCOVKA delete | would affect BlankStock only; RailLot is intentionally not restored | one TX but `assertTorcovkaGenericDeleteAllowed` rejects before mutation | disabled | n/a | none while disabled | **READY AS DISABLED**. Keep fail-closed; do not re-enable in P2. |
| `correctTorcovkaRailsTaken` (R-05) | `RailLot.remainingQuantity += deltaReturned` | one TX; request advisory lock -> ProductionOperation -> RailLot -> Batch | retained `ProductionOperationCorrection.requestId`, request serialization, payload match, expected-old CAS; replay returns retained row | `PRODUCTION_OPERATION_MUTATION` / ProductionOperationCorrection.id; USER | ADJUSTMENT on actual committed RailLot delta | **BLOCKED** only on explicit READ COMMITTED correctness dependency + common gate-first ordering; causal identity itself is READY. |
| `conductInventory` | Product/Blank/Detail/Nomenclature projections | one TX; Inventory row; currently cost-flow gate then one of two stock-lock plans | DRAFT -> CONDUCTED state transition; second call fails ALREADY_CONDUCTED | `INVENTORY` / Inventory.id; USER; current action discards `requireAdmin()` result, so userId must be captured before the TX and passed into the causal snapshot | ADJUSTMENT rows from canonical `InventoryPhysicalEffect[]` only | **BLOCKED** until one global BlankStock lock-set/order replaces the current two-batch dependency; common SHADOW gate must precede business/stock locks; dual-active state fails closed. |
| `applySupplyDeduction` | `ProductStock -toRemove`; Supply accounting watermarks | marketplace sync TX; Supply locks sorted by business key, ProductStock locked later per step | locked cumulative `deductedQty + shortfallQty` watermark / generation; retry does not deduct processed target twice | `SUPPLY` / Supply.id; USER for authenticated manual sync, SYSTEM for true automation | CONSUMPTION only actual `toRemove`; shortfall is not movement | **BLOCKED** until global deterministic Supply + ProductStock lock-set/order is implemented and the null-product accounting edge is guarded. |
| `applyOzonSupplyCancellation` | `ProductStock +deductedQty`, then cycle close | Supply row lock; ProductStock later | open-cycle state makes repeated cancellation no-op | `SUPPLY` / Supply.id; USER or SYSTEM by actual caller | positive compensating ADJUSTMENT of actual restored qty | **BLOCKED**: current code can close a cycle with `deductedQty>0` and `productId=NULL` without physical restore. Must fail closed before cycle close. |
| `deleteBatch` | hard-deletes RailLots, including potentially nonzero `remainingQuantity` when no ops/deal | transaction array; no physical movement document | no retained deletion command identity | future MANUAL or retained batch cancellation; USER | destruction of positive RAIL_LOT balance is physical | **BLOCKED**. Before P2 COMPLETE, deleting a batch with nonzero remaining RailLot quantity must be disabled or represented by retained causal physical effects. |
| Product/material/detail/nomenclature hard-delete paths | master records | app guards / FK restrictions; Product explicitly blocks any ProductStock row; material/detail/nomenclature usage guards | n/a | n/a | no verified enabled physical-stock destruction in current main | **NO WRITER**. Keep guards; any future cascade/direct stock destruction becomes a P2 contour. |
| `scripts/smoke-production-reversal.ts` | directly seeds/mutates Detail/Nomenclature/Product stock and invokes production mutations | non-runtime CLI, direct root Prisma | not a production causal writer | test-only | arbitrary fixture deltas | **NON-RUNTIME / BLOCKED FOR PRODUCTION**. Must never be treated as an allowed production stock writer. |
| `prisma/seed.ts` | destructive reset and direct RailLot/Blank/Detail/Nomenclature/Product stock writes | seed/reset path | not runtime idempotency | seed-only | arbitrary fixture/reset writes | **NON-RUNTIME / BLOCKED FOR PRODUCTION**. Excluded from P2 runtime coverage only because production execution is prohibited. |
| SHADOW reset / gate CLIs | InventoryMovement/Setting only | dedicated maintenance TX + exclusive advisory lock | controlled by R-10/Q4 | SYSTEM maintenance | no physical projection mutation | not a physical writer; preserve as approved maintenance path. |

P2 is incremental, but **P2 is not COMPLETE** until every enabled physical mutation contour is either dual-writing correctly or explicitly disabled from physical mutation.

## 3. Common InventoryMovement writer/gateway API

Introduce one narrow internal gateway, not a generic CommandExecution bus. Conceptual API:

```ts
appendShadowInventoryMovements(
  tx: Prisma.TransactionClient,
  input: {
    effectiveAt: Date;
    actor: MovementActorSnapshot;
    causation: MovementCausationSnapshot;
    effects: readonly MovementEffect[];
  }
): Promise<{ gateActive: boolean; inserted: number }>
```

Contract:

- accepts an **open** `Prisma.TransactionClient` only; root Prisma client fails closed;
- the approved writer entry must call `isInventoryMovementShadowWriteActiveForWriter(tx)`; no caller performs a direct gate read;
- helper obtains SHARED advisory TX lock on `(8322,1)` before reading the gate;
- malformed gate fails closed before physical mutation;
- inactive gate means legacy projection behavior and zero InventoryMovement rows;
- active gate writes authority = SHADOW and epochId = NULL;
- writer never supplies `recordedAt`; DB generates TIMESTAMPTZ(3);
- no monetary fields or monetary lookups;
- `quantityDelta=0` is omitted;
- insertion order is deterministic by canonical semantic effect key, never `recordedAt` or input-array order;
- projection mutation + movement insertion are in the same DB transaction;
- no post-commit/background insert and no root Prisma client;
- duplicate InventoryMovement uniqueness is the final barrier only; caller contour must independently prove physical idempotency.

## 4. Mandatory transaction / gate / lock order

For every SHADOW-capable physical transaction:

1. open DB transaction;
2. obtain SHARED InventoryMovement advisory TX lock via `isInventoryMovementShadowWriteActiveForWriter(tx)`;
3. read/validate `inventory_movement_shadow_write` inside that helper;
4. if contour is Inventory and gate is active, evaluate `production_cost_flow`; if it is also active, fail closed **before business/stock mutation** until the ACTIVE projection is unified with `InventoryPhysicalEffect[]`;
5. acquire business/document/stock locks in the contour's globally deterministic order;
6. validate current state;
7. derive exact physical effects;
8. mutate projection;
9. insert corresponding SHADOW InventoryMovement rows;
10. perform status/audit updates;
11. commit.

Gate OFF/reset continues to take EXCLUSIVE advisory TX lock on the same `(8322,1)` pair. Do not reuse `production_cost_flow`.

## 5. Canonical target key and effectKey v1

All effect keys are <=200 chars. To make collisions impossible without relying on separators inside ids, define a canonical target serialization and hash.

`targetKeyV1` is canonical UTF-8 JSON with keys in the exact order below and Decimal values encoded as exact strings:

- RAIL_LOT: `{"v":1,"d":"RAIL_LOT","railLotId":"..."}`
- BLANK: `{"v":1,"d":"BLANK","materialId":"...","lengthM":"0.0000","detailType":"...","sort":"..."}`
- DETAIL: `{"v":1,"d":"DETAIL","detailId":"...","torcevayaDone":true|false,"ploskostDone":true|false}`
- NOMENCLATURE: `{"v":1,"d":"NOMENCLATURE","nomenclatureId":"..."}`
- PRODUCT: `{"v":1,"d":"PRODUCT","productId":"..."}`

`targetHashV1 = lowerHex(SHA-256(UTF8(targetKeyV1)))`.

Exact `effectKey` grammar:

`imfx1:<semantic-role>:<targetHashV1>[:<qualifier>]`

where semantic-role is one of `receipt`, `consume`, `output`, `adjust`, `writeoff`, `restore`. Qualifiers below are part of the exact identity where specified. Hashing is deterministic, semantic (derived only from the physical target), retry-stable, independent of array order, and bounded well below 200 characters. The unhashed canonical target remains in `targetSnapshot`/target columns for interpretation.

## 6. Inventory contract

- causationKind = `INVENTORY`
- causationId = `Inventory.id`
- kind = `ADJUSTMENT`
- source = canonical `InventoryPhysicalEffect[]`; **never** aggregate InventoryLine deviation
- one InventoryLine may emit several physical movements
- zero-delta effects emit no row
- effects sorted by `targetKeyV1` before insertion
- `effectKey = imfx1:adjust:<targetHashV1>`

DETAIL example with canonical READY +3 and another READY -3 emits **two** movements even when aggregate line deviation is 0.

Physical targets are exactly R-06 grains:
BLANK `(materialId,lengthM fixed-4,detailType,sort)`; DETAIL `(detailId,torcevayaDone,ploskostDone)`; PRODUCT `productId`; NOMENCLATURE `nomenclatureId`.

Inventory first-writer prerequisite: replace `lockBlankSpecs(...)` + `lockBlankStockByIds(...)` as two separately acquired batches with one precomputed globally sorted BlankStock spec set and one acquisition pass. Do not rely on `assertNotLegacyInventoryDraft` to make an AB-BA shape unreachable.

## 7. Supply contract

- causationKind = `SUPPLY`
- causationId = `Supply.id`
- target = PRODUCT
- stockAccountingGeneration is part of effect identity
- accounting watermark is explicitly **processedQtyAfter = newDeducted + newShortfall** after the step
- shortfall contributes to retry/accounting watermark but **never** to movement quantity
- consume `quantityDelta = -toRemove` only; if `toRemove=0`, emit no row

Exact consume key:

`imfx1:consume:<productTargetHash>:g=<generation>:w=<processedQtyAfter>`

A later step in the same generation has a higher processed watermark. Retry of an already processed target emits no physical effect and no second movement.

Ozon cancellation uses a **positive ADJUSTMENT**, not `REVERSAL`. Reason: a SHADOW reset may have deleted the corresponding consume row, the cycle may have begun before SHADOW was enabled, and one generation may contain multiple consume steps. Requiring `reversalOfMovementId` would therefore make cancellation correctness depend on SHADOW history that is not guaranteed to exist.

Exact cancellation key:

`imfx1:restore:<productTargetHash>:g=<generation>:w=<deductedQtyBeforeClose>`

`quantityDelta = +deductedQtyBeforeClose` only when that physical restore actually commits. Zero restore emits no row.

Before Supply writer work, run the required **read-only** production query for `Supply.productId IS NULL AND (deductedQty>0 OR shortfallQty>0)`. No mutation. Regardless of the result, future cancellation must fail closed if an open deducted cycle has restoreQty > 0 but no bound productId.

Before writer implementation, compute the full Supply + ProductStock target set, then acquire one deterministic lock order. Do not rely on API/findMany/per-loop order.

## 8. Production-operation contract

Normal terminal operations use:

- causationKind = `PRODUCTION_OPERATION`
- causationId = retained `ProductionOperation.id`
- actorKind = EMPLOYEE; employeeId and immutable employee display snapshot captured at causal action
- `clientRequestId` is the retained retry identity and must remain unique/atomic with the projection mutation

Effects are derived from retained operation inputs/provenance, grouped by **semantic role + physical target**. Multiple retained lines with the same role/target are summed. Consume and output are not netted against each other merely because they happen to share a target.

Exact keys:

- consume: `imfx1:consume:<targetHashV1>`
- output: `imfx1:output:<targetHashV1>`

TORCOVKA: RailLot CONSUMPTION `-railsTaken`; Blank PRODUCTION_OUTPUT `+sum(outputs by spec)`.

PRISADKA: source Blank/Detail CONSUMPTION; destination Detail PRODUCTION_OUTPUT, using retained `OperationDetailLine` provenance.

UPAKOVKA: source Blank/Detail/Nomenclature CONSUMPTION; Product PRODUCTION_OUTPUT, using retained detail/nomenclature lines and product quantity.

Admin edit/delete paths are **not** allowed to mint new effects under the original `PRODUCTION_OPERATION` causation id. They require a retained `PRODUCTION_OPERATION_MUTATION` identity first.

## 9. R-05 correction mapping

The retained `ProductionOperationCorrection` is an R-05 correction subtype, not a claim that the table implements the full future ProductionOperationMutation model.

Freeze:

- causationKind = `PRODUCTION_OPERATION_MUTATION`
- causationId = `ProductionOperationCorrection.id`
- actorKind = USER; userId = retained adminUserId
- reason = retained correction reason
- stockDomain = RAIL_LOT
- kind = ADJUSTMENT
- quantityDelta = the **actual committed** `deltaReturned`
- `effectKey = imfx1:adjust:<railLotTargetHash>:rails-taken`
- zero committed delta emits no movement
- replay by the same requestId returns the retained correction and emits no second physical effect

Before its SHADOW writer, make READ COMMITTED an explicit transaction-isolation correctness dependency rather than relying on the database default.

## 10. Actor contract

- manual admin Inventory: USER; capture authenticated `User.id` from `requireAdmin()` before entering the physical transaction and pass it explicitly
- manual admin receipt/write-off and production correction/mutation: USER; likewise propagate authenticated `User.id` explicitly instead of re-resolving or discarding it
- manual admin correction/mutation: USER
- terminal production operation: EMPLOYEE
- authenticated user-triggered marketplace sync: USER
- true server automation: SYSTEM with stable server-controlled `systemActorKey`
- marketplace/provider itself is **not** an actor
- `actorDisplaySnapshot` is immutable text captured at the causal action
- consequential `MANUAL` causation requires nonblank reason; retained correction reasons stay required

## 11. Time contract

`recordedAt`:
- DB generated; writer never supplies it;
- TIMESTAMPTZ(3);
- transaction record time, not an intra-transaction cursor/sequence.

`effectiveAt`:
- writer supplied but server controlled;
- one timestamp per causal physical action unless an explicitly documented exception exists;
- never arbitrary client backdating;
- employee `workDate` does not authorize stock backdating.

Freeze current semantics:
- Inventory: server occurrence timestamp of successful conduct;
- Supply deduction/restore: server occurrence timestamp of the accounting mutation, not external marketplace `createdAt`;
- R-05 correction: server occurrence timestamp of accepted correction;
- terminal operation: server occurrence timestamp of the submitted physical operation.

Arbitrary backdating remains P10.

## 12. Snapshot contract

`targetSnapshot` and `causationSnapshot` are versioned JSON objects (`v:1`) sufficient to interpret history after mutable masters/sources change or disappear.

Rules:
- exact Decimal values are strings; never JS float serialization when an exact Decimal exists;
- no monetary cost/value fields;
- BLANK authoritative identity remains physical tuple, not a catalog Detail alias;
- mutable display labels may be snapshot metadata only;
- row ids may be provenance metadata but not the sole BLANK identity;
- Supply causationSnapshot records at least Supply identity, marketplace, externalId, sku, generation, accounting watermark, and action direction;
- production causationSnapshot records operation type, retained clientRequestId, and immutable causal source identifiers needed to explain the effect.

## 13. Zero delta and duplicate/retry rules

Global invariant: `InventoryMovement.quantityDelta` is never zero.

State transitions such as full Supply shortfall, zero cancellation restore, or zero Inventory effect may still update retained business state but write zero InventoryMovement rows.

`UNIQUE(causationKind, causationId, effectKey)` is a final duplicate barrier only. It does not make projection writes idempotent.

Required retry proof by contour:
- Inventory: DRAFT -> CONDUCTED state gate.
- Supply: locked generation + cumulative accounting watermark.
- R-05: requestId serialization + retained correction row + expected-old state.
- terminal production: unique `clientRequestId` atomic with operation/projection.
- every other contour: prove independently or remain BLOCKED.

## 14. Reconciliation diagnostic contract (design only; P3 NOT STARTED)

Every connected P2 writer must expose enough deterministic information to diagnose:

- expected signed movement delta;
- exact physical target;
- causal action;
- projection result after action;
- missing/duplicate movement;
- wrong sign;
- wrong target;
- wrong quantity.

Per-writer diagnostics may be implemented during P2. Projection remains the operational stock source during SHADOW. No authoritative reads. Formal ledger<->projection reconciliation remains PSR-P3.

## 15. CI / integrity contract

Before the first SHADOW writer PR may merge, GitHub CI must provision deterministic disposable PostgreSQL and execute critical DB-backed suites. The mandatory package must cover at minimum:

- SHARED writer gate vs EXCLUSIVE gate/reset concurrency;
- rollback releases advisory locks;
- malformed gate fails closed before projection mutation;
- direct application gate-read static policy;
- raw InventoryMovement TRUNCATE static policy;
- transaction-client assertion;
- duplicate/retry behavior;
- projection + movement atomic rollback;
- concurrent writer behavior;
- zero-delta omission;
- deterministic effect identity and insertion order;
- one movement per semantic physical effect;
- Inventory second conduct/retry cannot duplicate physical mutation;
- dual-active Inventory (`shadow_write=ACTIVE` + `production_cost_flow=ACTIVE`) fails before physical mutation.

`src/server/integrity/**` coverage must actually be invoked by CI. Source-pinning/unit tests alone do not satisfy this gate.

## 16. Implementation order

Evidence-driven sequence from current main:

1. **P2-GUARD/CI prerequisite** — common SHADOW gate-first gateway skeleton, gate-read static policy, TRUNCATE guard, PostgreSQL-backed mandatory CI, and explicit USER actor propagation pattern for admin physical actions; still no physical writer.
2. **Inventory prerequisite fix** — unify BlankStock lock set/order and dual-active fail-closed guard. Then implement Inventory SHADOW writer from canonical `InventoryPhysicalEffect[]`.
3. **Normal terminal production submits** — TORCOVKA/PRISADKA/UPAKOVKA using retained `clientRequestId` and semantic effect extraction.
4. **R-05 correction** — explicit READ COMMITTED + gate-first writer.
5. **Supply prerequisite fix** — read-only production edge query, null-product fail-closed, global Supply/ProductStock lock order; then Supply consume/cancel writer using ADJUSTMENT restore.
6. **Raw receipt/write-off contours** — close `createBatch` retry-recovery decision; implement batch receipt/write-off writers. Add retained request identity to SimplePurchase before its writer.
7. **Admin edit/delete/destructive contours** — add retained mutation/cancellation identity or disable physical mutation. Keep TORCOVKA generic delete disabled. Prevent positive-stock `deleteBatch` from bypassing ledger.
8. Only after all enabled physical contours satisfy the rule may PSR-P2 be called COMPLETE.

This order is not the simplistic Inventory -> Supply -> production sequence; Supply remains behind explicit blockers while terminal submissions already have stronger retained retry identity.

## 17. Roadmap boundaries

Preserve:
- P2: SHADOW dual-write across all enabled physical mutation contours.
- P3: ledger <-> projection diagnostic reconciliation.
- P4: physical count reconciliation / Q3.
- P5: history-preserving correction/cancellation and destructive-path cleanup.
- P6: final epoch / Q6 / opening balances.
- P7: soak.
- P8: authoritative physical-ledger cutover.
- P9: cost flow.
- P10: period close / arbitrary backdating policy.

First Inventory writer != P2 COMPLETE. Per-writer diagnostics != P3 COMPLETE.

## 18. Explicit closeout state for this contract package

- artifact: `audit/08.11-psr-p2-shadow-writer-contract.md`
- package type: architecture / contract only
- runtime InventoryMovement writer: **NO**
- production deploy: **NO**
- production mutation: **NO**
- SHADOW activation: **NO**
- `production_cost_flow` activation: **NO**
- dual-write: **NOT STARTED**
- PSR-P3: **NOT STARTED**
- AUDIT 2: **NOT STARTED**
- NEXT after accepted/merged contract: separate P2-GUARD/CI implementation package, then the first writer only after its contour blockers are closed.
