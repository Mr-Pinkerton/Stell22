# PSR-P2 — Inventory SHADOW writer

Updated: 2026-09-19

Status: **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED**.

Source/main now contains one physical InventoryMovement writer (`conductInventory`). Production application remains `eb02b17`. Production SHADOW writer currently live = **NO**. SHADOW **NOT ACTIVATED**. Production dual-write **NOT STARTED**. Ledger **NOT AUTHORITATIVE**. PSR-P2 globally **NOT COMPLETE**. PSR-P3 **NOT STARTED**.

| | |
| --- | --- |
| Base `origin/main` | `ab81d9b7094d063b3d0ef4ab6ddb117fc18aa878` |
| Exact accepted implementation HEAD | `c58131c02484ae49897fa0259ad623af65708b9e` (PR #22) |
| Merge commit | `2fd138e2c7195b63c2e943d0f85f2b9b0050740c` |
| Accepted PR CI | `35427250317` SUCCESS |
| Post-merge CI | `35427681739` SUCCESS |
| HEAD final review | **PASS** |
| Independent Cursor Opus adversarial review | **PASS — P0=0 / P1=0 / P2=4** (reviewed `28d7247`; no second Opus after W1/W4) |
| Findings | **P0=0 / P1=0 / P2=4**. W1 CLOSED. W2 accepted non-blocking. W3 accepted pre-existing/non-blocking. W4 CLOSED. |
| Production application checkpoint | `eb02b17f488ef9ec13a11dcf158fc169b78e3e37` (Production Deploy `35337215903`) **unchanged** |

The merge SHA is **not** a production application SHA. This package is **not deployed**. SHADOW remains **NOT ACTIVATED**. Dual-write remains **NOT STARTED**.

Canonicality: implementation PR #22 is already merged to `main`. This closeout update is on PR #23 and is **not** canonical on `main` until PR #23 receives HEAD acceptance and is merged. After reviewed merge of PR #23, this file becomes the canonical closeout state on `main`. Nothing in this docs PR is deployed.

```text
Source implementation:
Inventory writer CONNECTED / MERGED

Production application:
still eb02b17
writer NOT DEPLOYED

SHADOW:
NOT ACTIVATED

Production dual-write:
NOT STARTED
```

This package implements the first real physical InventoryMovement writer for `conductInventory()`. It does **not** activate SHADOW, start dual-write, or deploy.

## 1. Scope

Implemented:

- SHADOW-active Inventory conduct writes projection + SHADOW movements in one TX
- `INVENTORY_SHADOW_WRITER_NOT_CONNECTED` removed (writer is connected)
- dual-active fail-closed retained
- `inventoryPhysicalEffectsToMovementEffects` mapping (exhaustive domain switch)
- ACTIVE writer fail-closed on gateway `gateActive` + insert count (`INVENTORY_SHADOW_GATE_INVARIANT_VIOLATION`)
- single `conductedAt` for Inventory `date` and movement `effectiveAt`
- P2-02 mixed BLANK / no-prisadka DETAIL alias lock-plan proof
- P2-03 deletion of obsolete `lockBlankStockByIds`
- P2-05 composed OFF→ON and ON→OFF PostgreSQL proofs
- writer-specific PostgreSQL tests
- insertion-order clarification in `audit/08.11`

Not in this package:

- Supply / terminal / R-05 / receipt / write-off writers
- SHADOW production activation
- `production_cost_flow` activation
- schema / migration
- Correction Center / `ProductionOperationMutation`
- P3 reconciliation
- production deploy / DB access / mutation
- AUDIT 2

## 2. Transaction order

```text
requireAdmin outside TX
→ retained USER actor
BEGIN TX
→ isInventoryMovementShadowWriteActiveForWriter(tx)
   SHARED advisory TX lock + gate read
→ read production_cost_flow
→ SHADOW ACTIVE + cost-flow ACTIVE → INVENTORY_DUAL_ACTIVE_UNSUPPORTED
  before business/stock mutation
→ Inventory FOR UPDATE
→ DRAFT / legacy checks
→ deterministic stock locks
→ STALE_SNAPSHOT
→ deriveInventoryPhysicalEffects
→ conductedAt = one server Date
→ existing projection mutation
→ if SHADOW ACTIVE:
     appendShadowInventoryMovements(...)
     assert result.gateActive === true
     assert result.inserted === nonzero mapped effect count
     else INVENTORY_SHADOW_GATE_INVARIANT_VIOLATION (rollback)
→ InventoryLine / status / ChangeLog
→ COMMIT
```

The SHARED advisory lock is held until COMMIT. The common gateway may acquire/read the writer gate again; the outer TX already holds SHARED so the gate cannot change underneath the physical action.

## 3. SHADOW safety decision

| Gate state | Behavior |
| --- | --- |
| SHADOW missing/inactive + cost-flow inactive | Existing projection. Movements **0**. |
| SHADOW missing/inactive + cost-flow active | Existing ACTIVE cost-flow behavior. Movements **0**. |
| SHADOW ACTIVE + cost-flow inactive | Projection + SHADOW movements atomically |
| SHADOW ACTIVE + cost-flow ACTIVE | Fail `INVENTORY_DUAL_ACTIVE_UNSUPPORTED` before mutation |

`INVENTORY_SHADOW_WRITER_NOT_CONNECTED` is removed.

## 4. Mapping

Sole source: `deriveInventoryPhysicalEffects(...)`.

`inventoryPhysicalEffectsToMovementEffects`:

- `role = adjust`, `kind = ADJUSTMENT`, `quantityDelta = effect.quantityDelta`, no qualifier
- BLANK / DETAIL / PRODUCT / NOMENCLATURE targets as in R-06
- no RAIL_LOT
- zero-delta effects remain in the physical plan; the gateway omits `quantityDelta=0` rows
- expected insert count = mapped effects with `quantityDelta != 0` (valid `0 → 0` included)
- exhaustive `switch` on `stockDomain` with `assertNever` default; TypeScript forces an update if a domain is added

Exact effectKey: `imfx1:adjust:<targetHashV1>`.

Gateway insertion order follows canonical semantic `effectKey` (`audit/08.11` §3). Inventory does not fork gateway ordering.

## 5. Causation / actor / time

```text
causationKind = INVENTORY
causationId = Inventory.id
causationSnapshot = { "v": 1, "d": "INVENTORY", "inventoryId": "<id>", "action": "CONDUCT" }
actorKind = USER (retained requireAdmin() capture)
effectiveAt = conductedAt
Inventory.date = conductedAt (DB DateTime). UI `InventoryDocRow.date` remains the existing YYYY-MM-DD serialize slice and is not the movement identity.
recordedAt = DB-generated
```

## 6. Atomicity and retry

Movement insert is after projection mutation and before status/audit, same TX. Gateway failure rolls back projection. ACTIVE writer captures the gateway result and fails closed (`INVENTORY_SHADOW_GATE_INVARIANT_VIOLATION`) if `gateActive` is false or `inserted` does not equal the nonzero mapped-effect count. That rollback covers projection/status/audit in the same TX.

Retry / COMMIT-UNKNOWN: DRAFT→CONDUCTED is the contour gate. Second conduct is `ALREADY_CONDUCTED`. UNIQUE(causationKind, causationId, effectKey) is not projection idempotency; the rollback test uses it only as an injected failure barrier.

## 6.1 Writer-level non-UTC time proof

HEAD P1 on PR #22: gateway `effectiveAt` is `utcNaiveTimestampString(conductedAt)::timestamp`; Inventory CONDUCTED `date` was Prisma `Date`. Infrastructure proof in `psr-p2-guard-ci.integrity.test.ts` covers only the movement gateway.

Writer proof (transaction-local `conductInventoryInTransaction` under `SET LOCAL TIME ZONE 'America/New_York'`):

- session timezone: **`America/New_York`** (verified `current_setting('TimeZone')`)
- DB `to_char(Inventory.date)` = **`2026-09-19T05:46:11.527`**
- DB `to_char(InventoryMovement.effectiveAt)` = **`2026-09-19T05:46:11.527`**
- DB-level equality: **YES**
- Prisma `Inventory.date.toISOString()` = **`2026-09-19T05:46:11.527Z`**
- Prisma `effectiveAt.toISOString()` = **`2026-09-19T05:46:11.527Z`**
- `getTime()` equality: **YES**
- runtime conduct persistence change: **NOT REQUIRED** (Prisma `updateMany` `date: conductedAt` already stored the same UTC-naive instant as the gateway)

The clock fields above are from one successful disposable-DB run; the assertion is exact column equality, not a frozen wall-clock.

## 7. P2-02 / P2-03 / P2-05

- **P2-02 closed:** mixed BLANK + no-prisadka DETAIL alias / duplicate tuple / reversed order → one canonical spec; helper-level concurrent lock proof. Not a historical deadlock claim. No second legal DRAFT.
- **P2-03 closed:** `lockBlankStockByIds` deleted; no production callers remained.
- **P2-05 closed:** OFF→ON and ON→OFF two-client proofs. Gate EXCLUSIVE cannot commit while conduct holds SHARED. OFF→ON conduct (observed OFF) writes zero movements. ON→OFF writer conduct commits projection+movements atomically, then gate OFF.

## 7.1 Tests

- unit: `inventoryPhysicalEffectsToMovementEffects` mapping / identity / no-netting / reversed input / exhaustive switch pin
- unit: `decideInventoryShadowSafety` writer-connected proceed; dual-active fail-closed; `NOT_CONNECTED` removed
- unit: ACTIVE writer gateway result invariant (`gateActive` + insert count; zero-effect `0 → 0`)
- unit: P2-02 mixed BLANK / alias lock-plan helper
- PostgreSQL: SHADOW-off regression; ±ADJUSTMENT; zero-delta; DETAIL multi-effect; four domains; UNIQUE injected rollback; second-conduct; concurrent same DRAFT; dual-active; malformed gate; actor; effectiveAt; **writer-level non-UTC Inventory.date vs effectiveAt**; P2-05 OFF→ON and ON→OFF; W1 N-row and 0-row conduct proofs
- existing gateway / lock / prerequisite suites retained

## 8. Schema / activation / P2 completeness

- Schema changes: **NONE**
- Migrations: **NONE**
- SHADOW activation: **NO**
- `production_cost_flow`: unchanged / INACTIVE
- Dual-write: **NOT STARTED**
- Production access / mutation / deploy: **NO / NO / NO**
- Runtime P2 global completeness: **NOT COMPLETE** (other contours unconnected)

## 9. Independent Opus P2 disposition

Independent Opus of `28d724736a61d36aea6713b79a652b5e20a8582c`: **PASS — P0=0 / P1=0 / P2=4**. No second Opus after localized W1/W4 corrections on accepted HEAD `c58131c`.

### W1

**CLOSED.**

ACTIVE writer now fails closed if:

- gateway result says gate inactive; or
- inserted count differs from expected nonzero canonical movement-effect count.

Stable error: `INVENTORY_SHADOW_GATE_INVARIANT_VIOLATION`.

### W2

**Accepted / non-blocking test-hardening.**

Current concurrent same-DRAFT test proves one success / one `ALREADY_CONDUCTED` / projection once / movement once. It does not pin that the loser waited on the Inventory row before winner commit. Correctness mechanism remains durable `DRAFT → CONDUCTED`. Does not block closeout.

### W3

**Accepted / pre-existing activation/cutover risk.**

`production_cost_flow` remains INACTIVE. Cost-flow read is not coordinated by the SHADOW advisory lock. This existed before PR #22 and is not fixed by this writer package. Before any future cost-flow activation/cutover there must be a no-in-flight physical operations procedure or an explicit accepted coordination contract.

### W4

**CLOSED.**

Mapping is exhaustive: BLANK / DETAIL / PRODUCT / NOMENCLATURE. Future domain addition must force a compile-time update via `assertNever`.

## 10. NEXT

**NEXT = PSR-P2 NORMAL TERMINAL PRODUCTION SHADOW WRITERS PACKAGE**.

Later scope (not started here):

- normal `submitTorcovka`
- normal `submitPrisadka`
- normal `submitUpakovka`

using retained `ProductionOperation.id` + `clientRequestId` + EMPLOYEE actor + semantic CONSUMPTION / PRODUCTION_OUTPUT effects.

Not R-05. Not Supply. Not SHADOW activation. Not production deploy. Not P3.
