# PSR-P2 — R-05 TORCOVKA correction SHADOW writer

Updated: 2026-09-19

Status: **IMPLEMENTED / MERGED TO MAIN / CI VERIFIED / NOT DEPLOYED / SHADOW NOT ACTIVATED**.

Source/main now contains a physical InventoryMovement writer for R-05 `correctTorcovkaRailsTaken` in addition to Inventory conduct and the three normal terminal submits. Production application remains `eb02b17`. Production writers currently live = **NO**. SHADOW **NOT ACTIVATED**. Production dual-write **NOT STARTED**. Ledger **NOT AUTHORITATIVE**. PSR-P2 globally **NOT COMPLETE**. PSR-P3 **NOT STARTED**.

R-05 identity prerequisite remains separately **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED / DEPLOYED** (`audit/08.09`). Do not conflate that identity deploy with this writer merge.

| | |
| --- | --- |
| Project / repository | Stell22 / `Mr-Pinkerton/Stell22` |
| Base `origin/main` | `f89081f70b7522afe21694527fa22f8f778daafe` |
| Exact accepted implementation HEAD | `c9d3c0b62894d505987685ee399fa99c69ce3126` (PR #26) |
| Merge commit | `debafdae9510acab7182e334fcfe1a5b00608e0d` |
| Exact-head CI | `35443727340` SUCCESS |
| Post-merge CI | `35444256886` SUCCESS |
| HEAD final review | **PASS** |
| Findings | **P0=0 / P1=0 / P2=0** |
| Independent Opus | **SKIPPED BY OWNER COST POLICY** |
| Production application checkpoint | `eb02b17f488ef9ec13a11dcf158fc169b78e3e37` (Production Deploy `35337215903`) **unchanged** |
| Schema / migration | **NONE** |
| SHADOW activation | **NO** |
| `production_cost_flow` | **INACTIVE** (unchanged; not activated) |
| Production access / mutation / deploy | **NO / NO / NO** |
| Runtime P2 global completeness | **NOT COMPLETE** |
| P3 | **NOT STARTED** |
| Ledger authoritative | **NO** |

The merge SHA is **not** a production application SHA. This package is **not deployed**. SHADOW remains **NOT ACTIVATED**. Dual-write remains **NOT STARTED**.

Canonicality: implementation PR #26 is already merged to `main`. This closeout update is on the docs PR and is **not** canonical on `main` until that PR receives HEAD acceptance and is merged. After reviewed merge of the closeout PR, this file becomes the canonical closeout state on `main`. Nothing in this docs PR is deployed.

```text
Source/main connected physical InventoryMovement contours:
1. Inventory conduct
2. normal TORCOVKA
3. normal PRISADKA
4. normal UPAKOVKA
5. R-05 correctTorcovkaRailsTaken

Production application:
still eb02b17
R-05 SHADOW writer NOT DEPLOYED
other source writers NOT DEPLOYED

SHADOW:
NOT ACTIVATED

Production dual-write:
NOT STARTED

production_cost_flow:
INACTIVE
```

R-05 identity prerequisite remains **ARCHITECTURE CLOSED / IMPLEMENTATION VERIFIED / MERGED / DEPLOYED** (`audit/08.09`). This package does **not** redesign that identity.

## HEAD review

Accepted implementation HEAD `c9d3c0b62894d505987685ee399fa99c69ce3126`: **PASS — P0=0 / P1=0 / P2=0**. Independent Opus **SKIPPED BY OWNER COST POLICY**. Exact-head CI `35443727340` SUCCESS. Post-merge CI `35444256886` SUCCESS.

## 1. Scope

Connected:

- `correctTorcovkaRailsTaken` only

Not connected:

- `updateProductionLineQuantity`
- `deleteProductionOperation`
- generic TORCOVKA delete
- Supply
- `createBatch` / `writeOffBatchRemainder` / `createSimplePurchase`
- Correction Center / full `ProductionOperationMutation`
- any other production correction/edit contour

No schema change. No migration.

## 2. Explicit READ COMMITTED

`audit/08.11` blocked this writer until READ COMMITTED is a declared correctness dependency.

The physical R-05 transaction now sets:

```ts
isolationLevel: Prisma.TransactionIsolationLevel.ReadCommitted
```

It does **not** rely on PostgreSQL's default isolation.

The TX performs: requestId lookup → wait for `ProductionOperation FOR UPDATE` → second requestId lookup. The second lookup must observe a correction committed while this transaction was waiting. Replay/CAS semantics are unchanged.

PostgreSQL proof: holder locks the operation; waiter passes first requestId lookup and blocks; a non-conforming writer inserts and COMMITs a matching `ProductionOperationCorrection` for the same `requestId`; holder releases; waiter’s second lookup sees the row and returns replay; no RailLot/op mutation; no second correction; no P2002.

## 3. Gate-first transaction order

Authenticated admin + USER actor are captured **outside** the TX. Ordinary code does not read `inventory_movement_shadow_write`.

```text
requireAdmin() + userMovementActorFromAdmin(admin) outside TX

BEGIN READ COMMITTED
→ SHARED InventoryMovement advisory lock + SHADOW gate read
  (isInventoryMovementShadowWriteActiveForWriter(tx))
→ R-05 requestId advisory lock (8325)
→ lookup retained correction
→ replay if found (no InventoryMovement, including no backfill)
→ ProductionOperation FOR UPDATE
→ second requestId lookup
→ operation/type/paid/CAS validation
→ existing cost-flow decision
→ RailLot / Batch / existing physical locks
→ physical validation
→ insert ProductionOperationCorrection
→ apply physical correction
→ append SHADOW InventoryMovement if outer SHADOW gate was ACTIVE
→ ChangeLog / Batch reopen audit/status
→ COMMIT
```

No physical mutation before SHADOW gate validation. No direct Setting read.

Malformed SHADOW Setting fails closed in the gate helper before the R-05 request lock, retained insert, operation-lock-dependent physical mutation, RailLot/op change, ChangeLog, or InventoryMovement.

## 4. USER actor

`requireAdmin()` already runs outside the transaction. Its result is retained.

```text
actorKind = USER
userId = authenticated admin.id
actorDisplaySnapshot = authenticated admin.name
employeeId = NULL
systemActorKey = NULL
```

The user is not re-resolved inside the TX. Movement `userId` equals `ProductionOperationCorrection.adminUserId`. Mismatch fails closed (`R05_SHADOW_ACTOR_MISMATCH`).

## 5. Retained causation identity

Preserve `ProductionOperationCorrection` exactly (id, requestId, operationId, adminUserId, railLotId, batchId, expectedOldRailsTaken, newRailsTaken, deltaReturned, reason, recordedAt).

- globally unique `requestId`
- advisory namespace `8325`
- replay payload binding
- expected-old CAS
- `STALE_CORRECTION`
- `REQUEST_ID_REUSE`
- same-request replay
- different-request same-old race

Do **not** introduce `ProductionOperationMutation`. `ProductionOperationCorrection.id` is the retained causation identity for this contour.

## 6. Exact physical effect and movement

Successful new correction: `deltaReturned = expectedOldRailsTaken - newRailsTaken`.

Physical projection: `RailLot.remainingQuantity += deltaReturned`.

Exactly one semantic movement:

```text
stockDomain = RAIL_LOT
railLotId = ProductionOperationCorrection.railLotId
kind = ADJUSTMENT
quantityDelta = +deltaReturned
causationKind = PRODUCTION_OPERATION_MUTATION
causationId = ProductionOperationCorrection.id
actorKind = USER
userId = ProductionOperationCorrection.adminUserId
reason = ProductionOperationCorrection.reason
authority = SHADOW
epochId = NULL
```

No BLANK movement. No ProductionOperation output correction movement. No money fields.

`production_cost_flow` ACTIVE vs INACTIVE does not change this RAIL_LOT quantity semantic. ACTIVE monetary correction remains `correctActiveTorcovkaRailsTakenInTx`. `ARCH-P1-001` is not fixed. Cost-flow is not activated.

## 7. Exact effectKey

Accepted contract:

`imfx1:adjust:<railLotTargetHashV1>:rails-taken`

```ts
{
  role: "adjust",
  kind: "ADJUSTMENT",
  quantityDelta: correction.deltaReturned,
  target: { stockDomain: "RAIL_LOT", railLotId: correction.railLotId },
  qualifier: "rails-taken",
}
```

Canonical target/hash machinery is unchanged.

## 8. Causation snapshot v1

```json
{
  "v": 1,
  "d": "PRODUCTION_OPERATION_MUTATION",
  "type": "R05_TORCOVKA_RAILS_TAKEN",
  "correctionId": "...",
  "requestId": "...",
  "operationId": "...",
  "railLotId": "...",
  "batchId": "...",
  "expectedOldRailsTaken": 10,
  "newRailsTaken": 7,
  "deltaReturned": 3
}
```

No money fields. No mutable display labels. No ChangeLog id. Reason lives on the movement `reason` field.

Pinned by `src/server/internal/r05-correction-shadow-write.test.ts`.

## 9. effectiveAt

Causal occurrence time is the retained DB-generated `ProductionOperationCorrection.recordedAt` (`TIMESTAMPTZ(3)` returned by the insert).

Movement `effectiveAt` uses the existing gateway UTC-naive normalization:

```text
correction.recordedAt instant
==
movement.effectiveAt interpreted as UTC
```

Not client time, not `ProductionOperation.workDate`, not `operation.createdAt`. `InventoryMovement.recordedAt` remains its own DB default.

PostgreSQL proof under `SET LOCAL TIME ZONE 'America/New_York'`.

## 10. Replay / no backfill

If `ProductionOperationCorrection` already exists for the same `requestId` and payload matches: return the existing replay result. Do **not** append an InventoryMovement.

Includes:

- correction committed while SHADOW OFF → gate later ON → same requestId retried → **no movement backfill**
- correction committed while SHADOW ON → retry → **no second movement**

The movement is created only in the transaction that creates the retained correction and applies the physical effect.

## 11. Gateway-result invariant

When the outer SHADOW read was ACTIVE, a newly committed nonzero R-05 correction requires:

```text
result.gateActive === true
result.inserted === 1
```

Schema/command already guarantees `deltaReturned > 0`. Mismatch throws `R05_SHADOW_GATE_INVARIANT_VIOLATION` and rolls back. Replay does not call the gateway.

## 12. Writer placement

`correctTorcovkaRailsTaken` remains the `"use server"` action. The physical TX body lives in `src/server/internal/correct-torcovka-rails-taken-tx.ts`. The SHADOW append wrapper is `appendR05CorrectionShadowMovement` → `appendShadowInventoryMovements`.

For a new correction only: persist correction → apply physical effect → append SHADOW if outer gate ACTIVE → ChangeLog / Batch reopen → commit. Movement append failure rolls back the whole correction.

## 13. Proofs (PostgreSQL `stell22_integrity`)

Existing identity suite A–M is retained. SHADOW OFF identity semantics are unchanged.

New writer suite `src/server/integrity/psr-p2-r05-correction-shadow-writer.integrity.test.ts`:

- SHADOW missing/inactive 10→7: one correction, RailLot +3, railsTaken=7, ChangeLog preserved, movements 0; replay stays 0
- SHADOW ACTIVE 10→7: one RAIL_LOT ADJUSTMENT +3, exact effectKey/snapshots/actor/authority
- SHADOW ACTIVE retry: one correction, one physical effect, one movement
- OFF→ON replay: zero backfill
- concurrent identical requestId: one correction, one physical, one movement, waiter replay
- concurrent different expected-old: winner one movement; loser `STALE_CORRECTION` and no movement; refreshed new requestId emits a second movement
- `REQUEST_ID_REUSE`: no extra physical/correction/movement/ChangeLog
- malformed SHADOW gate fails before request-lock effects and physical mutation
- gate-flip: SHARED before later business lock; EXCLUSIVE waits; correction commits with one movement; then gate OFF
- INSERT reject: atomic rollback of correction, RailLot, op, ChangeLog, Batch reopen state, movement
- non-UTC `recordedAt` / `effectiveAt` UTC instant equality
- READ COMMITTED second-lookup replay of a concurrently committed retained row
- ACTIVE cost-flow 10→7 monetary conservation + same RAIL_LOT +3 movement

## 14. Connected vs blocked contours

After this merge, `appendShadowInventoryMovements` runtime callers:

1. Inventory conduct
2. normal TORCOVKA
3. normal PRISADKA
4. normal UPAKOVKA
5. R-05 `correctTorcovkaRailsTaken` (via `appendR05CorrectionShadowMovement`)

Still unconnected: TORCOVKA line quantity edit; PRISADKA/UPAKOVKA edit/delete; generic TORCOVKA delete; Supply; batch receipt/write-off; simple purchase; destructive master paths.

No direct `InventoryMovement.create` / `createMany` / raw INSERT outside the approved gateway.

## 15. Remaining P2 work

Remaining P2 still includes:

- Supply prerequisite fix + Supply writer
- `createBatch` retained retry/recovery identity
- `writeOffBatchRemainder` retained lifecycle/command identity
- `createSimplePurchase` retained request identity
- admin edit/delete/destructive contours or explicit disablement

Generic TORCOVKA delete remains disabled.

P2 **NOT COMPLETE**. P3 **NOT STARTED**. Ledger **NOT AUTHORITATIVE**.

## 16. Local validation (pre-PR)

Local, disposable `stell22_integrity` only. No production access.

| Check | Result |
| --- | --- |
| Unit `npm test` | 883 passed / 94 files |
| R-05 identity unit | `production-operation-correction.test.ts` 13 + migration pin 1 |
| R-05 writer unit | snapshot/effect/actor/invariant 6 + confinement 3 |
| Integrity `npm run test:integrity:ci` | 345 passed / 27 files |
| R-05 identity PostgreSQL | 13 (A–M retained) |
| R-05 writer PostgreSQL | 13 |
| Inventory writer regression | included in integrity CI (15) |
| Terminal writer regression | included in integrity CI (14) |
| Cost-flow package 2 / 3 | included in integrity CI (36 / 43) |
| Typecheck / lint / build | pass / pass / pass |
| Server Action source / CLI imports / append-only / SHADOW gate control / INSERT confinement / manifest | pass |
| `git diff --check` | pass |
| Schema / migration | **NONE** |

## 17. NEXT

**PSR-P2 SUPPLY PREREQUISITE FIX PACKAGE**

Follows `audit/08.11` §16. Later-package scope: read-only production edge query, null-product fail-closed, global Supply/ProductStock lock order. Only after that prerequisite is accepted should the Supply consume/cancel SHADOW writer be implemented using ADJUSTMENT restore semantics.

Do not start Supply in this closeout. Do not combine prerequisite + later Supply writer here. Do not deploy. Do not activate SHADOW. Do not activate `production_cost_flow`. Independent Opus remains **SKIPPED BY OWNER COST POLICY**.
