# План работ по аудиту (2026-07-12)

Источник: `docs/AUDIT-ERP-2026-07-12.md`. Одна задача = одна строка.
Порядок = приоритет фиксов. Развилки (⚠) требуют решения до кода.
Под каждой задачей — пояснение простыми словами (_курсивом_).

---

## Приоритет 1 — Себестоимость (ядро)

- [x] **A2** — валидация/фолбэк при `P1=P2=0`, чтобы материал не обнулялся (`src/lib/cost.ts:88-104`) + unit-тест
  - _Если у партии не заполнили цены сортов (нули), система решает «древесина бесплатная» и пишет 0₽ в себестоимость. Изделие выглядит дешевле, чем есть._
- [x] ⚠ **A1** — РЕШЕНО (Вариант 3): узаконить WAC-по-породе для изделия в v2 + правиле cost-integrity (цены партий стабильны; FIFO-слои не нужны). Уровень детали остаётся по партии. FIFO — backlog, если цены станут волатильны (`src/lib/cost-report.ts:271-294`, `Описание проекта v2.txt` §1)
  - _Материал изделия считается по средней цене породы, а не по конкретной партии, из которой деталь сделали. Две партии дуба (дорогая и дешёвая) — себестоимость выйдет неверной. Заготовка «забывает» свою партию._
- [x] **A3** — `getCostReport` читает `BatchCost` (FINAL) при `frozenAt`, live только для открытых (`src/server/cost.ts:166-204`)
  - _При закрытии партии итог замораживается (снимок), но отчёт этот снимок игнорирует и каждый раз пересчитывает заново. Два разных ответа на один вопрос._
- [x] **A4** — статус FINAL из `frozenAt`/`BatchCost.status`, а не из `ARCHIVED` (`src/lib/cost-report.ts:336`)
  - _Табличка «Итог/Окончательно» показывается по факту «партия в архиве», хотя цифры ещё предварительные и пересчитываются. Пользователь видит ложное «окончательно»._
- [x] ⚠ **A5** — согласовать заморозку: РЕШЕНО оставить код (`paid+closed`), текст v2 приведён к коду (`src/server/cost.ts:330-350`, `Описание проекта v2.txt`)
  - _Спека: замораживать при закрытии партии. Код: ещё и когда вся торцовка оплачена. Рассогласование — надо выбрать правильный вариант и свести документ с кодом._
- [x] **A6** — «работа» в отчёте из факта операций (сделка исполнителя + доля почасовой смены), а не среднее по справочнику (`src/lib/payroll.ts actualProductionRates`, `src/server/cost.ts`)
  - _Оплата труда в себестоимости берётся как средняя ставка из справочника, а не сколько реально начислили за операции. При разных расценках цифра «работа» неверная._

## Приоритет 2 — Производство / склад

- [x] **A7** — серверная валидация длины заготовок ≤ взятые рейки в `submitTorcovka` (`src/server/terminal.ts:209-286`)
  - _Интерфейс не даёт нарезать заготовок больше, чем взято реек, но вызов сервера напрямую этой проверки не делает. Заготовки из воздуха → ломается отход и цена за м³._
- [x] **A8** — та же проверка бюджета реек при правке TORCOVKA вверх (`src/server/production.ts:316-334`)
  - _Если админ правит количество торцовки вручную вверх — та же дыра: можно превысить реально взятые рейки._
- [ ] **A9** — инвентаризация DETAIL: обнулять/перераспределять все buckets (partial присадка) (`src/server/warehouse.ts:315-354`)
  - _Частично готовые детали (часть отверстий просверлена) лежат в промежуточных корзинах. При инвентаризации они не обнуляются → на складе числится больше деталей, чем есть._
- [ ] ⚠ **A10** — split недостачи: сырьё→отход / ГП→«Потеря ГП» по v2 (`src/server/warehouse.ts:288-291`)
  - _При нехватке надо различать: не хватило сырья = отход, не хватило готовой продукции = убыток «Потеря ГП». Сейчас всё в одну кучу, аналитика смазана._

## Приоритет 3 — Накладные / ЗП / финансы

- [x] **A11** — периодный отчёт себестоимости: `getPeriodOverhead(period)` + `getCostReport(period)`, охват по периоду, оценка ₽/м³ по полному производству (`src/server/cost.ts`, `src/lib/report-period.ts`, `src/lib/dates.ts`)
  - _Накладные (аренда, свет) раскидываются на продукцию за всё время, без фильтра дат. На единицу продукции падает то слишком много, то слишком мало._
- [~] **A12** — дата-часть: период прокинут во ВСЕ вкладки (закупки по `purchaseDate`, отход/ЗП по `workDate`/`paidAt`, себестоимость); неделя пт–чт в URL для ЗП; кнопка «Выплатить» (выплата = весь неоплаченный период, не трогается). ОСТАЁТСЯ: TZ-границы в UTC+3 (сейчас локаль хоста)
  - _Зарплатная неделя (пт–чт) и фильтры по датам нарисованы, но не подключены. «Выплатить за неделю» гасит вообще все невыплаченные начисления, фильтр дат — картинка._
- [ ] **A13** — единое правило `confirmed` для cost/deal (карантин) (`finance.ts:1117-1127`)
  - _Суммы с неподтверждённого счёта (карантин) не должны влиять на расчёты, но стоимость партии может измениться от денег, которых ещё нет в движении._

## Приоритет 4 — Безопасность / API / UI

- [ ] **A14** — серверная проверка employee+PIN/session для terminal actions (`src/proxy.ts`, `terminal.ts:209`)
  - _PIN работника проверяется только в браузере. Сервер операции не проверяет — любой с доступом к сети может провести операцию мимо PIN._
- [ ] **A20** — параметры/min stock из БД вместо моков **или** явная пометка prototype (`settings-view.tsx`)
  - _Настройки (порог отхода, минимальные остатки) живут на заглушках. Поменял → после перезагрузки вернулось. Не пишутся в базу._
- [ ] **A22** — DAL/`requireAdmin` в server actions (`session`)
  - _Большинство серверных действий не проверяют, что их вызвал админ. Пока ролей мало — терпимо, но на будущее дыра в правах._

## Приоритет 5 — Очистка архитектуры

- [ ] **A15** — единый путь снапшота: `computeBatchSnapshot` vs `buildBatchSnapshots` (`server/cost.ts:219-251`)
  - _Расчёт себестоимости продублирован в двух местах. Поправят одно — второе тихо разойдётся, снова расхождения._
- [ ] **A17** — `closeMonth` job + оживить/убрать `ProductCost` (`prisma/schema.prisma:703-723`)
  - _Есть заготовка под помесячный расчёт, но она мёртвая. Нет механизма «закрыть месяц и зафиксировать накладные»._
- [ ] **A16** — списывать `ProductExtra` при упаковке (`src/server/terminal.ts:554-667`)
  - _Доп. комплектующие изделия в себестоимость входят, но со склада при упаковке не списываются. Расхождение «посчитали» ≠ «фактически есть»._
- [ ] **A18** — `costMismatch` через `isBatchCostMismatch` + `totalCost` (`purchases.ts:45-50`)
  - _Проверка «оплаченная сумма не сходится» считается по плавающим числам мимо общего модуля. Может ложно срабатывать или пропускать реальное расхождение._
- [ ] **A19** — анти-TOCTOU на `markEmployeePaid` (`updateMany where isPaid=false` + count) (`src/server/payroll.ts:177-207`)
  - _Двойной быстрый клик «Выплачено» (или две вкладки) может создать две выплаты одному человеку. Нет защиты от гонки._
- [ ] **A21** — idempotency key (`clientRequestId unique`) на terminal submit (`terminal.ts`)
  - _Как A19, но про производство: быстрый двойной тап на терминале может продублировать операцию. Нет уникального ключа запроса._
- [ ] **A23** — убрать unused import в `scripts/probe-ozon-supply-get.ts`
  - _Лишний неиспользуемый импорт в служебном скрипте. Просто шум в линтере, удалить._

---

## Тесты (сопровождают фиксы)

- [ ] Unit: `P1=P2=0`; `P1=0 P2>0`
- [ ] Unit: `costStatus` vs `frozenAt`
- [ ] Unit: нет blend при одной партии vs двух
- [x] Unit: период — `getMonthPeriod`/`endOfDay`, `periodFromParams`/round-trip фильтра
- [x] Unit: оценка ₽/м³ по полному производству vs охват периода (A11)
- [ ] Unit: inventory partial buckets
- [ ] Integration: `submitTorcovka` length over budget
- [ ] Integration: `updateProductionLineQuantity` вверх
- [ ] Integration: `conductInventory` inflation
- [ ] Integration: `markEmployeePaid` concurrency
- [ ] Integration: freeze only when paid+closed
- [ ] Integration: `importStatement` idempotency
- [ ] Integration: quarantine vs `syncBatchTotalCost`

---

**Зависимости:** A1 блокирует корректные изделия до решения provenance; A3–A5 делать вместе; A11/A12 независимы.
**Правило:** «готово» = линтер чист + `npx tsc --noEmit` + `npm test` зелёный.
